# Consensus RPC endpoint. Defaults to http://localhost:26657 when unset.
QORE_RPC_URL=http://localhost:26657
# REQUIRED: a deployed CosmWasm contract address (qor1...).
QORE_CONTRACT=
# Smart query message as JSON. Defaults to a CW20 {"token_info":{}} query.
QORE_QUERY_MSG={"token_info":{}}
