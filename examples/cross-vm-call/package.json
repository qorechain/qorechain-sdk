{
  "name": "@qorechain/example-cross-vm-call",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Call an EVM contract from a native account via crossVM, then an atomic batch hitting two VMs.",
  "scripts": {
    "start": "tsx index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cosmjs/tendermint-rpc": "^0.39.0",
    "@qorechain/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3"
  }
}
