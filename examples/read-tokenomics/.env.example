# EVM JSON-RPC endpoint (the qor_* namespace is served here). Defaults to :8545.
QORE_EVM_RPC_URL=http://localhost:8545
# Address used for the xQORE position lookup. Defaults to a sample qor1... address.
QORE_ADDRESS=
