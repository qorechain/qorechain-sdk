// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/reputation/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.reputation.v1";

export interface QueryParamsRequest {
}

export interface QueryParamsResponse {
  alpha: string;
  beta: string;
  gamma: string;
  delta: string;
  lambda: string;
  minScore: string;
}

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { alpha: "", beta: "", gamma: "", delta: "", lambda: "", minScore: "" };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.alpha !== "") {
      writer.uint32(10).string(message.alpha);
    }
    if (message.beta !== "") {
      writer.uint32(18).string(message.beta);
    }
    if (message.gamma !== "") {
      writer.uint32(26).string(message.gamma);
    }
    if (message.delta !== "") {
      writer.uint32(34).string(message.delta);
    }
    if (message.lambda !== "") {
      writer.uint32(42).string(message.lambda);
    }
    if (message.minScore !== "") {
      writer.uint32(50).string(message.minScore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.alpha = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.beta = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.gamma = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.delta = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.lambda = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.minScore = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return {
      alpha: isSet(object.alpha) ? globalThis.String(object.alpha) : "",
      beta: isSet(object.beta) ? globalThis.String(object.beta) : "",
      gamma: isSet(object.gamma) ? globalThis.String(object.gamma) : "",
      delta: isSet(object.delta) ? globalThis.String(object.delta) : "",
      lambda: isSet(object.lambda) ? globalThis.String(object.lambda) : "",
      minScore: isSet(object.minScore)
        ? globalThis.String(object.minScore)
        : isSet(object.min_score)
        ? globalThis.String(object.min_score)
        : "",
    };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.alpha !== "") {
      obj.alpha = message.alpha;
    }
    if (message.beta !== "") {
      obj.beta = message.beta;
    }
    if (message.gamma !== "") {
      obj.gamma = message.gamma;
    }
    if (message.delta !== "") {
      obj.delta = message.delta;
    }
    if (message.lambda !== "") {
      obj.lambda = message.lambda;
    }
    if (message.minScore !== "") {
      obj.minScore = message.minScore;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.alpha = object.alpha ?? "";
    message.beta = object.beta ?? "";
    message.gamma = object.gamma ?? "";
    message.delta = object.delta ?? "";
    message.lambda = object.lambda ?? "";
    message.minScore = object.minScore ?? "";
    return message;
  },
};

/** Query defines the reputation module's gRPC query service. */
export type QueryDefinition = typeof QueryDefinition;
export const QueryDefinition = {
  name: "Query",
  fullName: "qorechain.reputation.v1.Query",
  methods: {
    /** Params returns the reputation formula weights (R_i = α·S_i + β·P_i + γ·C_i + δ·T_i). */
    params: {
      name: "Params",
      requestType: QueryParamsRequest as typeof QueryParamsRequest,
      requestStream: false,
      responseType: QueryParamsResponse as typeof QueryParamsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
