// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/bridge/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.bridge.v1";

/**
 * MsgUpdateChainConfig sets/replaces a chain's bridge configuration. Empty
 * string / zero fields fall back to the existing config (merge semantics in the
 * handler), so a caller can flip just `status`+one verifier flag to activate.
 */
export interface MsgUpdateChainConfig {
  admin: string;
  chainId: string;
  /** contract/program address on the external chain */
  bridgeContract: string;
  confirmationsRequired: number;
  /** ChainArchitecture string (empty keeps existing) */
  architecture: string;
  /** "active" | "paused" | "pending" (empty keeps existing) */
  status: string;
  /** Exactly one verifier flag should be set when activating trustless mode. */
  verifier: string;
  /** hex topic0 of the lock event (EVM/light-client chains) */
  lockEventSig: string;
}

export interface MsgUpdateChainConfigResponse {
}

/**
 * MsgSetVerifierBootstrap installs a verifier trust root. Exactly one of the
 * sub-messages must be populated; the handler routes by which is non-nil.
 */
export interface MsgSetVerifierBootstrap {
  admin: string;
  chainId: string;
  wormhole?: WormholeGuardianSet | undefined;
  ed25519?: ValidatorQuorum | undefined;
  bls?: ValidatorQuorum | undefined;
  bitcoin?: BitcoinCheckpoint | undefined;
  starknetStateRoot: Uint8Array;
}

export interface MsgSetVerifierBootstrapResponse {
}

export interface WormholeGuardianSet {
  /** 20-byte guardian eth addresses */
  addresses: Uint8Array[];
  quorum: number;
}

export interface ValidatorQuorum {
  /** ed25519 (32B) or BLS (48B) pubkeys */
  pubkeys: Uint8Array[];
  threshold: number;
}

export interface BitcoinCheckpoint {
  blockHash: Uint8Array;
  minConfs: number;
}

export interface MsgBridgeDeposit {
  sender: string;
  sourceChain: string;
  sourceTxHash: string;
  asset: string;
  amount: string;
  bridgeValidatorSigs: Uint8Array;
  pqcCommitment: Uint8Array;
}

export interface MsgBridgeDepositResponse {
}

export interface MsgBridgeWithdraw {
  sender: string;
  destinationChain: string;
  destinationAddress: string;
  asset: string;
  amount: string;
}

export interface MsgBridgeWithdrawResponse {
}

export interface MsgRegisterBridgeValidator {
  validatorAddress: string;
  pqcPubkey: Uint8Array;
  supportedChains: string[];
}

export interface MsgRegisterBridgeValidatorResponse {
}

export interface MsgBridgeAttestation {
  validator: string;
  chain: string;
  eventType: string;
  operationId: string;
  txHash: string;
  amount: string;
  asset: string;
  proof: Uint8Array;
  pqcSignature: Uint8Array;
}

export interface MsgBridgeAttestationResponse {
}

export interface MsgUpdateEthLightClient {
  relayer: string;
  /**
   * Encoded Altair LightClientUpdate bundle verified against the stored sync
   * committee before advancing the on-chain light client.
   */
  update: Uint8Array;
}

export interface MsgUpdateEthLightClientResponse {
}

function createBaseMsgUpdateChainConfig(): MsgUpdateChainConfig {
  return {
    admin: "",
    chainId: "",
    bridgeContract: "",
    confirmationsRequired: 0,
    architecture: "",
    status: "",
    verifier: "",
    lockEventSig: "",
  };
}

export const MsgUpdateChainConfig: MessageFns<MsgUpdateChainConfig> = {
  encode(message: MsgUpdateChainConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.admin !== "") {
      writer.uint32(10).string(message.admin);
    }
    if (message.chainId !== "") {
      writer.uint32(18).string(message.chainId);
    }
    if (message.bridgeContract !== "") {
      writer.uint32(26).string(message.bridgeContract);
    }
    if (message.confirmationsRequired !== 0) {
      writer.uint32(32).uint32(message.confirmationsRequired);
    }
    if (message.architecture !== "") {
      writer.uint32(42).string(message.architecture);
    }
    if (message.status !== "") {
      writer.uint32(50).string(message.status);
    }
    if (message.verifier !== "") {
      writer.uint32(58).string(message.verifier);
    }
    if (message.lockEventSig !== "") {
      writer.uint32(66).string(message.lockEventSig);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateChainConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateChainConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.admin = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chainId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.bridgeContract = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.confirmationsRequired = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.architecture = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.verifier = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.lockEventSig = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateChainConfig {
    return {
      admin: isSet(object.admin) ? globalThis.String(object.admin) : "",
      chainId: isSet(object.chainId)
        ? globalThis.String(object.chainId)
        : isSet(object.chain_id)
        ? globalThis.String(object.chain_id)
        : "",
      bridgeContract: isSet(object.bridgeContract)
        ? globalThis.String(object.bridgeContract)
        : isSet(object.bridge_contract)
        ? globalThis.String(object.bridge_contract)
        : "",
      confirmationsRequired: isSet(object.confirmationsRequired)
        ? globalThis.Number(object.confirmationsRequired)
        : isSet(object.confirmations_required)
        ? globalThis.Number(object.confirmations_required)
        : 0,
      architecture: isSet(object.architecture) ? globalThis.String(object.architecture) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      verifier: isSet(object.verifier) ? globalThis.String(object.verifier) : "",
      lockEventSig: isSet(object.lockEventSig)
        ? globalThis.String(object.lockEventSig)
        : isSet(object.lock_event_sig)
        ? globalThis.String(object.lock_event_sig)
        : "",
    };
  },

  toJSON(message: MsgUpdateChainConfig): unknown {
    const obj: any = {};
    if (message.admin !== "") {
      obj.admin = message.admin;
    }
    if (message.chainId !== "") {
      obj.chainId = message.chainId;
    }
    if (message.bridgeContract !== "") {
      obj.bridgeContract = message.bridgeContract;
    }
    if (message.confirmationsRequired !== 0) {
      obj.confirmationsRequired = Math.round(message.confirmationsRequired);
    }
    if (message.architecture !== "") {
      obj.architecture = message.architecture;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.verifier !== "") {
      obj.verifier = message.verifier;
    }
    if (message.lockEventSig !== "") {
      obj.lockEventSig = message.lockEventSig;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateChainConfig>): MsgUpdateChainConfig {
    return MsgUpdateChainConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgUpdateChainConfig>): MsgUpdateChainConfig {
    const message = createBaseMsgUpdateChainConfig();
    message.admin = object.admin ?? "";
    message.chainId = object.chainId ?? "";
    message.bridgeContract = object.bridgeContract ?? "";
    message.confirmationsRequired = object.confirmationsRequired ?? 0;
    message.architecture = object.architecture ?? "";
    message.status = object.status ?? "";
    message.verifier = object.verifier ?? "";
    message.lockEventSig = object.lockEventSig ?? "";
    return message;
  },
};

function createBaseMsgUpdateChainConfigResponse(): MsgUpdateChainConfigResponse {
  return {};
}

export const MsgUpdateChainConfigResponse: MessageFns<MsgUpdateChainConfigResponse> = {
  encode(_: MsgUpdateChainConfigResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateChainConfigResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateChainConfigResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateChainConfigResponse {
    return {};
  },

  toJSON(_: MsgUpdateChainConfigResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateChainConfigResponse>): MsgUpdateChainConfigResponse {
    return MsgUpdateChainConfigResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgUpdateChainConfigResponse>): MsgUpdateChainConfigResponse {
    const message = createBaseMsgUpdateChainConfigResponse();
    return message;
  },
};

function createBaseMsgSetVerifierBootstrap(): MsgSetVerifierBootstrap {
  return {
    admin: "",
    chainId: "",
    wormhole: undefined,
    ed25519: undefined,
    bls: undefined,
    bitcoin: undefined,
    starknetStateRoot: new Uint8Array(0),
  };
}

export const MsgSetVerifierBootstrap: MessageFns<MsgSetVerifierBootstrap> = {
  encode(message: MsgSetVerifierBootstrap, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.admin !== "") {
      writer.uint32(10).string(message.admin);
    }
    if (message.chainId !== "") {
      writer.uint32(18).string(message.chainId);
    }
    if (message.wormhole !== undefined) {
      WormholeGuardianSet.encode(message.wormhole, writer.uint32(26).fork()).join();
    }
    if (message.ed25519 !== undefined) {
      ValidatorQuorum.encode(message.ed25519, writer.uint32(34).fork()).join();
    }
    if (message.bls !== undefined) {
      ValidatorQuorum.encode(message.bls, writer.uint32(42).fork()).join();
    }
    if (message.bitcoin !== undefined) {
      BitcoinCheckpoint.encode(message.bitcoin, writer.uint32(50).fork()).join();
    }
    if (message.starknetStateRoot.length !== 0) {
      writer.uint32(58).bytes(message.starknetStateRoot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgSetVerifierBootstrap {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSetVerifierBootstrap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.admin = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chainId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.wormhole = WormholeGuardianSet.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ed25519 = ValidatorQuorum.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.bls = ValidatorQuorum.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.bitcoin = BitcoinCheckpoint.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.starknetStateRoot = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgSetVerifierBootstrap {
    return {
      admin: isSet(object.admin) ? globalThis.String(object.admin) : "",
      chainId: isSet(object.chainId)
        ? globalThis.String(object.chainId)
        : isSet(object.chain_id)
        ? globalThis.String(object.chain_id)
        : "",
      wormhole: isSet(object.wormhole) ? WormholeGuardianSet.fromJSON(object.wormhole) : undefined,
      ed25519: isSet(object.ed25519) ? ValidatorQuorum.fromJSON(object.ed25519) : undefined,
      bls: isSet(object.bls) ? ValidatorQuorum.fromJSON(object.bls) : undefined,
      bitcoin: isSet(object.bitcoin) ? BitcoinCheckpoint.fromJSON(object.bitcoin) : undefined,
      starknetStateRoot: isSet(object.starknetStateRoot)
        ? bytesFromBase64(object.starknetStateRoot)
        : isSet(object.starknet_state_root)
        ? bytesFromBase64(object.starknet_state_root)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgSetVerifierBootstrap): unknown {
    const obj: any = {};
    if (message.admin !== "") {
      obj.admin = message.admin;
    }
    if (message.chainId !== "") {
      obj.chainId = message.chainId;
    }
    if (message.wormhole !== undefined) {
      obj.wormhole = WormholeGuardianSet.toJSON(message.wormhole);
    }
    if (message.ed25519 !== undefined) {
      obj.ed25519 = ValidatorQuorum.toJSON(message.ed25519);
    }
    if (message.bls !== undefined) {
      obj.bls = ValidatorQuorum.toJSON(message.bls);
    }
    if (message.bitcoin !== undefined) {
      obj.bitcoin = BitcoinCheckpoint.toJSON(message.bitcoin);
    }
    if (message.starknetStateRoot.length !== 0) {
      obj.starknetStateRoot = base64FromBytes(message.starknetStateRoot);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgSetVerifierBootstrap>): MsgSetVerifierBootstrap {
    return MsgSetVerifierBootstrap.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgSetVerifierBootstrap>): MsgSetVerifierBootstrap {
    const message = createBaseMsgSetVerifierBootstrap();
    message.admin = object.admin ?? "";
    message.chainId = object.chainId ?? "";
    message.wormhole = (object.wormhole !== undefined && object.wormhole !== null)
      ? WormholeGuardianSet.fromPartial(object.wormhole)
      : undefined;
    message.ed25519 = (object.ed25519 !== undefined && object.ed25519 !== null)
      ? ValidatorQuorum.fromPartial(object.ed25519)
      : undefined;
    message.bls = (object.bls !== undefined && object.bls !== null)
      ? ValidatorQuorum.fromPartial(object.bls)
      : undefined;
    message.bitcoin = (object.bitcoin !== undefined && object.bitcoin !== null)
      ? BitcoinCheckpoint.fromPartial(object.bitcoin)
      : undefined;
    message.starknetStateRoot = object.starknetStateRoot ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgSetVerifierBootstrapResponse(): MsgSetVerifierBootstrapResponse {
  return {};
}

export const MsgSetVerifierBootstrapResponse: MessageFns<MsgSetVerifierBootstrapResponse> = {
  encode(_: MsgSetVerifierBootstrapResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgSetVerifierBootstrapResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSetVerifierBootstrapResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgSetVerifierBootstrapResponse {
    return {};
  },

  toJSON(_: MsgSetVerifierBootstrapResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgSetVerifierBootstrapResponse>): MsgSetVerifierBootstrapResponse {
    return MsgSetVerifierBootstrapResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgSetVerifierBootstrapResponse>): MsgSetVerifierBootstrapResponse {
    const message = createBaseMsgSetVerifierBootstrapResponse();
    return message;
  },
};

function createBaseWormholeGuardianSet(): WormholeGuardianSet {
  return { addresses: [], quorum: 0 };
}

export const WormholeGuardianSet: MessageFns<WormholeGuardianSet> = {
  encode(message: WormholeGuardianSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.addresses) {
      writer.uint32(10).bytes(v!);
    }
    if (message.quorum !== 0) {
      writer.uint32(16).uint32(message.quorum);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WormholeGuardianSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWormholeGuardianSet();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.addresses.push(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.quorum = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WormholeGuardianSet {
    return {
      addresses: globalThis.Array.isArray(object?.addresses)
        ? object.addresses.map((e: any) => bytesFromBase64(e))
        : [],
      quorum: isSet(object.quorum) ? globalThis.Number(object.quorum) : 0,
    };
  },

  toJSON(message: WormholeGuardianSet): unknown {
    const obj: any = {};
    if (message.addresses?.length) {
      obj.addresses = message.addresses.map((e) => base64FromBytes(e));
    }
    if (message.quorum !== 0) {
      obj.quorum = Math.round(message.quorum);
    }
    return obj;
  },

  create(base?: DeepPartial<WormholeGuardianSet>): WormholeGuardianSet {
    return WormholeGuardianSet.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WormholeGuardianSet>): WormholeGuardianSet {
    const message = createBaseWormholeGuardianSet();
    message.addresses = object.addresses?.map((e) => e) || [];
    message.quorum = object.quorum ?? 0;
    return message;
  },
};

function createBaseValidatorQuorum(): ValidatorQuorum {
  return { pubkeys: [], threshold: 0 };
}

export const ValidatorQuorum: MessageFns<ValidatorQuorum> = {
  encode(message: ValidatorQuorum, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.pubkeys) {
      writer.uint32(10).bytes(v!);
    }
    if (message.threshold !== 0) {
      writer.uint32(16).uint32(message.threshold);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidatorQuorum {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidatorQuorum();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pubkeys.push(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.threshold = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValidatorQuorum {
    return {
      pubkeys: globalThis.Array.isArray(object?.pubkeys) ? object.pubkeys.map((e: any) => bytesFromBase64(e)) : [],
      threshold: isSet(object.threshold) ? globalThis.Number(object.threshold) : 0,
    };
  },

  toJSON(message: ValidatorQuorum): unknown {
    const obj: any = {};
    if (message.pubkeys?.length) {
      obj.pubkeys = message.pubkeys.map((e) => base64FromBytes(e));
    }
    if (message.threshold !== 0) {
      obj.threshold = Math.round(message.threshold);
    }
    return obj;
  },

  create(base?: DeepPartial<ValidatorQuorum>): ValidatorQuorum {
    return ValidatorQuorum.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ValidatorQuorum>): ValidatorQuorum {
    const message = createBaseValidatorQuorum();
    message.pubkeys = object.pubkeys?.map((e) => e) || [];
    message.threshold = object.threshold ?? 0;
    return message;
  },
};

function createBaseBitcoinCheckpoint(): BitcoinCheckpoint {
  return { blockHash: new Uint8Array(0), minConfs: 0 };
}

export const BitcoinCheckpoint: MessageFns<BitcoinCheckpoint> = {
  encode(message: BitcoinCheckpoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.blockHash.length !== 0) {
      writer.uint32(10).bytes(message.blockHash);
    }
    if (message.minConfs !== 0) {
      writer.uint32(16).uint32(message.minConfs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BitcoinCheckpoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBitcoinCheckpoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.blockHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.minConfs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BitcoinCheckpoint {
    return {
      blockHash: isSet(object.blockHash)
        ? bytesFromBase64(object.blockHash)
        : isSet(object.block_hash)
        ? bytesFromBase64(object.block_hash)
        : new Uint8Array(0),
      minConfs: isSet(object.minConfs)
        ? globalThis.Number(object.minConfs)
        : isSet(object.min_confs)
        ? globalThis.Number(object.min_confs)
        : 0,
    };
  },

  toJSON(message: BitcoinCheckpoint): unknown {
    const obj: any = {};
    if (message.blockHash.length !== 0) {
      obj.blockHash = base64FromBytes(message.blockHash);
    }
    if (message.minConfs !== 0) {
      obj.minConfs = Math.round(message.minConfs);
    }
    return obj;
  },

  create(base?: DeepPartial<BitcoinCheckpoint>): BitcoinCheckpoint {
    return BitcoinCheckpoint.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BitcoinCheckpoint>): BitcoinCheckpoint {
    const message = createBaseBitcoinCheckpoint();
    message.blockHash = object.blockHash ?? new Uint8Array(0);
    message.minConfs = object.minConfs ?? 0;
    return message;
  },
};

function createBaseMsgBridgeDeposit(): MsgBridgeDeposit {
  return {
    sender: "",
    sourceChain: "",
    sourceTxHash: "",
    asset: "",
    amount: "",
    bridgeValidatorSigs: new Uint8Array(0),
    pqcCommitment: new Uint8Array(0),
  };
}

export const MsgBridgeDeposit: MessageFns<MsgBridgeDeposit> = {
  encode(message: MsgBridgeDeposit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.sourceChain !== "") {
      writer.uint32(18).string(message.sourceChain);
    }
    if (message.sourceTxHash !== "") {
      writer.uint32(26).string(message.sourceTxHash);
    }
    if (message.asset !== "") {
      writer.uint32(34).string(message.asset);
    }
    if (message.amount !== "") {
      writer.uint32(42).string(message.amount);
    }
    if (message.bridgeValidatorSigs.length !== 0) {
      writer.uint32(50).bytes(message.bridgeValidatorSigs);
    }
    if (message.pqcCommitment.length !== 0) {
      writer.uint32(58).bytes(message.pqcCommitment);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgBridgeDeposit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgBridgeDeposit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourceChain = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sourceTxHash = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.asset = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.amount = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.bridgeValidatorSigs = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.pqcCommitment = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgBridgeDeposit {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      sourceChain: isSet(object.sourceChain)
        ? globalThis.String(object.sourceChain)
        : isSet(object.source_chain)
        ? globalThis.String(object.source_chain)
        : "",
      sourceTxHash: isSet(object.sourceTxHash)
        ? globalThis.String(object.sourceTxHash)
        : isSet(object.source_tx_hash)
        ? globalThis.String(object.source_tx_hash)
        : "",
      asset: isSet(object.asset) ? globalThis.String(object.asset) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      bridgeValidatorSigs: isSet(object.bridgeValidatorSigs)
        ? bytesFromBase64(object.bridgeValidatorSigs)
        : isSet(object.bridge_validator_sigs)
        ? bytesFromBase64(object.bridge_validator_sigs)
        : new Uint8Array(0),
      pqcCommitment: isSet(object.pqcCommitment)
        ? bytesFromBase64(object.pqcCommitment)
        : isSet(object.pqc_commitment)
        ? bytesFromBase64(object.pqc_commitment)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgBridgeDeposit): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.sourceChain !== "") {
      obj.sourceChain = message.sourceChain;
    }
    if (message.sourceTxHash !== "") {
      obj.sourceTxHash = message.sourceTxHash;
    }
    if (message.asset !== "") {
      obj.asset = message.asset;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.bridgeValidatorSigs.length !== 0) {
      obj.bridgeValidatorSigs = base64FromBytes(message.bridgeValidatorSigs);
    }
    if (message.pqcCommitment.length !== 0) {
      obj.pqcCommitment = base64FromBytes(message.pqcCommitment);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgBridgeDeposit>): MsgBridgeDeposit {
    return MsgBridgeDeposit.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgBridgeDeposit>): MsgBridgeDeposit {
    const message = createBaseMsgBridgeDeposit();
    message.sender = object.sender ?? "";
    message.sourceChain = object.sourceChain ?? "";
    message.sourceTxHash = object.sourceTxHash ?? "";
    message.asset = object.asset ?? "";
    message.amount = object.amount ?? "";
    message.bridgeValidatorSigs = object.bridgeValidatorSigs ?? new Uint8Array(0);
    message.pqcCommitment = object.pqcCommitment ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgBridgeDepositResponse(): MsgBridgeDepositResponse {
  return {};
}

export const MsgBridgeDepositResponse: MessageFns<MsgBridgeDepositResponse> = {
  encode(_: MsgBridgeDepositResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgBridgeDepositResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgBridgeDepositResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgBridgeDepositResponse {
    return {};
  },

  toJSON(_: MsgBridgeDepositResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgBridgeDepositResponse>): MsgBridgeDepositResponse {
    return MsgBridgeDepositResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgBridgeDepositResponse>): MsgBridgeDepositResponse {
    const message = createBaseMsgBridgeDepositResponse();
    return message;
  },
};

function createBaseMsgBridgeWithdraw(): MsgBridgeWithdraw {
  return { sender: "", destinationChain: "", destinationAddress: "", asset: "", amount: "" };
}

export const MsgBridgeWithdraw: MessageFns<MsgBridgeWithdraw> = {
  encode(message: MsgBridgeWithdraw, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.destinationChain !== "") {
      writer.uint32(18).string(message.destinationChain);
    }
    if (message.destinationAddress !== "") {
      writer.uint32(26).string(message.destinationAddress);
    }
    if (message.asset !== "") {
      writer.uint32(34).string(message.asset);
    }
    if (message.amount !== "") {
      writer.uint32(42).string(message.amount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgBridgeWithdraw {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgBridgeWithdraw();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.destinationChain = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.destinationAddress = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.asset = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.amount = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgBridgeWithdraw {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      destinationChain: isSet(object.destinationChain)
        ? globalThis.String(object.destinationChain)
        : isSet(object.destination_chain)
        ? globalThis.String(object.destination_chain)
        : "",
      destinationAddress: isSet(object.destinationAddress)
        ? globalThis.String(object.destinationAddress)
        : isSet(object.destination_address)
        ? globalThis.String(object.destination_address)
        : "",
      asset: isSet(object.asset) ? globalThis.String(object.asset) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
    };
  },

  toJSON(message: MsgBridgeWithdraw): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.destinationChain !== "") {
      obj.destinationChain = message.destinationChain;
    }
    if (message.destinationAddress !== "") {
      obj.destinationAddress = message.destinationAddress;
    }
    if (message.asset !== "") {
      obj.asset = message.asset;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgBridgeWithdraw>): MsgBridgeWithdraw {
    return MsgBridgeWithdraw.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgBridgeWithdraw>): MsgBridgeWithdraw {
    const message = createBaseMsgBridgeWithdraw();
    message.sender = object.sender ?? "";
    message.destinationChain = object.destinationChain ?? "";
    message.destinationAddress = object.destinationAddress ?? "";
    message.asset = object.asset ?? "";
    message.amount = object.amount ?? "";
    return message;
  },
};

function createBaseMsgBridgeWithdrawResponse(): MsgBridgeWithdrawResponse {
  return {};
}

export const MsgBridgeWithdrawResponse: MessageFns<MsgBridgeWithdrawResponse> = {
  encode(_: MsgBridgeWithdrawResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgBridgeWithdrawResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgBridgeWithdrawResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgBridgeWithdrawResponse {
    return {};
  },

  toJSON(_: MsgBridgeWithdrawResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgBridgeWithdrawResponse>): MsgBridgeWithdrawResponse {
    return MsgBridgeWithdrawResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgBridgeWithdrawResponse>): MsgBridgeWithdrawResponse {
    const message = createBaseMsgBridgeWithdrawResponse();
    return message;
  },
};

function createBaseMsgRegisterBridgeValidator(): MsgRegisterBridgeValidator {
  return { validatorAddress: "", pqcPubkey: new Uint8Array(0), supportedChains: [] };
}

export const MsgRegisterBridgeValidator: MessageFns<MsgRegisterBridgeValidator> = {
  encode(message: MsgRegisterBridgeValidator, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.validatorAddress !== "") {
      writer.uint32(10).string(message.validatorAddress);
    }
    if (message.pqcPubkey.length !== 0) {
      writer.uint32(18).bytes(message.pqcPubkey);
    }
    for (const v of message.supportedChains) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterBridgeValidator {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterBridgeValidator();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.validatorAddress = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pqcPubkey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.supportedChains.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterBridgeValidator {
    return {
      validatorAddress: isSet(object.validatorAddress)
        ? globalThis.String(object.validatorAddress)
        : isSet(object.validator_address)
        ? globalThis.String(object.validator_address)
        : "",
      pqcPubkey: isSet(object.pqcPubkey)
        ? bytesFromBase64(object.pqcPubkey)
        : isSet(object.pqc_pubkey)
        ? bytesFromBase64(object.pqc_pubkey)
        : new Uint8Array(0),
      supportedChains: globalThis.Array.isArray(object?.supportedChains)
        ? object.supportedChains.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.supported_chains)
        ? object.supported_chains.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: MsgRegisterBridgeValidator): unknown {
    const obj: any = {};
    if (message.validatorAddress !== "") {
      obj.validatorAddress = message.validatorAddress;
    }
    if (message.pqcPubkey.length !== 0) {
      obj.pqcPubkey = base64FromBytes(message.pqcPubkey);
    }
    if (message.supportedChains?.length) {
      obj.supportedChains = message.supportedChains;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterBridgeValidator>): MsgRegisterBridgeValidator {
    return MsgRegisterBridgeValidator.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterBridgeValidator>): MsgRegisterBridgeValidator {
    const message = createBaseMsgRegisterBridgeValidator();
    message.validatorAddress = object.validatorAddress ?? "";
    message.pqcPubkey = object.pqcPubkey ?? new Uint8Array(0);
    message.supportedChains = object.supportedChains?.map((e) => e) || [];
    return message;
  },
};

function createBaseMsgRegisterBridgeValidatorResponse(): MsgRegisterBridgeValidatorResponse {
  return {};
}

export const MsgRegisterBridgeValidatorResponse: MessageFns<MsgRegisterBridgeValidatorResponse> = {
  encode(_: MsgRegisterBridgeValidatorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterBridgeValidatorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterBridgeValidatorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRegisterBridgeValidatorResponse {
    return {};
  },

  toJSON(_: MsgRegisterBridgeValidatorResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterBridgeValidatorResponse>): MsgRegisterBridgeValidatorResponse {
    return MsgRegisterBridgeValidatorResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgRegisterBridgeValidatorResponse>): MsgRegisterBridgeValidatorResponse {
    const message = createBaseMsgRegisterBridgeValidatorResponse();
    return message;
  },
};

function createBaseMsgBridgeAttestation(): MsgBridgeAttestation {
  return {
    validator: "",
    chain: "",
    eventType: "",
    operationId: "",
    txHash: "",
    amount: "",
    asset: "",
    proof: new Uint8Array(0),
    pqcSignature: new Uint8Array(0),
  };
}

export const MsgBridgeAttestation: MessageFns<MsgBridgeAttestation> = {
  encode(message: MsgBridgeAttestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.validator !== "") {
      writer.uint32(10).string(message.validator);
    }
    if (message.chain !== "") {
      writer.uint32(18).string(message.chain);
    }
    if (message.eventType !== "") {
      writer.uint32(26).string(message.eventType);
    }
    if (message.operationId !== "") {
      writer.uint32(34).string(message.operationId);
    }
    if (message.txHash !== "") {
      writer.uint32(42).string(message.txHash);
    }
    if (message.amount !== "") {
      writer.uint32(50).string(message.amount);
    }
    if (message.asset !== "") {
      writer.uint32(58).string(message.asset);
    }
    if (message.proof.length !== 0) {
      writer.uint32(66).bytes(message.proof);
    }
    if (message.pqcSignature.length !== 0) {
      writer.uint32(74).bytes(message.pqcSignature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgBridgeAttestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgBridgeAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.validator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chain = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.operationId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.txHash = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.amount = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.asset = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.proof = reader.bytes();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.pqcSignature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgBridgeAttestation {
    return {
      validator: isSet(object.validator) ? globalThis.String(object.validator) : "",
      chain: isSet(object.chain) ? globalThis.String(object.chain) : "",
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
        ? globalThis.String(object.event_type)
        : "",
      operationId: isSet(object.operationId)
        ? globalThis.String(object.operationId)
        : isSet(object.operation_id)
        ? globalThis.String(object.operation_id)
        : "",
      txHash: isSet(object.txHash)
        ? globalThis.String(object.txHash)
        : isSet(object.tx_hash)
        ? globalThis.String(object.tx_hash)
        : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      asset: isSet(object.asset) ? globalThis.String(object.asset) : "",
      proof: isSet(object.proof) ? bytesFromBase64(object.proof) : new Uint8Array(0),
      pqcSignature: isSet(object.pqcSignature)
        ? bytesFromBase64(object.pqcSignature)
        : isSet(object.pqc_signature)
        ? bytesFromBase64(object.pqc_signature)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgBridgeAttestation): unknown {
    const obj: any = {};
    if (message.validator !== "") {
      obj.validator = message.validator;
    }
    if (message.chain !== "") {
      obj.chain = message.chain;
    }
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.operationId !== "") {
      obj.operationId = message.operationId;
    }
    if (message.txHash !== "") {
      obj.txHash = message.txHash;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.asset !== "") {
      obj.asset = message.asset;
    }
    if (message.proof.length !== 0) {
      obj.proof = base64FromBytes(message.proof);
    }
    if (message.pqcSignature.length !== 0) {
      obj.pqcSignature = base64FromBytes(message.pqcSignature);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgBridgeAttestation>): MsgBridgeAttestation {
    return MsgBridgeAttestation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgBridgeAttestation>): MsgBridgeAttestation {
    const message = createBaseMsgBridgeAttestation();
    message.validator = object.validator ?? "";
    message.chain = object.chain ?? "";
    message.eventType = object.eventType ?? "";
    message.operationId = object.operationId ?? "";
    message.txHash = object.txHash ?? "";
    message.amount = object.amount ?? "";
    message.asset = object.asset ?? "";
    message.proof = object.proof ?? new Uint8Array(0);
    message.pqcSignature = object.pqcSignature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgBridgeAttestationResponse(): MsgBridgeAttestationResponse {
  return {};
}

export const MsgBridgeAttestationResponse: MessageFns<MsgBridgeAttestationResponse> = {
  encode(_: MsgBridgeAttestationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgBridgeAttestationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgBridgeAttestationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgBridgeAttestationResponse {
    return {};
  },

  toJSON(_: MsgBridgeAttestationResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgBridgeAttestationResponse>): MsgBridgeAttestationResponse {
    return MsgBridgeAttestationResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgBridgeAttestationResponse>): MsgBridgeAttestationResponse {
    const message = createBaseMsgBridgeAttestationResponse();
    return message;
  },
};

function createBaseMsgUpdateEthLightClient(): MsgUpdateEthLightClient {
  return { relayer: "", update: new Uint8Array(0) };
}

export const MsgUpdateEthLightClient: MessageFns<MsgUpdateEthLightClient> = {
  encode(message: MsgUpdateEthLightClient, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.relayer !== "") {
      writer.uint32(10).string(message.relayer);
    }
    if (message.update.length !== 0) {
      writer.uint32(18).bytes(message.update);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateEthLightClient {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateEthLightClient();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.relayer = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateEthLightClient {
    return {
      relayer: isSet(object.relayer) ? globalThis.String(object.relayer) : "",
      update: isSet(object.update) ? bytesFromBase64(object.update) : new Uint8Array(0),
    };
  },

  toJSON(message: MsgUpdateEthLightClient): unknown {
    const obj: any = {};
    if (message.relayer !== "") {
      obj.relayer = message.relayer;
    }
    if (message.update.length !== 0) {
      obj.update = base64FromBytes(message.update);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateEthLightClient>): MsgUpdateEthLightClient {
    return MsgUpdateEthLightClient.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgUpdateEthLightClient>): MsgUpdateEthLightClient {
    const message = createBaseMsgUpdateEthLightClient();
    message.relayer = object.relayer ?? "";
    message.update = object.update ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgUpdateEthLightClientResponse(): MsgUpdateEthLightClientResponse {
  return {};
}

export const MsgUpdateEthLightClientResponse: MessageFns<MsgUpdateEthLightClientResponse> = {
  encode(_: MsgUpdateEthLightClientResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateEthLightClientResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateEthLightClientResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateEthLightClientResponse {
    return {};
  },

  toJSON(_: MsgUpdateEthLightClientResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateEthLightClientResponse>): MsgUpdateEthLightClientResponse {
    return MsgUpdateEthLightClientResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgUpdateEthLightClientResponse>): MsgUpdateEthLightClientResponse {
    const message = createBaseMsgUpdateEthLightClientResponse();
    return message;
  },
};

/** Msg defines the bridge module's transaction service. */
export type MsgDefinition = typeof MsgDefinition;
export const MsgDefinition = {
  name: "Msg",
  fullName: "qorechain.bridge.v1.Msg",
  methods: {
    /** BridgeDeposit credits assets bridged in from a source chain. */
    bridgeDeposit: {
      name: "BridgeDeposit",
      requestType: MsgBridgeDeposit as typeof MsgBridgeDeposit,
      requestStream: false,
      responseType: MsgBridgeDepositResponse as typeof MsgBridgeDepositResponse,
      responseStream: false,
      options: {},
    },
    /** BridgeWithdraw initiates a withdrawal to a destination chain. */
    bridgeWithdraw: {
      name: "BridgeWithdraw",
      requestType: MsgBridgeWithdraw as typeof MsgBridgeWithdraw,
      requestStream: false,
      responseType: MsgBridgeWithdrawResponse as typeof MsgBridgeWithdrawResponse,
      responseStream: false,
      options: {},
    },
    /** RegisterBridgeValidator registers a validator for bridge attestation duty. */
    registerBridgeValidator: {
      name: "RegisterBridgeValidator",
      requestType: MsgRegisterBridgeValidator as typeof MsgRegisterBridgeValidator,
      requestStream: false,
      responseType: MsgRegisterBridgeValidatorResponse as typeof MsgRegisterBridgeValidatorResponse,
      responseStream: false,
      options: {},
    },
    /** BridgeAttestation submits a validator attestation for a bridge operation. */
    bridgeAttestation: {
      name: "BridgeAttestation",
      requestType: MsgBridgeAttestation as typeof MsgBridgeAttestation,
      requestStream: false,
      responseType: MsgBridgeAttestationResponse as typeof MsgBridgeAttestationResponse,
      responseStream: false,
      options: {},
    },
    /**
     * UpdateEthLightClient advances the on-chain Ethereum light client with a
     * verified Altair LightClientUpdate (carried as an opaque encoded bundle).
     */
    updateEthLightClient: {
      name: "UpdateEthLightClient",
      requestType: MsgUpdateEthLightClient as typeof MsgUpdateEthLightClient,
      requestStream: false,
      responseType: MsgUpdateEthLightClientResponse as typeof MsgUpdateEthLightClientResponse,
      responseStream: false,
      options: {},
    },
    /**
     * UpdateChainConfig registers/updates a chain's bridge config — real contract
     * address, confirmations, architecture, status, and which trustless verifier
     * is active. Authorized by the bridge_admin key OR a qcb_bridge license; lets
     * operators activate a chain's bridge POST-DEPLOY without governance or an
     * upgrade.
     */
    updateChainConfig: {
      name: "UpdateChainConfig",
      requestType: MsgUpdateChainConfig as typeof MsgUpdateChainConfig,
      requestStream: false,
      responseType: MsgUpdateChainConfigResponse as typeof MsgUpdateChainConfigResponse,
      responseStream: false,
      options: {},
    },
    /**
     * SetVerifierBootstrap installs the per-architecture trust root a chain's
     * trustless verifier needs (Wormhole guardian set / ed25519 or BLS validator
     * quorum / Bitcoin checkpoint / Starknet state root). Same authorization as
     * UpdateChainConfig. (Ethereum uses UpdateEthLightClient.)
     */
    setVerifierBootstrap: {
      name: "SetVerifierBootstrap",
      requestType: MsgSetVerifierBootstrap as typeof MsgSetVerifierBootstrap,
      requestStream: false,
      responseType: MsgSetVerifierBootstrapResponse as typeof MsgSetVerifierBootstrapResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
