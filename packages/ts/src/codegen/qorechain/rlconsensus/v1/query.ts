// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/rlconsensus/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.rlconsensus.v1";

export interface QueryObservationRequest {
}

export interface QueryObservationResponse {
  height: string;
  values: string[];
}

export interface QueryRewardRequest {
}

export interface QueryRewardResponse {
  height: string;
  totalReward: string;
  throughputDelta: string;
  finalityDelta: string;
  decentralizationDelta: string;
  mevEstimate: string;
  failedTxRatio: string;
}

export interface QueryPolicyRequest {
}

export interface QueryPolicyResponse {
  epoch: string;
  updatedAt: string;
  weightCount: string;
}

export interface QueryAgentStatusRequest {
}

export interface QueryAgentStatusResponse {
  mode: string;
  currentEpoch: string;
  totalSteps: string;
  lastObservationAt: string;
  lastActionAt: string;
  circuitBreakerActive: boolean;
  blocksSinceRevert: string;
}

export interface QueryParamsRequest {
}

export interface QueryParamsResponse {
  enabled: boolean;
  observationInterval: string;
  agentMode: string;
  maxChangeConservative: string;
  maxChangeAutonomous: string;
  circuitBreakerWindow: string;
  circuitBreakerThreshold: string;
  /**
   * Reward weights (flattened to avoid clashing with the hand-written
   * types.RewardWeights struct used in genesis state).
   */
  rewardWeightThroughput: string;
  rewardWeightFinality: string;
  rewardWeightDecentralization: string;
  rewardWeightMev: string;
  rewardWeightFailedTxs: string;
  defaultBlockTimeMs: string;
  defaultBaseGasPrice: string;
  defaultValidatorSetSize: string;
}

function createBaseQueryObservationRequest(): QueryObservationRequest {
  return {};
}

export const QueryObservationRequest: MessageFns<QueryObservationRequest> = {
  encode(_: QueryObservationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryObservationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryObservationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryObservationRequest {
    return {};
  },

  toJSON(_: QueryObservationRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryObservationRequest>): QueryObservationRequest {
    return QueryObservationRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryObservationRequest>): QueryObservationRequest {
    const message = createBaseQueryObservationRequest();
    return message;
  },
};

function createBaseQueryObservationResponse(): QueryObservationResponse {
  return { height: "0", values: [] };
}

export const QueryObservationResponse: MessageFns<QueryObservationResponse> = {
  encode(message: QueryObservationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.height !== "0") {
      writer.uint32(8).int64(message.height);
    }
    for (const v of message.values) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryObservationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryObservationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.height = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.values.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryObservationResponse {
    return {
      height: isSet(object.height) ? globalThis.String(object.height) : "0",
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: QueryObservationResponse): unknown {
    const obj: any = {};
    if (message.height !== "0") {
      obj.height = message.height;
    }
    if (message.values?.length) {
      obj.values = message.values;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryObservationResponse>): QueryObservationResponse {
    return QueryObservationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryObservationResponse>): QueryObservationResponse {
    const message = createBaseQueryObservationResponse();
    message.height = object.height ?? "0";
    message.values = object.values?.map((e) => e) || [];
    return message;
  },
};

function createBaseQueryRewardRequest(): QueryRewardRequest {
  return {};
}

export const QueryRewardRequest: MessageFns<QueryRewardRequest> = {
  encode(_: QueryRewardRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRewardRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRewardRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryRewardRequest {
    return {};
  },

  toJSON(_: QueryRewardRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryRewardRequest>): QueryRewardRequest {
    return QueryRewardRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryRewardRequest>): QueryRewardRequest {
    const message = createBaseQueryRewardRequest();
    return message;
  },
};

function createBaseQueryRewardResponse(): QueryRewardResponse {
  return {
    height: "0",
    totalReward: "",
    throughputDelta: "",
    finalityDelta: "",
    decentralizationDelta: "",
    mevEstimate: "",
    failedTxRatio: "",
  };
}

export const QueryRewardResponse: MessageFns<QueryRewardResponse> = {
  encode(message: QueryRewardResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.height !== "0") {
      writer.uint32(8).int64(message.height);
    }
    if (message.totalReward !== "") {
      writer.uint32(18).string(message.totalReward);
    }
    if (message.throughputDelta !== "") {
      writer.uint32(26).string(message.throughputDelta);
    }
    if (message.finalityDelta !== "") {
      writer.uint32(34).string(message.finalityDelta);
    }
    if (message.decentralizationDelta !== "") {
      writer.uint32(42).string(message.decentralizationDelta);
    }
    if (message.mevEstimate !== "") {
      writer.uint32(50).string(message.mevEstimate);
    }
    if (message.failedTxRatio !== "") {
      writer.uint32(58).string(message.failedTxRatio);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRewardResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRewardResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.height = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.totalReward = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.throughputDelta = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.finalityDelta = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.decentralizationDelta = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.mevEstimate = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.failedTxRatio = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRewardResponse {
    return {
      height: isSet(object.height) ? globalThis.String(object.height) : "0",
      totalReward: isSet(object.totalReward)
        ? globalThis.String(object.totalReward)
        : isSet(object.total_reward)
        ? globalThis.String(object.total_reward)
        : "",
      throughputDelta: isSet(object.throughputDelta)
        ? globalThis.String(object.throughputDelta)
        : isSet(object.throughput_delta)
        ? globalThis.String(object.throughput_delta)
        : "",
      finalityDelta: isSet(object.finalityDelta)
        ? globalThis.String(object.finalityDelta)
        : isSet(object.finality_delta)
        ? globalThis.String(object.finality_delta)
        : "",
      decentralizationDelta: isSet(object.decentralizationDelta)
        ? globalThis.String(object.decentralizationDelta)
        : isSet(object.decentralization_delta)
        ? globalThis.String(object.decentralization_delta)
        : "",
      mevEstimate: isSet(object.mevEstimate)
        ? globalThis.String(object.mevEstimate)
        : isSet(object.mev_estimate)
        ? globalThis.String(object.mev_estimate)
        : "",
      failedTxRatio: isSet(object.failedTxRatio)
        ? globalThis.String(object.failedTxRatio)
        : isSet(object.failed_tx_ratio)
        ? globalThis.String(object.failed_tx_ratio)
        : "",
    };
  },

  toJSON(message: QueryRewardResponse): unknown {
    const obj: any = {};
    if (message.height !== "0") {
      obj.height = message.height;
    }
    if (message.totalReward !== "") {
      obj.totalReward = message.totalReward;
    }
    if (message.throughputDelta !== "") {
      obj.throughputDelta = message.throughputDelta;
    }
    if (message.finalityDelta !== "") {
      obj.finalityDelta = message.finalityDelta;
    }
    if (message.decentralizationDelta !== "") {
      obj.decentralizationDelta = message.decentralizationDelta;
    }
    if (message.mevEstimate !== "") {
      obj.mevEstimate = message.mevEstimate;
    }
    if (message.failedTxRatio !== "") {
      obj.failedTxRatio = message.failedTxRatio;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRewardResponse>): QueryRewardResponse {
    return QueryRewardResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRewardResponse>): QueryRewardResponse {
    const message = createBaseQueryRewardResponse();
    message.height = object.height ?? "0";
    message.totalReward = object.totalReward ?? "";
    message.throughputDelta = object.throughputDelta ?? "";
    message.finalityDelta = object.finalityDelta ?? "";
    message.decentralizationDelta = object.decentralizationDelta ?? "";
    message.mevEstimate = object.mevEstimate ?? "";
    message.failedTxRatio = object.failedTxRatio ?? "";
    return message;
  },
};

function createBaseQueryPolicyRequest(): QueryPolicyRequest {
  return {};
}

export const QueryPolicyRequest: MessageFns<QueryPolicyRequest> = {
  encode(_: QueryPolicyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryPolicyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryPolicyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryPolicyRequest {
    return {};
  },

  toJSON(_: QueryPolicyRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryPolicyRequest>): QueryPolicyRequest {
    return QueryPolicyRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryPolicyRequest>): QueryPolicyRequest {
    const message = createBaseQueryPolicyRequest();
    return message;
  },
};

function createBaseQueryPolicyResponse(): QueryPolicyResponse {
  return { epoch: "0", updatedAt: "0", weightCount: "0" };
}

export const QueryPolicyResponse: MessageFns<QueryPolicyResponse> = {
  encode(message: QueryPolicyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.epoch !== "0") {
      writer.uint32(8).uint64(message.epoch);
    }
    if (message.updatedAt !== "0") {
      writer.uint32(16).int64(message.updatedAt);
    }
    if (message.weightCount !== "0") {
      writer.uint32(24).uint64(message.weightCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryPolicyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryPolicyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.epoch = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.updatedAt = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.weightCount = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryPolicyResponse {
    return {
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "0",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "0",
      weightCount: isSet(object.weightCount)
        ? globalThis.String(object.weightCount)
        : isSet(object.weight_count)
        ? globalThis.String(object.weight_count)
        : "0",
    };
  },

  toJSON(message: QueryPolicyResponse): unknown {
    const obj: any = {};
    if (message.epoch !== "0") {
      obj.epoch = message.epoch;
    }
    if (message.updatedAt !== "0") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.weightCount !== "0") {
      obj.weightCount = message.weightCount;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryPolicyResponse>): QueryPolicyResponse {
    return QueryPolicyResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryPolicyResponse>): QueryPolicyResponse {
    const message = createBaseQueryPolicyResponse();
    message.epoch = object.epoch ?? "0";
    message.updatedAt = object.updatedAt ?? "0";
    message.weightCount = object.weightCount ?? "0";
    return message;
  },
};

function createBaseQueryAgentStatusRequest(): QueryAgentStatusRequest {
  return {};
}

export const QueryAgentStatusRequest: MessageFns<QueryAgentStatusRequest> = {
  encode(_: QueryAgentStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAgentStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAgentStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryAgentStatusRequest {
    return {};
  },

  toJSON(_: QueryAgentStatusRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryAgentStatusRequest>): QueryAgentStatusRequest {
    return QueryAgentStatusRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryAgentStatusRequest>): QueryAgentStatusRequest {
    const message = createBaseQueryAgentStatusRequest();
    return message;
  },
};

function createBaseQueryAgentStatusResponse(): QueryAgentStatusResponse {
  return {
    mode: "",
    currentEpoch: "0",
    totalSteps: "0",
    lastObservationAt: "0",
    lastActionAt: "0",
    circuitBreakerActive: false,
    blocksSinceRevert: "0",
  };
}

export const QueryAgentStatusResponse: MessageFns<QueryAgentStatusResponse> = {
  encode(message: QueryAgentStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mode !== "") {
      writer.uint32(10).string(message.mode);
    }
    if (message.currentEpoch !== "0") {
      writer.uint32(16).uint64(message.currentEpoch);
    }
    if (message.totalSteps !== "0") {
      writer.uint32(24).uint64(message.totalSteps);
    }
    if (message.lastObservationAt !== "0") {
      writer.uint32(32).int64(message.lastObservationAt);
    }
    if (message.lastActionAt !== "0") {
      writer.uint32(40).int64(message.lastActionAt);
    }
    if (message.circuitBreakerActive !== false) {
      writer.uint32(48).bool(message.circuitBreakerActive);
    }
    if (message.blocksSinceRevert !== "0") {
      writer.uint32(56).int64(message.blocksSinceRevert);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAgentStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAgentStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mode = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.currentEpoch = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalSteps = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lastObservationAt = reader.int64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.lastActionAt = reader.int64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.circuitBreakerActive = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.blocksSinceRevert = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAgentStatusResponse {
    return {
      mode: isSet(object.mode) ? globalThis.String(object.mode) : "",
      currentEpoch: isSet(object.currentEpoch)
        ? globalThis.String(object.currentEpoch)
        : isSet(object.current_epoch)
        ? globalThis.String(object.current_epoch)
        : "0",
      totalSteps: isSet(object.totalSteps)
        ? globalThis.String(object.totalSteps)
        : isSet(object.total_steps)
        ? globalThis.String(object.total_steps)
        : "0",
      lastObservationAt: isSet(object.lastObservationAt)
        ? globalThis.String(object.lastObservationAt)
        : isSet(object.last_observation_at)
        ? globalThis.String(object.last_observation_at)
        : "0",
      lastActionAt: isSet(object.lastActionAt)
        ? globalThis.String(object.lastActionAt)
        : isSet(object.last_action_at)
        ? globalThis.String(object.last_action_at)
        : "0",
      circuitBreakerActive: isSet(object.circuitBreakerActive)
        ? globalThis.Boolean(object.circuitBreakerActive)
        : isSet(object.circuit_breaker_active)
        ? globalThis.Boolean(object.circuit_breaker_active)
        : false,
      blocksSinceRevert: isSet(object.blocksSinceRevert)
        ? globalThis.String(object.blocksSinceRevert)
        : isSet(object.blocks_since_revert)
        ? globalThis.String(object.blocks_since_revert)
        : "0",
    };
  },

  toJSON(message: QueryAgentStatusResponse): unknown {
    const obj: any = {};
    if (message.mode !== "") {
      obj.mode = message.mode;
    }
    if (message.currentEpoch !== "0") {
      obj.currentEpoch = message.currentEpoch;
    }
    if (message.totalSteps !== "0") {
      obj.totalSteps = message.totalSteps;
    }
    if (message.lastObservationAt !== "0") {
      obj.lastObservationAt = message.lastObservationAt;
    }
    if (message.lastActionAt !== "0") {
      obj.lastActionAt = message.lastActionAt;
    }
    if (message.circuitBreakerActive !== false) {
      obj.circuitBreakerActive = message.circuitBreakerActive;
    }
    if (message.blocksSinceRevert !== "0") {
      obj.blocksSinceRevert = message.blocksSinceRevert;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAgentStatusResponse>): QueryAgentStatusResponse {
    return QueryAgentStatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAgentStatusResponse>): QueryAgentStatusResponse {
    const message = createBaseQueryAgentStatusResponse();
    message.mode = object.mode ?? "";
    message.currentEpoch = object.currentEpoch ?? "0";
    message.totalSteps = object.totalSteps ?? "0";
    message.lastObservationAt = object.lastObservationAt ?? "0";
    message.lastActionAt = object.lastActionAt ?? "0";
    message.circuitBreakerActive = object.circuitBreakerActive ?? false;
    message.blocksSinceRevert = object.blocksSinceRevert ?? "0";
    return message;
  },
};

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return {
    enabled: false,
    observationInterval: "0",
    agentMode: "",
    maxChangeConservative: "",
    maxChangeAutonomous: "",
    circuitBreakerWindow: "0",
    circuitBreakerThreshold: "",
    rewardWeightThroughput: "",
    rewardWeightFinality: "",
    rewardWeightDecentralization: "",
    rewardWeightMev: "",
    rewardWeightFailedTxs: "",
    defaultBlockTimeMs: "0",
    defaultBaseGasPrice: "",
    defaultValidatorSetSize: "0",
  };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.observationInterval !== "0") {
      writer.uint32(16).uint64(message.observationInterval);
    }
    if (message.agentMode !== "") {
      writer.uint32(26).string(message.agentMode);
    }
    if (message.maxChangeConservative !== "") {
      writer.uint32(34).string(message.maxChangeConservative);
    }
    if (message.maxChangeAutonomous !== "") {
      writer.uint32(42).string(message.maxChangeAutonomous);
    }
    if (message.circuitBreakerWindow !== "0") {
      writer.uint32(48).uint64(message.circuitBreakerWindow);
    }
    if (message.circuitBreakerThreshold !== "") {
      writer.uint32(58).string(message.circuitBreakerThreshold);
    }
    if (message.rewardWeightThroughput !== "") {
      writer.uint32(66).string(message.rewardWeightThroughput);
    }
    if (message.rewardWeightFinality !== "") {
      writer.uint32(74).string(message.rewardWeightFinality);
    }
    if (message.rewardWeightDecentralization !== "") {
      writer.uint32(82).string(message.rewardWeightDecentralization);
    }
    if (message.rewardWeightMev !== "") {
      writer.uint32(90).string(message.rewardWeightMev);
    }
    if (message.rewardWeightFailedTxs !== "") {
      writer.uint32(98).string(message.rewardWeightFailedTxs);
    }
    if (message.defaultBlockTimeMs !== "0") {
      writer.uint32(104).int64(message.defaultBlockTimeMs);
    }
    if (message.defaultBaseGasPrice !== "") {
      writer.uint32(114).string(message.defaultBaseGasPrice);
    }
    if (message.defaultValidatorSetSize !== "0") {
      writer.uint32(120).uint64(message.defaultValidatorSetSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.observationInterval = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.agentMode = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.maxChangeConservative = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.maxChangeAutonomous = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.circuitBreakerWindow = reader.uint64().toString();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.circuitBreakerThreshold = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.rewardWeightThroughput = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.rewardWeightFinality = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.rewardWeightDecentralization = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.rewardWeightMev = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.rewardWeightFailedTxs = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.defaultBlockTimeMs = reader.int64().toString();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.defaultBaseGasPrice = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.defaultValidatorSetSize = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      observationInterval: isSet(object.observationInterval)
        ? globalThis.String(object.observationInterval)
        : isSet(object.observation_interval)
        ? globalThis.String(object.observation_interval)
        : "0",
      agentMode: isSet(object.agentMode)
        ? globalThis.String(object.agentMode)
        : isSet(object.agent_mode)
        ? globalThis.String(object.agent_mode)
        : "",
      maxChangeConservative: isSet(object.maxChangeConservative)
        ? globalThis.String(object.maxChangeConservative)
        : isSet(object.max_change_conservative)
        ? globalThis.String(object.max_change_conservative)
        : "",
      maxChangeAutonomous: isSet(object.maxChangeAutonomous)
        ? globalThis.String(object.maxChangeAutonomous)
        : isSet(object.max_change_autonomous)
        ? globalThis.String(object.max_change_autonomous)
        : "",
      circuitBreakerWindow: isSet(object.circuitBreakerWindow)
        ? globalThis.String(object.circuitBreakerWindow)
        : isSet(object.circuit_breaker_window)
        ? globalThis.String(object.circuit_breaker_window)
        : "0",
      circuitBreakerThreshold: isSet(object.circuitBreakerThreshold)
        ? globalThis.String(object.circuitBreakerThreshold)
        : isSet(object.circuit_breaker_threshold)
        ? globalThis.String(object.circuit_breaker_threshold)
        : "",
      rewardWeightThroughput: isSet(object.rewardWeightThroughput)
        ? globalThis.String(object.rewardWeightThroughput)
        : isSet(object.reward_weight_throughput)
        ? globalThis.String(object.reward_weight_throughput)
        : "",
      rewardWeightFinality: isSet(object.rewardWeightFinality)
        ? globalThis.String(object.rewardWeightFinality)
        : isSet(object.reward_weight_finality)
        ? globalThis.String(object.reward_weight_finality)
        : "",
      rewardWeightDecentralization: isSet(object.rewardWeightDecentralization)
        ? globalThis.String(object.rewardWeightDecentralization)
        : isSet(object.reward_weight_decentralization)
        ? globalThis.String(object.reward_weight_decentralization)
        : "",
      rewardWeightMev: isSet(object.rewardWeightMev)
        ? globalThis.String(object.rewardWeightMev)
        : isSet(object.reward_weight_mev)
        ? globalThis.String(object.reward_weight_mev)
        : "",
      rewardWeightFailedTxs: isSet(object.rewardWeightFailedTxs)
        ? globalThis.String(object.rewardWeightFailedTxs)
        : isSet(object.reward_weight_failed_txs)
        ? globalThis.String(object.reward_weight_failed_txs)
        : "",
      defaultBlockTimeMs: isSet(object.defaultBlockTimeMs)
        ? globalThis.String(object.defaultBlockTimeMs)
        : isSet(object.default_block_time_ms)
        ? globalThis.String(object.default_block_time_ms)
        : "0",
      defaultBaseGasPrice: isSet(object.defaultBaseGasPrice)
        ? globalThis.String(object.defaultBaseGasPrice)
        : isSet(object.default_base_gas_price)
        ? globalThis.String(object.default_base_gas_price)
        : "",
      defaultValidatorSetSize: isSet(object.defaultValidatorSetSize)
        ? globalThis.String(object.defaultValidatorSetSize)
        : isSet(object.default_validator_set_size)
        ? globalThis.String(object.default_validator_set_size)
        : "0",
    };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.observationInterval !== "0") {
      obj.observationInterval = message.observationInterval;
    }
    if (message.agentMode !== "") {
      obj.agentMode = message.agentMode;
    }
    if (message.maxChangeConservative !== "") {
      obj.maxChangeConservative = message.maxChangeConservative;
    }
    if (message.maxChangeAutonomous !== "") {
      obj.maxChangeAutonomous = message.maxChangeAutonomous;
    }
    if (message.circuitBreakerWindow !== "0") {
      obj.circuitBreakerWindow = message.circuitBreakerWindow;
    }
    if (message.circuitBreakerThreshold !== "") {
      obj.circuitBreakerThreshold = message.circuitBreakerThreshold;
    }
    if (message.rewardWeightThroughput !== "") {
      obj.rewardWeightThroughput = message.rewardWeightThroughput;
    }
    if (message.rewardWeightFinality !== "") {
      obj.rewardWeightFinality = message.rewardWeightFinality;
    }
    if (message.rewardWeightDecentralization !== "") {
      obj.rewardWeightDecentralization = message.rewardWeightDecentralization;
    }
    if (message.rewardWeightMev !== "") {
      obj.rewardWeightMev = message.rewardWeightMev;
    }
    if (message.rewardWeightFailedTxs !== "") {
      obj.rewardWeightFailedTxs = message.rewardWeightFailedTxs;
    }
    if (message.defaultBlockTimeMs !== "0") {
      obj.defaultBlockTimeMs = message.defaultBlockTimeMs;
    }
    if (message.defaultBaseGasPrice !== "") {
      obj.defaultBaseGasPrice = message.defaultBaseGasPrice;
    }
    if (message.defaultValidatorSetSize !== "0") {
      obj.defaultValidatorSetSize = message.defaultValidatorSetSize;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.enabled = object.enabled ?? false;
    message.observationInterval = object.observationInterval ?? "0";
    message.agentMode = object.agentMode ?? "";
    message.maxChangeConservative = object.maxChangeConservative ?? "";
    message.maxChangeAutonomous = object.maxChangeAutonomous ?? "";
    message.circuitBreakerWindow = object.circuitBreakerWindow ?? "0";
    message.circuitBreakerThreshold = object.circuitBreakerThreshold ?? "";
    message.rewardWeightThroughput = object.rewardWeightThroughput ?? "";
    message.rewardWeightFinality = object.rewardWeightFinality ?? "";
    message.rewardWeightDecentralization = object.rewardWeightDecentralization ?? "";
    message.rewardWeightMev = object.rewardWeightMev ?? "";
    message.rewardWeightFailedTxs = object.rewardWeightFailedTxs ?? "";
    message.defaultBlockTimeMs = object.defaultBlockTimeMs ?? "0";
    message.defaultBaseGasPrice = object.defaultBaseGasPrice ?? "";
    message.defaultValidatorSetSize = object.defaultValidatorSetSize ?? "0";
    return message;
  },
};

/** Query defines the rlconsensus module's gRPC query service. */
export type QueryDefinition = typeof QueryDefinition;
export const QueryDefinition = {
  name: "Query",
  fullName: "qorechain.rlconsensus.v1.Query",
  methods: {
    /** AgentStatus returns the live RL agent status (epoch, steps, mode). */
    agentStatus: {
      name: "AgentStatus",
      requestType: QueryAgentStatusRequest as typeof QueryAgentStatusRequest,
      requestStream: false,
      responseType: QueryAgentStatusResponse as typeof QueryAgentStatusResponse,
      responseStream: false,
      options: {},
    },
    /** Params returns the rlconsensus module parameters. */
    params: {
      name: "Params",
      requestType: QueryParamsRequest as typeof QueryParamsRequest,
      requestStream: false,
      responseType: QueryParamsResponse as typeof QueryParamsResponse,
      responseStream: false,
      options: {},
    },
    /** Observation returns the latest 25-dim observation vector captured by the agent. */
    observation: {
      name: "Observation",
      requestType: QueryObservationRequest as typeof QueryObservationRequest,
      requestStream: false,
      responseType: QueryObservationResponse as typeof QueryObservationResponse,
      responseStream: false,
      options: {},
    },
    /** Reward returns the latest reward signal (total + per-component deltas). */
    reward: {
      name: "Reward",
      requestType: QueryRewardRequest as typeof QueryRewardRequest,
      requestStream: false,
      responseType: QueryRewardResponse as typeof QueryRewardResponse,
      responseStream: false,
      options: {},
    },
    /** Policy returns the current policy network summary (epoch, params, update height). */
    policy: {
      name: "Policy",
      requestType: QueryPolicyRequest as typeof QueryPolicyRequest,
      requestStream: false,
      responseType: QueryPolicyResponse as typeof QueryPolicyResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
