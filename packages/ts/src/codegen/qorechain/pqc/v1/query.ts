// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/pqc/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.pqc.v1";

export interface QueryEVMWindowRequest {
  address: string;
}

export interface QueryEVMWindowResponse {
  /**
   * found reports whether a window is stored. A stored window may still be
   * exhausted or expired; check live.
   */
  found: boolean;
  /**
   * live reports whether the window admits at least one more transaction of
   * zero value at the current height.
   */
  live: boolean;
  openedHeight: string;
  expiryHeight: string;
  maxTxs: string;
  usedTxs: string;
  maxValue: string;
  usedValue: string;
  /**
   * remaining_blocks, remaining_txs and remaining_value say which bound is
   * closest to running out, so a refused caller can tell why.
   */
  remainingBlocks: string;
  remainingTxs: string;
  remainingValue: string;
}

export interface QueryAccountRequest {
  /** address is the bech32 account address whose PQC key is requested. */
  address: string;
}

export interface QueryAccountResponse {
  /** found reports whether the account has a registered PQC key. */
  found: boolean;
  account?: PQCAccountView | undefined;
}

/** PQCAccountView is the query-facing view of a registered PQC account. */
export interface PQCAccountView {
  address: string;
  publicKey: Uint8Array;
  algorithmId: number;
  algorithmName: string;
  ecdsaPubkey: Uint8Array;
  keyType: string;
  createdAtHeight: string;
  migrationPublicKey: Uint8Array;
  migrationAlgorithmId: number;
}

function createBaseQueryEVMWindowRequest(): QueryEVMWindowRequest {
  return { address: "" };
}

export const QueryEVMWindowRequest: MessageFns<QueryEVMWindowRequest> = {
  encode(message: QueryEVMWindowRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryEVMWindowRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryEVMWindowRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryEVMWindowRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryEVMWindowRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryEVMWindowRequest>): QueryEVMWindowRequest {
    return QueryEVMWindowRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryEVMWindowRequest>): QueryEVMWindowRequest {
    const message = createBaseQueryEVMWindowRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryEVMWindowResponse(): QueryEVMWindowResponse {
  return {
    found: false,
    live: false,
    openedHeight: "0",
    expiryHeight: "0",
    maxTxs: "0",
    usedTxs: "0",
    maxValue: "",
    usedValue: "",
    remainingBlocks: "0",
    remainingTxs: "0",
    remainingValue: "",
  };
}

export const QueryEVMWindowResponse: MessageFns<QueryEVMWindowResponse> = {
  encode(message: QueryEVMWindowResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.found !== false) {
      writer.uint32(8).bool(message.found);
    }
    if (message.live !== false) {
      writer.uint32(16).bool(message.live);
    }
    if (message.openedHeight !== "0") {
      writer.uint32(24).uint64(message.openedHeight);
    }
    if (message.expiryHeight !== "0") {
      writer.uint32(32).uint64(message.expiryHeight);
    }
    if (message.maxTxs !== "0") {
      writer.uint32(40).uint64(message.maxTxs);
    }
    if (message.usedTxs !== "0") {
      writer.uint32(48).uint64(message.usedTxs);
    }
    if (message.maxValue !== "") {
      writer.uint32(58).string(message.maxValue);
    }
    if (message.usedValue !== "") {
      writer.uint32(66).string(message.usedValue);
    }
    if (message.remainingBlocks !== "0") {
      writer.uint32(72).uint64(message.remainingBlocks);
    }
    if (message.remainingTxs !== "0") {
      writer.uint32(80).uint64(message.remainingTxs);
    }
    if (message.remainingValue !== "") {
      writer.uint32(90).string(message.remainingValue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryEVMWindowResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryEVMWindowResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.live = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.openedHeight = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.expiryHeight = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxTxs = reader.uint64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.usedTxs = reader.uint64().toString();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.maxValue = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.usedValue = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.remainingBlocks = reader.uint64().toString();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.remainingTxs = reader.uint64().toString();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.remainingValue = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryEVMWindowResponse {
    return {
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
      live: isSet(object.live) ? globalThis.Boolean(object.live) : false,
      openedHeight: isSet(object.openedHeight)
        ? globalThis.String(object.openedHeight)
        : isSet(object.opened_height)
        ? globalThis.String(object.opened_height)
        : "0",
      expiryHeight: isSet(object.expiryHeight)
        ? globalThis.String(object.expiryHeight)
        : isSet(object.expiry_height)
        ? globalThis.String(object.expiry_height)
        : "0",
      maxTxs: isSet(object.maxTxs)
        ? globalThis.String(object.maxTxs)
        : isSet(object.max_txs)
        ? globalThis.String(object.max_txs)
        : "0",
      usedTxs: isSet(object.usedTxs)
        ? globalThis.String(object.usedTxs)
        : isSet(object.used_txs)
        ? globalThis.String(object.used_txs)
        : "0",
      maxValue: isSet(object.maxValue)
        ? globalThis.String(object.maxValue)
        : isSet(object.max_value)
        ? globalThis.String(object.max_value)
        : "",
      usedValue: isSet(object.usedValue)
        ? globalThis.String(object.usedValue)
        : isSet(object.used_value)
        ? globalThis.String(object.used_value)
        : "",
      remainingBlocks: isSet(object.remainingBlocks)
        ? globalThis.String(object.remainingBlocks)
        : isSet(object.remaining_blocks)
        ? globalThis.String(object.remaining_blocks)
        : "0",
      remainingTxs: isSet(object.remainingTxs)
        ? globalThis.String(object.remainingTxs)
        : isSet(object.remaining_txs)
        ? globalThis.String(object.remaining_txs)
        : "0",
      remainingValue: isSet(object.remainingValue)
        ? globalThis.String(object.remainingValue)
        : isSet(object.remaining_value)
        ? globalThis.String(object.remaining_value)
        : "",
    };
  },

  toJSON(message: QueryEVMWindowResponse): unknown {
    const obj: any = {};
    if (message.found !== false) {
      obj.found = message.found;
    }
    if (message.live !== false) {
      obj.live = message.live;
    }
    if (message.openedHeight !== "0") {
      obj.openedHeight = message.openedHeight;
    }
    if (message.expiryHeight !== "0") {
      obj.expiryHeight = message.expiryHeight;
    }
    if (message.maxTxs !== "0") {
      obj.maxTxs = message.maxTxs;
    }
    if (message.usedTxs !== "0") {
      obj.usedTxs = message.usedTxs;
    }
    if (message.maxValue !== "") {
      obj.maxValue = message.maxValue;
    }
    if (message.usedValue !== "") {
      obj.usedValue = message.usedValue;
    }
    if (message.remainingBlocks !== "0") {
      obj.remainingBlocks = message.remainingBlocks;
    }
    if (message.remainingTxs !== "0") {
      obj.remainingTxs = message.remainingTxs;
    }
    if (message.remainingValue !== "") {
      obj.remainingValue = message.remainingValue;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryEVMWindowResponse>): QueryEVMWindowResponse {
    return QueryEVMWindowResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryEVMWindowResponse>): QueryEVMWindowResponse {
    const message = createBaseQueryEVMWindowResponse();
    message.found = object.found ?? false;
    message.live = object.live ?? false;
    message.openedHeight = object.openedHeight ?? "0";
    message.expiryHeight = object.expiryHeight ?? "0";
    message.maxTxs = object.maxTxs ?? "0";
    message.usedTxs = object.usedTxs ?? "0";
    message.maxValue = object.maxValue ?? "";
    message.usedValue = object.usedValue ?? "";
    message.remainingBlocks = object.remainingBlocks ?? "0";
    message.remainingTxs = object.remainingTxs ?? "0";
    message.remainingValue = object.remainingValue ?? "";
    return message;
  },
};

function createBaseQueryAccountRequest(): QueryAccountRequest {
  return { address: "" };
}

export const QueryAccountRequest: MessageFns<QueryAccountRequest> = {
  encode(message: QueryAccountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAccountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAccountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAccountRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryAccountRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAccountRequest>): QueryAccountRequest {
    return QueryAccountRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAccountRequest>): QueryAccountRequest {
    const message = createBaseQueryAccountRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryAccountResponse(): QueryAccountResponse {
  return { found: false, account: undefined };
}

export const QueryAccountResponse: MessageFns<QueryAccountResponse> = {
  encode(message: QueryAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.found !== false) {
      writer.uint32(8).bool(message.found);
    }
    if (message.account !== undefined) {
      PQCAccountView.encode(message.account, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAccountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.account = PQCAccountView.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAccountResponse {
    return {
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
      account: isSet(object.account) ? PQCAccountView.fromJSON(object.account) : undefined,
    };
  },

  toJSON(message: QueryAccountResponse): unknown {
    const obj: any = {};
    if (message.found !== false) {
      obj.found = message.found;
    }
    if (message.account !== undefined) {
      obj.account = PQCAccountView.toJSON(message.account);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAccountResponse>): QueryAccountResponse {
    return QueryAccountResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAccountResponse>): QueryAccountResponse {
    const message = createBaseQueryAccountResponse();
    message.found = object.found ?? false;
    message.account = (object.account !== undefined && object.account !== null)
      ? PQCAccountView.fromPartial(object.account)
      : undefined;
    return message;
  },
};

function createBasePQCAccountView(): PQCAccountView {
  return {
    address: "",
    publicKey: new Uint8Array(0),
    algorithmId: 0,
    algorithmName: "",
    ecdsaPubkey: new Uint8Array(0),
    keyType: "",
    createdAtHeight: "0",
    migrationPublicKey: new Uint8Array(0),
    migrationAlgorithmId: 0,
  };
}

export const PQCAccountView: MessageFns<PQCAccountView> = {
  encode(message: PQCAccountView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.publicKey.length !== 0) {
      writer.uint32(18).bytes(message.publicKey);
    }
    if (message.algorithmId !== 0) {
      writer.uint32(24).uint32(message.algorithmId);
    }
    if (message.algorithmName !== "") {
      writer.uint32(34).string(message.algorithmName);
    }
    if (message.ecdsaPubkey.length !== 0) {
      writer.uint32(42).bytes(message.ecdsaPubkey);
    }
    if (message.keyType !== "") {
      writer.uint32(50).string(message.keyType);
    }
    if (message.createdAtHeight !== "0") {
      writer.uint32(56).int64(message.createdAtHeight);
    }
    if (message.migrationPublicKey.length !== 0) {
      writer.uint32(66).bytes(message.migrationPublicKey);
    }
    if (message.migrationAlgorithmId !== 0) {
      writer.uint32(72).uint32(message.migrationAlgorithmId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PQCAccountView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePQCAccountView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.publicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.algorithmId = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.algorithmName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.ecdsaPubkey = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.keyType = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.createdAtHeight = reader.int64().toString();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.migrationPublicKey = reader.bytes();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.migrationAlgorithmId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PQCAccountView {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      publicKey: isSet(object.publicKey)
        ? bytesFromBase64(object.publicKey)
        : isSet(object.public_key)
        ? bytesFromBase64(object.public_key)
        : new Uint8Array(0),
      algorithmId: isSet(object.algorithmId)
        ? globalThis.Number(object.algorithmId)
        : isSet(object.algorithm_id)
        ? globalThis.Number(object.algorithm_id)
        : 0,
      algorithmName: isSet(object.algorithmName)
        ? globalThis.String(object.algorithmName)
        : isSet(object.algorithm_name)
        ? globalThis.String(object.algorithm_name)
        : "",
      ecdsaPubkey: isSet(object.ecdsaPubkey)
        ? bytesFromBase64(object.ecdsaPubkey)
        : isSet(object.ecdsa_pubkey)
        ? bytesFromBase64(object.ecdsa_pubkey)
        : new Uint8Array(0),
      keyType: isSet(object.keyType)
        ? globalThis.String(object.keyType)
        : isSet(object.key_type)
        ? globalThis.String(object.key_type)
        : "",
      createdAtHeight: isSet(object.createdAtHeight)
        ? globalThis.String(object.createdAtHeight)
        : isSet(object.created_at_height)
        ? globalThis.String(object.created_at_height)
        : "0",
      migrationPublicKey: isSet(object.migrationPublicKey)
        ? bytesFromBase64(object.migrationPublicKey)
        : isSet(object.migration_public_key)
        ? bytesFromBase64(object.migration_public_key)
        : new Uint8Array(0),
      migrationAlgorithmId: isSet(object.migrationAlgorithmId)
        ? globalThis.Number(object.migrationAlgorithmId)
        : isSet(object.migration_algorithm_id)
        ? globalThis.Number(object.migration_algorithm_id)
        : 0,
    };
  },

  toJSON(message: PQCAccountView): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.publicKey.length !== 0) {
      obj.publicKey = base64FromBytes(message.publicKey);
    }
    if (message.algorithmId !== 0) {
      obj.algorithmId = Math.round(message.algorithmId);
    }
    if (message.algorithmName !== "") {
      obj.algorithmName = message.algorithmName;
    }
    if (message.ecdsaPubkey.length !== 0) {
      obj.ecdsaPubkey = base64FromBytes(message.ecdsaPubkey);
    }
    if (message.keyType !== "") {
      obj.keyType = message.keyType;
    }
    if (message.createdAtHeight !== "0") {
      obj.createdAtHeight = message.createdAtHeight;
    }
    if (message.migrationPublicKey.length !== 0) {
      obj.migrationPublicKey = base64FromBytes(message.migrationPublicKey);
    }
    if (message.migrationAlgorithmId !== 0) {
      obj.migrationAlgorithmId = Math.round(message.migrationAlgorithmId);
    }
    return obj;
  },

  create(base?: DeepPartial<PQCAccountView>): PQCAccountView {
    return PQCAccountView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PQCAccountView>): PQCAccountView {
    const message = createBasePQCAccountView();
    message.address = object.address ?? "";
    message.publicKey = object.publicKey ?? new Uint8Array(0);
    message.algorithmId = object.algorithmId ?? 0;
    message.algorithmName = object.algorithmName ?? "";
    message.ecdsaPubkey = object.ecdsaPubkey ?? new Uint8Array(0);
    message.keyType = object.keyType ?? "";
    message.createdAtHeight = object.createdAtHeight ?? "0";
    message.migrationPublicKey = object.migrationPublicKey ?? new Uint8Array(0);
    message.migrationAlgorithmId = object.migrationAlgorithmId ?? 0;
    return message;
  },
};

/** Query defines the gRPC query service for the PQC module. */
export type QueryDefinition = typeof QueryDefinition;
export const QueryDefinition = {
  name: "Query",
  fullName: "qorechain.pqc.v1.Query",
  methods: {
    /** Account returns the registered PQC key information for an address. */
    account: {
      name: "Account",
      requestType: QueryAccountRequest as typeof QueryAccountRequest,
      requestStream: false,
      responseType: QueryAccountResponse as typeof QueryAccountResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              37,
              18,
              35,
              47,
              113,
              111,
              114,
              101,
              99,
              104,
              97,
              105,
              110,
              47,
              112,
              113,
              99,
              47,
              118,
              49,
              47,
              97,
              99,
              99,
              111,
              117,
              110,
              116,
              47,
              123,
              97,
              100,
              100,
              114,
              101,
              115,
              115,
              125,
            ]) as Uint8Array,
          ],
        },
      },
    },
    /**
     * EVMWindow returns an address's open EVM-lane authorisation window, if any,
     * with what remains of it at the current height.
     */
    eVMWindow: {
      name: "EVMWindow",
      requestType: QueryEVMWindowRequest as typeof QueryEVMWindowRequest,
      requestStream: false,
      responseType: QueryEVMWindowResponse as typeof QueryEVMWindowResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              40,
              18,
              38,
              47,
              113,
              111,
              114,
              101,
              99,
              104,
              97,
              105,
              110,
              47,
              112,
              113,
              99,
              47,
              118,
              49,
              47,
              101,
              118,
              109,
              95,
              119,
              105,
              110,
              100,
              111,
              119,
              47,
              123,
              97,
              100,
              100,
              114,
              101,
              115,
              115,
              125,
            ]) as Uint8Array,
          ],
        },
      },
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
