// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/pqc/v1/hybrid.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.pqc.v1";

/**
 * PQCHybridSignature is a transaction extension option carried in
 * TxBody.extension_options. It pairs a post-quantum (Dilithium-5) signature with
 * the account's classical secp256k1 signature so every transaction can be
 * quantum-safe while remaining compatible with the standard Native auth
 * path. It is registered as a cosmos.tx.v1beta1.TxExtensionOptionI.
 */
export interface PQCHybridSignature {
  /** algorithm_id identifies the PQC signature algorithm (e.g. Dilithium-5 = 1). */
  algorithmId: number;
  /** pqc_signature is the raw PQC signature (Dilithium-5: 4627 bytes). */
  pqcSignature: Uint8Array;
  /**
   * pqc_public_key optionally carries the signer's PQC public key
   * (Dilithium-5: 2592 bytes).
   */
  pqcPublicKey: Uint8Array;
}

function createBasePQCHybridSignature(): PQCHybridSignature {
  return { algorithmId: 0, pqcSignature: new Uint8Array(0), pqcPublicKey: new Uint8Array(0) };
}

export const PQCHybridSignature: MessageFns<PQCHybridSignature> = {
  encode(message: PQCHybridSignature, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.algorithmId !== 0) {
      writer.uint32(8).uint32(message.algorithmId);
    }
    if (message.pqcSignature.length !== 0) {
      writer.uint32(18).bytes(message.pqcSignature);
    }
    if (message.pqcPublicKey.length !== 0) {
      writer.uint32(26).bytes(message.pqcPublicKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PQCHybridSignature {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePQCHybridSignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.algorithmId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pqcSignature = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pqcPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PQCHybridSignature {
    return {
      algorithmId: isSet(object.algorithmId)
        ? globalThis.Number(object.algorithmId)
        : isSet(object.algorithm_id)
        ? globalThis.Number(object.algorithm_id)
        : 0,
      pqcSignature: isSet(object.pqcSignature)
        ? bytesFromBase64(object.pqcSignature)
        : isSet(object.pqc_signature)
        ? bytesFromBase64(object.pqc_signature)
        : new Uint8Array(0),
      pqcPublicKey: isSet(object.pqcPublicKey)
        ? bytesFromBase64(object.pqcPublicKey)
        : isSet(object.pqc_public_key)
        ? bytesFromBase64(object.pqc_public_key)
        : new Uint8Array(0),
    };
  },

  toJSON(message: PQCHybridSignature): unknown {
    const obj: any = {};
    if (message.algorithmId !== 0) {
      obj.algorithmId = Math.round(message.algorithmId);
    }
    if (message.pqcSignature.length !== 0) {
      obj.pqcSignature = base64FromBytes(message.pqcSignature);
    }
    if (message.pqcPublicKey.length !== 0) {
      obj.pqcPublicKey = base64FromBytes(message.pqcPublicKey);
    }
    return obj;
  },

  create(base?: DeepPartial<PQCHybridSignature>): PQCHybridSignature {
    return PQCHybridSignature.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PQCHybridSignature>): PQCHybridSignature {
    const message = createBasePQCHybridSignature();
    message.algorithmId = object.algorithmId ?? 0;
    message.pqcSignature = object.pqcSignature ?? new Uint8Array(0);
    message.pqcPublicKey = object.pqcPublicKey ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
