// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/pqc/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.pqc.v1";

/**
 * MsgRegisterPQCKey registers a PQC keypair for an account.
 * Deprecated: use MsgRegisterPQCKeyV2 which includes algorithm_id.
 */
export interface MsgRegisterPQCKey {
  sender: string;
  dilithiumPubkey: Uint8Array;
  ecdsaPubkey: Uint8Array;
  keyType: string;
}

export interface MsgRegisterPQCKeyResponse {
}

/** MsgRegisterPQCKeyV2 registers a PQC key with explicit algorithm selection. */
export interface MsgRegisterPQCKeyV2 {
  sender: string;
  publicKey: Uint8Array;
  algorithmId: number;
  ecdsaPubkey: Uint8Array;
  keyType: string;
}

export interface MsgRegisterPQCKeyV2Response {
}

/** MsgMigratePQCKey migrates an account's PQC key, proving ownership of both keys. */
export interface MsgMigratePQCKey {
  sender: string;
  oldPublicKey: Uint8Array;
  newPublicKey: Uint8Array;
  newAlgorithmId: number;
  oldSignature: Uint8Array;
  newSignature: Uint8Array;
}

export interface MsgMigratePQCKeyResponse {
}

/** MsgDeprecateAlgorithm proposes deprecating an algorithm (starts migration period). */
export interface MsgDeprecateAlgorithm {
  authority: string;
  algorithmId: number;
  migrationBlocks: string;
  replacementAlgorithmId: number;
}

export interface MsgDeprecateAlgorithmResponse {
}

/** MsgDisableAlgorithm emergency-disables an algorithm (e.g., vulnerability discovered). */
export interface MsgDisableAlgorithm {
  authority: string;
  algorithmId: number;
  reason: string;
}

export interface MsgDisableAlgorithmResponse {
}

function createBaseMsgRegisterPQCKey(): MsgRegisterPQCKey {
  return { sender: "", dilithiumPubkey: new Uint8Array(0), ecdsaPubkey: new Uint8Array(0), keyType: "" };
}

export const MsgRegisterPQCKey: MessageFns<MsgRegisterPQCKey> = {
  encode(message: MsgRegisterPQCKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.dilithiumPubkey.length !== 0) {
      writer.uint32(18).bytes(message.dilithiumPubkey);
    }
    if (message.ecdsaPubkey.length !== 0) {
      writer.uint32(26).bytes(message.ecdsaPubkey);
    }
    if (message.keyType !== "") {
      writer.uint32(34).string(message.keyType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterPQCKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterPQCKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dilithiumPubkey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ecdsaPubkey = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.keyType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterPQCKey {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      dilithiumPubkey: isSet(object.dilithiumPubkey)
        ? bytesFromBase64(object.dilithiumPubkey)
        : isSet(object.dilithium_pubkey)
        ? bytesFromBase64(object.dilithium_pubkey)
        : new Uint8Array(0),
      ecdsaPubkey: isSet(object.ecdsaPubkey)
        ? bytesFromBase64(object.ecdsaPubkey)
        : isSet(object.ecdsa_pubkey)
        ? bytesFromBase64(object.ecdsa_pubkey)
        : new Uint8Array(0),
      keyType: isSet(object.keyType)
        ? globalThis.String(object.keyType)
        : isSet(object.key_type)
        ? globalThis.String(object.key_type)
        : "",
    };
  },

  toJSON(message: MsgRegisterPQCKey): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.dilithiumPubkey.length !== 0) {
      obj.dilithiumPubkey = base64FromBytes(message.dilithiumPubkey);
    }
    if (message.ecdsaPubkey.length !== 0) {
      obj.ecdsaPubkey = base64FromBytes(message.ecdsaPubkey);
    }
    if (message.keyType !== "") {
      obj.keyType = message.keyType;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterPQCKey>): MsgRegisterPQCKey {
    return MsgRegisterPQCKey.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterPQCKey>): MsgRegisterPQCKey {
    const message = createBaseMsgRegisterPQCKey();
    message.sender = object.sender ?? "";
    message.dilithiumPubkey = object.dilithiumPubkey ?? new Uint8Array(0);
    message.ecdsaPubkey = object.ecdsaPubkey ?? new Uint8Array(0);
    message.keyType = object.keyType ?? "";
    return message;
  },
};

function createBaseMsgRegisterPQCKeyResponse(): MsgRegisterPQCKeyResponse {
  return {};
}

export const MsgRegisterPQCKeyResponse: MessageFns<MsgRegisterPQCKeyResponse> = {
  encode(_: MsgRegisterPQCKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterPQCKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterPQCKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRegisterPQCKeyResponse {
    return {};
  },

  toJSON(_: MsgRegisterPQCKeyResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterPQCKeyResponse>): MsgRegisterPQCKeyResponse {
    return MsgRegisterPQCKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgRegisterPQCKeyResponse>): MsgRegisterPQCKeyResponse {
    const message = createBaseMsgRegisterPQCKeyResponse();
    return message;
  },
};

function createBaseMsgRegisterPQCKeyV2(): MsgRegisterPQCKeyV2 {
  return { sender: "", publicKey: new Uint8Array(0), algorithmId: 0, ecdsaPubkey: new Uint8Array(0), keyType: "" };
}

export const MsgRegisterPQCKeyV2: MessageFns<MsgRegisterPQCKeyV2> = {
  encode(message: MsgRegisterPQCKeyV2, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.publicKey.length !== 0) {
      writer.uint32(18).bytes(message.publicKey);
    }
    if (message.algorithmId !== 0) {
      writer.uint32(24).uint32(message.algorithmId);
    }
    if (message.ecdsaPubkey.length !== 0) {
      writer.uint32(34).bytes(message.ecdsaPubkey);
    }
    if (message.keyType !== "") {
      writer.uint32(42).string(message.keyType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterPQCKeyV2 {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterPQCKeyV2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.publicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.algorithmId = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ecdsaPubkey = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.keyType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterPQCKeyV2 {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      publicKey: isSet(object.publicKey)
        ? bytesFromBase64(object.publicKey)
        : isSet(object.public_key)
        ? bytesFromBase64(object.public_key)
        : new Uint8Array(0),
      algorithmId: isSet(object.algorithmId)
        ? globalThis.Number(object.algorithmId)
        : isSet(object.algorithm_id)
        ? globalThis.Number(object.algorithm_id)
        : 0,
      ecdsaPubkey: isSet(object.ecdsaPubkey)
        ? bytesFromBase64(object.ecdsaPubkey)
        : isSet(object.ecdsa_pubkey)
        ? bytesFromBase64(object.ecdsa_pubkey)
        : new Uint8Array(0),
      keyType: isSet(object.keyType)
        ? globalThis.String(object.keyType)
        : isSet(object.key_type)
        ? globalThis.String(object.key_type)
        : "",
    };
  },

  toJSON(message: MsgRegisterPQCKeyV2): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.publicKey.length !== 0) {
      obj.publicKey = base64FromBytes(message.publicKey);
    }
    if (message.algorithmId !== 0) {
      obj.algorithmId = Math.round(message.algorithmId);
    }
    if (message.ecdsaPubkey.length !== 0) {
      obj.ecdsaPubkey = base64FromBytes(message.ecdsaPubkey);
    }
    if (message.keyType !== "") {
      obj.keyType = message.keyType;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterPQCKeyV2>): MsgRegisterPQCKeyV2 {
    return MsgRegisterPQCKeyV2.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterPQCKeyV2>): MsgRegisterPQCKeyV2 {
    const message = createBaseMsgRegisterPQCKeyV2();
    message.sender = object.sender ?? "";
    message.publicKey = object.publicKey ?? new Uint8Array(0);
    message.algorithmId = object.algorithmId ?? 0;
    message.ecdsaPubkey = object.ecdsaPubkey ?? new Uint8Array(0);
    message.keyType = object.keyType ?? "";
    return message;
  },
};

function createBaseMsgRegisterPQCKeyV2Response(): MsgRegisterPQCKeyV2Response {
  return {};
}

export const MsgRegisterPQCKeyV2Response: MessageFns<MsgRegisterPQCKeyV2Response> = {
  encode(_: MsgRegisterPQCKeyV2Response, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterPQCKeyV2Response {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterPQCKeyV2Response();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRegisterPQCKeyV2Response {
    return {};
  },

  toJSON(_: MsgRegisterPQCKeyV2Response): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterPQCKeyV2Response>): MsgRegisterPQCKeyV2Response {
    return MsgRegisterPQCKeyV2Response.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgRegisterPQCKeyV2Response>): MsgRegisterPQCKeyV2Response {
    const message = createBaseMsgRegisterPQCKeyV2Response();
    return message;
  },
};

function createBaseMsgMigratePQCKey(): MsgMigratePQCKey {
  return {
    sender: "",
    oldPublicKey: new Uint8Array(0),
    newPublicKey: new Uint8Array(0),
    newAlgorithmId: 0,
    oldSignature: new Uint8Array(0),
    newSignature: new Uint8Array(0),
  };
}

export const MsgMigratePQCKey: MessageFns<MsgMigratePQCKey> = {
  encode(message: MsgMigratePQCKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.oldPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.oldPublicKey);
    }
    if (message.newPublicKey.length !== 0) {
      writer.uint32(26).bytes(message.newPublicKey);
    }
    if (message.newAlgorithmId !== 0) {
      writer.uint32(32).uint32(message.newAlgorithmId);
    }
    if (message.oldSignature.length !== 0) {
      writer.uint32(42).bytes(message.oldSignature);
    }
    if (message.newSignature.length !== 0) {
      writer.uint32(50).bytes(message.newSignature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgMigratePQCKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgMigratePQCKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.oldPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.newPublicKey = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.newAlgorithmId = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.oldSignature = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.newSignature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgMigratePQCKey {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      oldPublicKey: isSet(object.oldPublicKey)
        ? bytesFromBase64(object.oldPublicKey)
        : isSet(object.old_public_key)
        ? bytesFromBase64(object.old_public_key)
        : new Uint8Array(0),
      newPublicKey: isSet(object.newPublicKey)
        ? bytesFromBase64(object.newPublicKey)
        : isSet(object.new_public_key)
        ? bytesFromBase64(object.new_public_key)
        : new Uint8Array(0),
      newAlgorithmId: isSet(object.newAlgorithmId)
        ? globalThis.Number(object.newAlgorithmId)
        : isSet(object.new_algorithm_id)
        ? globalThis.Number(object.new_algorithm_id)
        : 0,
      oldSignature: isSet(object.oldSignature)
        ? bytesFromBase64(object.oldSignature)
        : isSet(object.old_signature)
        ? bytesFromBase64(object.old_signature)
        : new Uint8Array(0),
      newSignature: isSet(object.newSignature)
        ? bytesFromBase64(object.newSignature)
        : isSet(object.new_signature)
        ? bytesFromBase64(object.new_signature)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgMigratePQCKey): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.oldPublicKey.length !== 0) {
      obj.oldPublicKey = base64FromBytes(message.oldPublicKey);
    }
    if (message.newPublicKey.length !== 0) {
      obj.newPublicKey = base64FromBytes(message.newPublicKey);
    }
    if (message.newAlgorithmId !== 0) {
      obj.newAlgorithmId = Math.round(message.newAlgorithmId);
    }
    if (message.oldSignature.length !== 0) {
      obj.oldSignature = base64FromBytes(message.oldSignature);
    }
    if (message.newSignature.length !== 0) {
      obj.newSignature = base64FromBytes(message.newSignature);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgMigratePQCKey>): MsgMigratePQCKey {
    return MsgMigratePQCKey.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgMigratePQCKey>): MsgMigratePQCKey {
    const message = createBaseMsgMigratePQCKey();
    message.sender = object.sender ?? "";
    message.oldPublicKey = object.oldPublicKey ?? new Uint8Array(0);
    message.newPublicKey = object.newPublicKey ?? new Uint8Array(0);
    message.newAlgorithmId = object.newAlgorithmId ?? 0;
    message.oldSignature = object.oldSignature ?? new Uint8Array(0);
    message.newSignature = object.newSignature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgMigratePQCKeyResponse(): MsgMigratePQCKeyResponse {
  return {};
}

export const MsgMigratePQCKeyResponse: MessageFns<MsgMigratePQCKeyResponse> = {
  encode(_: MsgMigratePQCKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgMigratePQCKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgMigratePQCKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgMigratePQCKeyResponse {
    return {};
  },

  toJSON(_: MsgMigratePQCKeyResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgMigratePQCKeyResponse>): MsgMigratePQCKeyResponse {
    return MsgMigratePQCKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgMigratePQCKeyResponse>): MsgMigratePQCKeyResponse {
    const message = createBaseMsgMigratePQCKeyResponse();
    return message;
  },
};

function createBaseMsgDeprecateAlgorithm(): MsgDeprecateAlgorithm {
  return { authority: "", algorithmId: 0, migrationBlocks: "0", replacementAlgorithmId: 0 };
}

export const MsgDeprecateAlgorithm: MessageFns<MsgDeprecateAlgorithm> = {
  encode(message: MsgDeprecateAlgorithm, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.algorithmId !== 0) {
      writer.uint32(16).uint32(message.algorithmId);
    }
    if (message.migrationBlocks !== "0") {
      writer.uint32(24).int64(message.migrationBlocks);
    }
    if (message.replacementAlgorithmId !== 0) {
      writer.uint32(32).uint32(message.replacementAlgorithmId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgDeprecateAlgorithm {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgDeprecateAlgorithm();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.algorithmId = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.migrationBlocks = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.replacementAlgorithmId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgDeprecateAlgorithm {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      algorithmId: isSet(object.algorithmId)
        ? globalThis.Number(object.algorithmId)
        : isSet(object.algorithm_id)
        ? globalThis.Number(object.algorithm_id)
        : 0,
      migrationBlocks: isSet(object.migrationBlocks)
        ? globalThis.String(object.migrationBlocks)
        : isSet(object.migration_blocks)
        ? globalThis.String(object.migration_blocks)
        : "0",
      replacementAlgorithmId: isSet(object.replacementAlgorithmId)
        ? globalThis.Number(object.replacementAlgorithmId)
        : isSet(object.replacement_algorithm_id)
        ? globalThis.Number(object.replacement_algorithm_id)
        : 0,
    };
  },

  toJSON(message: MsgDeprecateAlgorithm): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.algorithmId !== 0) {
      obj.algorithmId = Math.round(message.algorithmId);
    }
    if (message.migrationBlocks !== "0") {
      obj.migrationBlocks = message.migrationBlocks;
    }
    if (message.replacementAlgorithmId !== 0) {
      obj.replacementAlgorithmId = Math.round(message.replacementAlgorithmId);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgDeprecateAlgorithm>): MsgDeprecateAlgorithm {
    return MsgDeprecateAlgorithm.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgDeprecateAlgorithm>): MsgDeprecateAlgorithm {
    const message = createBaseMsgDeprecateAlgorithm();
    message.authority = object.authority ?? "";
    message.algorithmId = object.algorithmId ?? 0;
    message.migrationBlocks = object.migrationBlocks ?? "0";
    message.replacementAlgorithmId = object.replacementAlgorithmId ?? 0;
    return message;
  },
};

function createBaseMsgDeprecateAlgorithmResponse(): MsgDeprecateAlgorithmResponse {
  return {};
}

export const MsgDeprecateAlgorithmResponse: MessageFns<MsgDeprecateAlgorithmResponse> = {
  encode(_: MsgDeprecateAlgorithmResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgDeprecateAlgorithmResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgDeprecateAlgorithmResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgDeprecateAlgorithmResponse {
    return {};
  },

  toJSON(_: MsgDeprecateAlgorithmResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgDeprecateAlgorithmResponse>): MsgDeprecateAlgorithmResponse {
    return MsgDeprecateAlgorithmResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgDeprecateAlgorithmResponse>): MsgDeprecateAlgorithmResponse {
    const message = createBaseMsgDeprecateAlgorithmResponse();
    return message;
  },
};

function createBaseMsgDisableAlgorithm(): MsgDisableAlgorithm {
  return { authority: "", algorithmId: 0, reason: "" };
}

export const MsgDisableAlgorithm: MessageFns<MsgDisableAlgorithm> = {
  encode(message: MsgDisableAlgorithm, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.algorithmId !== 0) {
      writer.uint32(16).uint32(message.algorithmId);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgDisableAlgorithm {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgDisableAlgorithm();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.algorithmId = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgDisableAlgorithm {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      algorithmId: isSet(object.algorithmId)
        ? globalThis.Number(object.algorithmId)
        : isSet(object.algorithm_id)
        ? globalThis.Number(object.algorithm_id)
        : 0,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: MsgDisableAlgorithm): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.algorithmId !== 0) {
      obj.algorithmId = Math.round(message.algorithmId);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgDisableAlgorithm>): MsgDisableAlgorithm {
    return MsgDisableAlgorithm.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgDisableAlgorithm>): MsgDisableAlgorithm {
    const message = createBaseMsgDisableAlgorithm();
    message.authority = object.authority ?? "";
    message.algorithmId = object.algorithmId ?? 0;
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseMsgDisableAlgorithmResponse(): MsgDisableAlgorithmResponse {
  return {};
}

export const MsgDisableAlgorithmResponse: MessageFns<MsgDisableAlgorithmResponse> = {
  encode(_: MsgDisableAlgorithmResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgDisableAlgorithmResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgDisableAlgorithmResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgDisableAlgorithmResponse {
    return {};
  },

  toJSON(_: MsgDisableAlgorithmResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgDisableAlgorithmResponse>): MsgDisableAlgorithmResponse {
    return MsgDisableAlgorithmResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgDisableAlgorithmResponse>): MsgDisableAlgorithmResponse {
    const message = createBaseMsgDisableAlgorithmResponse();
    return message;
  },
};

/** Msg defines the PQC module's transaction service. */
export type MsgDefinition = typeof MsgDefinition;
export const MsgDefinition = {
  name: "Msg",
  fullName: "qorechain.pqc.v1.Msg",
  methods: {
    /** RegisterPQCKey registers a PQC keypair for an account (legacy v1, defaults to Dilithium-5). */
    registerPQCKey: {
      name: "RegisterPQCKey",
      requestType: MsgRegisterPQCKey as typeof MsgRegisterPQCKey,
      requestStream: false,
      responseType: MsgRegisterPQCKeyResponse as typeof MsgRegisterPQCKeyResponse,
      responseStream: false,
      options: {},
    },
    /** RegisterPQCKeyV2 registers a PQC key with explicit algorithm selection. */
    registerPQCKeyV2: {
      name: "RegisterPQCKeyV2",
      requestType: MsgRegisterPQCKeyV2 as typeof MsgRegisterPQCKeyV2,
      requestStream: false,
      responseType: MsgRegisterPQCKeyV2Response as typeof MsgRegisterPQCKeyV2Response,
      responseStream: false,
      options: {},
    },
    /** MigratePQCKey migrates an account's PQC key from one algorithm to another. */
    migratePQCKey: {
      name: "MigratePQCKey",
      requestType: MsgMigratePQCKey as typeof MsgMigratePQCKey,
      requestStream: false,
      responseType: MsgMigratePQCKeyResponse as typeof MsgMigratePQCKeyResponse,
      responseStream: false,
      options: {},
    },
    /** DeprecateAlgorithm starts the migration period for an algorithm (governance). */
    deprecateAlgorithm: {
      name: "DeprecateAlgorithm",
      requestType: MsgDeprecateAlgorithm as typeof MsgDeprecateAlgorithm,
      requestStream: false,
      responseType: MsgDeprecateAlgorithmResponse as typeof MsgDeprecateAlgorithmResponse,
      responseStream: false,
      options: {},
    },
    /** DisableAlgorithm emergency-disables an algorithm (governance). */
    disableAlgorithm: {
      name: "DisableAlgorithm",
      requestType: MsgDisableAlgorithm as typeof MsgDisableAlgorithm,
      requestStream: false,
      responseType: MsgDisableAlgorithmResponse as typeof MsgDisableAlgorithmResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
