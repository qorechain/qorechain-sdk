// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/lightnode/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.lightnode.v1";

/** LightNodeView is a flat representation of a registered light node. */
export interface LightNodeView {
  address: string;
  nodeType: string;
  version: string;
  capabilities: string[];
  status: string;
  registeredAt: string;
  lastHeartbeat: string;
  totalHeartbeats: string;
  expectedHeartbeats: string;
  delegatedStake: string;
  accumulatedRewards: string;
  initialHeartbeatInterval: string;
}

export interface QueryLightNodeRequest {
  address: string;
}

export interface QueryLightNodeResponse {
  node?: LightNodeView | undefined;
  found: boolean;
}

export interface QueryLightNodesRequest {
}

export interface QueryLightNodesResponse {
  nodes: LightNodeView[];
}

export interface QueryParamsRequest {
}

export interface QueryParamsResponse {
  registrationFee: string;
  heartbeatInterval: string;
  minDelegatedStake: string;
  rewardShare: string;
  minUptimeForRewards: string;
  maxLightNodes: string;
  heartbeatGracePeriod: string;
}

export interface QueryRewardsRequest {
  address: string;
}

export interface QueryRewardsResponse {
  accumulatedRewards: string;
}

export interface QueryStatsRequest {
}

export interface QueryStatsResponse {
  totalRegistered: string;
  totalActive: string;
  totalRewards: string;
  lastRewardHeight: string;
}

function createBaseLightNodeView(): LightNodeView {
  return {
    address: "",
    nodeType: "",
    version: "",
    capabilities: [],
    status: "",
    registeredAt: "0",
    lastHeartbeat: "0",
    totalHeartbeats: "0",
    expectedHeartbeats: "0",
    delegatedStake: "",
    accumulatedRewards: "",
    initialHeartbeatInterval: "0",
  };
}

export const LightNodeView: MessageFns<LightNodeView> = {
  encode(message: LightNodeView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.nodeType !== "") {
      writer.uint32(18).string(message.nodeType);
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    for (const v of message.capabilities) {
      writer.uint32(34).string(v!);
    }
    if (message.status !== "") {
      writer.uint32(42).string(message.status);
    }
    if (message.registeredAt !== "0") {
      writer.uint32(48).int64(message.registeredAt);
    }
    if (message.lastHeartbeat !== "0") {
      writer.uint32(56).int64(message.lastHeartbeat);
    }
    if (message.totalHeartbeats !== "0") {
      writer.uint32(64).uint64(message.totalHeartbeats);
    }
    if (message.expectedHeartbeats !== "0") {
      writer.uint32(72).uint64(message.expectedHeartbeats);
    }
    if (message.delegatedStake !== "") {
      writer.uint32(82).string(message.delegatedStake);
    }
    if (message.accumulatedRewards !== "") {
      writer.uint32(90).string(message.accumulatedRewards);
    }
    if (message.initialHeartbeatInterval !== "0") {
      writer.uint32(96).int64(message.initialHeartbeatInterval);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LightNodeView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLightNodeView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nodeType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.capabilities.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.registeredAt = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.lastHeartbeat = reader.int64().toString();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.totalHeartbeats = reader.uint64().toString();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.expectedHeartbeats = reader.uint64().toString();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.delegatedStake = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.accumulatedRewards = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.initialHeartbeatInterval = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LightNodeView {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      nodeType: isSet(object.nodeType)
        ? globalThis.String(object.nodeType)
        : isSet(object.node_type)
        ? globalThis.String(object.node_type)
        : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => globalThis.String(e))
        : [],
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      registeredAt: isSet(object.registeredAt)
        ? globalThis.String(object.registeredAt)
        : isSet(object.registered_at)
        ? globalThis.String(object.registered_at)
        : "0",
      lastHeartbeat: isSet(object.lastHeartbeat)
        ? globalThis.String(object.lastHeartbeat)
        : isSet(object.last_heartbeat)
        ? globalThis.String(object.last_heartbeat)
        : "0",
      totalHeartbeats: isSet(object.totalHeartbeats)
        ? globalThis.String(object.totalHeartbeats)
        : isSet(object.total_heartbeats)
        ? globalThis.String(object.total_heartbeats)
        : "0",
      expectedHeartbeats: isSet(object.expectedHeartbeats)
        ? globalThis.String(object.expectedHeartbeats)
        : isSet(object.expected_heartbeats)
        ? globalThis.String(object.expected_heartbeats)
        : "0",
      delegatedStake: isSet(object.delegatedStake)
        ? globalThis.String(object.delegatedStake)
        : isSet(object.delegated_stake)
        ? globalThis.String(object.delegated_stake)
        : "",
      accumulatedRewards: isSet(object.accumulatedRewards)
        ? globalThis.String(object.accumulatedRewards)
        : isSet(object.accumulated_rewards)
        ? globalThis.String(object.accumulated_rewards)
        : "",
      initialHeartbeatInterval: isSet(object.initialHeartbeatInterval)
        ? globalThis.String(object.initialHeartbeatInterval)
        : isSet(object.initial_heartbeat_interval)
        ? globalThis.String(object.initial_heartbeat_interval)
        : "0",
    };
  },

  toJSON(message: LightNodeView): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.nodeType !== "") {
      obj.nodeType = message.nodeType;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.registeredAt !== "0") {
      obj.registeredAt = message.registeredAt;
    }
    if (message.lastHeartbeat !== "0") {
      obj.lastHeartbeat = message.lastHeartbeat;
    }
    if (message.totalHeartbeats !== "0") {
      obj.totalHeartbeats = message.totalHeartbeats;
    }
    if (message.expectedHeartbeats !== "0") {
      obj.expectedHeartbeats = message.expectedHeartbeats;
    }
    if (message.delegatedStake !== "") {
      obj.delegatedStake = message.delegatedStake;
    }
    if (message.accumulatedRewards !== "") {
      obj.accumulatedRewards = message.accumulatedRewards;
    }
    if (message.initialHeartbeatInterval !== "0") {
      obj.initialHeartbeatInterval = message.initialHeartbeatInterval;
    }
    return obj;
  },

  create(base?: DeepPartial<LightNodeView>): LightNodeView {
    return LightNodeView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LightNodeView>): LightNodeView {
    const message = createBaseLightNodeView();
    message.address = object.address ?? "";
    message.nodeType = object.nodeType ?? "";
    message.version = object.version ?? "";
    message.capabilities = object.capabilities?.map((e) => e) || [];
    message.status = object.status ?? "";
    message.registeredAt = object.registeredAt ?? "0";
    message.lastHeartbeat = object.lastHeartbeat ?? "0";
    message.totalHeartbeats = object.totalHeartbeats ?? "0";
    message.expectedHeartbeats = object.expectedHeartbeats ?? "0";
    message.delegatedStake = object.delegatedStake ?? "";
    message.accumulatedRewards = object.accumulatedRewards ?? "";
    message.initialHeartbeatInterval = object.initialHeartbeatInterval ?? "0";
    return message;
  },
};

function createBaseQueryLightNodeRequest(): QueryLightNodeRequest {
  return { address: "" };
}

export const QueryLightNodeRequest: MessageFns<QueryLightNodeRequest> = {
  encode(message: QueryLightNodeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryLightNodeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryLightNodeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryLightNodeRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryLightNodeRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryLightNodeRequest>): QueryLightNodeRequest {
    return QueryLightNodeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryLightNodeRequest>): QueryLightNodeRequest {
    const message = createBaseQueryLightNodeRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryLightNodeResponse(): QueryLightNodeResponse {
  return { node: undefined, found: false };
}

export const QueryLightNodeResponse: MessageFns<QueryLightNodeResponse> = {
  encode(message: QueryLightNodeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.node !== undefined) {
      LightNodeView.encode(message.node, writer.uint32(10).fork()).join();
    }
    if (message.found !== false) {
      writer.uint32(16).bool(message.found);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryLightNodeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryLightNodeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.node = LightNodeView.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryLightNodeResponse {
    return {
      node: isSet(object.node) ? LightNodeView.fromJSON(object.node) : undefined,
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
    };
  },

  toJSON(message: QueryLightNodeResponse): unknown {
    const obj: any = {};
    if (message.node !== undefined) {
      obj.node = LightNodeView.toJSON(message.node);
    }
    if (message.found !== false) {
      obj.found = message.found;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryLightNodeResponse>): QueryLightNodeResponse {
    return QueryLightNodeResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryLightNodeResponse>): QueryLightNodeResponse {
    const message = createBaseQueryLightNodeResponse();
    message.node = (object.node !== undefined && object.node !== null)
      ? LightNodeView.fromPartial(object.node)
      : undefined;
    message.found = object.found ?? false;
    return message;
  },
};

function createBaseQueryLightNodesRequest(): QueryLightNodesRequest {
  return {};
}

export const QueryLightNodesRequest: MessageFns<QueryLightNodesRequest> = {
  encode(_: QueryLightNodesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryLightNodesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryLightNodesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryLightNodesRequest {
    return {};
  },

  toJSON(_: QueryLightNodesRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryLightNodesRequest>): QueryLightNodesRequest {
    return QueryLightNodesRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryLightNodesRequest>): QueryLightNodesRequest {
    const message = createBaseQueryLightNodesRequest();
    return message;
  },
};

function createBaseQueryLightNodesResponse(): QueryLightNodesResponse {
  return { nodes: [] };
}

export const QueryLightNodesResponse: MessageFns<QueryLightNodesResponse> = {
  encode(message: QueryLightNodesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.nodes) {
      LightNodeView.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryLightNodesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryLightNodesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nodes.push(LightNodeView.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryLightNodesResponse {
    return {
      nodes: globalThis.Array.isArray(object?.nodes) ? object.nodes.map((e: any) => LightNodeView.fromJSON(e)) : [],
    };
  },

  toJSON(message: QueryLightNodesResponse): unknown {
    const obj: any = {};
    if (message.nodes?.length) {
      obj.nodes = message.nodes.map((e) => LightNodeView.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<QueryLightNodesResponse>): QueryLightNodesResponse {
    return QueryLightNodesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryLightNodesResponse>): QueryLightNodesResponse {
    const message = createBaseQueryLightNodesResponse();
    message.nodes = object.nodes?.map((e) => LightNodeView.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return {
    registrationFee: "",
    heartbeatInterval: "0",
    minDelegatedStake: "",
    rewardShare: "",
    minUptimeForRewards: "",
    maxLightNodes: "0",
    heartbeatGracePeriod: "0",
  };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.registrationFee !== "") {
      writer.uint32(10).string(message.registrationFee);
    }
    if (message.heartbeatInterval !== "0") {
      writer.uint32(16).int64(message.heartbeatInterval);
    }
    if (message.minDelegatedStake !== "") {
      writer.uint32(26).string(message.minDelegatedStake);
    }
    if (message.rewardShare !== "") {
      writer.uint32(34).string(message.rewardShare);
    }
    if (message.minUptimeForRewards !== "") {
      writer.uint32(42).string(message.minUptimeForRewards);
    }
    if (message.maxLightNodes !== "0") {
      writer.uint32(48).uint64(message.maxLightNodes);
    }
    if (message.heartbeatGracePeriod !== "0") {
      writer.uint32(56).int64(message.heartbeatGracePeriod);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.registrationFee = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.heartbeatInterval = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.minDelegatedStake = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.rewardShare = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.minUptimeForRewards = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.maxLightNodes = reader.uint64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.heartbeatGracePeriod = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return {
      registrationFee: isSet(object.registrationFee)
        ? globalThis.String(object.registrationFee)
        : isSet(object.registration_fee)
        ? globalThis.String(object.registration_fee)
        : "",
      heartbeatInterval: isSet(object.heartbeatInterval)
        ? globalThis.String(object.heartbeatInterval)
        : isSet(object.heartbeat_interval)
        ? globalThis.String(object.heartbeat_interval)
        : "0",
      minDelegatedStake: isSet(object.minDelegatedStake)
        ? globalThis.String(object.minDelegatedStake)
        : isSet(object.min_delegated_stake)
        ? globalThis.String(object.min_delegated_stake)
        : "",
      rewardShare: isSet(object.rewardShare)
        ? globalThis.String(object.rewardShare)
        : isSet(object.reward_share)
        ? globalThis.String(object.reward_share)
        : "",
      minUptimeForRewards: isSet(object.minUptimeForRewards)
        ? globalThis.String(object.minUptimeForRewards)
        : isSet(object.min_uptime_for_rewards)
        ? globalThis.String(object.min_uptime_for_rewards)
        : "",
      maxLightNodes: isSet(object.maxLightNodes)
        ? globalThis.String(object.maxLightNodes)
        : isSet(object.max_light_nodes)
        ? globalThis.String(object.max_light_nodes)
        : "0",
      heartbeatGracePeriod: isSet(object.heartbeatGracePeriod)
        ? globalThis.String(object.heartbeatGracePeriod)
        : isSet(object.heartbeat_grace_period)
        ? globalThis.String(object.heartbeat_grace_period)
        : "0",
    };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.registrationFee !== "") {
      obj.registrationFee = message.registrationFee;
    }
    if (message.heartbeatInterval !== "0") {
      obj.heartbeatInterval = message.heartbeatInterval;
    }
    if (message.minDelegatedStake !== "") {
      obj.minDelegatedStake = message.minDelegatedStake;
    }
    if (message.rewardShare !== "") {
      obj.rewardShare = message.rewardShare;
    }
    if (message.minUptimeForRewards !== "") {
      obj.minUptimeForRewards = message.minUptimeForRewards;
    }
    if (message.maxLightNodes !== "0") {
      obj.maxLightNodes = message.maxLightNodes;
    }
    if (message.heartbeatGracePeriod !== "0") {
      obj.heartbeatGracePeriod = message.heartbeatGracePeriod;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.registrationFee = object.registrationFee ?? "";
    message.heartbeatInterval = object.heartbeatInterval ?? "0";
    message.minDelegatedStake = object.minDelegatedStake ?? "";
    message.rewardShare = object.rewardShare ?? "";
    message.minUptimeForRewards = object.minUptimeForRewards ?? "";
    message.maxLightNodes = object.maxLightNodes ?? "0";
    message.heartbeatGracePeriod = object.heartbeatGracePeriod ?? "0";
    return message;
  },
};

function createBaseQueryRewardsRequest(): QueryRewardsRequest {
  return { address: "" };
}

export const QueryRewardsRequest: MessageFns<QueryRewardsRequest> = {
  encode(message: QueryRewardsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRewardsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRewardsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRewardsRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryRewardsRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRewardsRequest>): QueryRewardsRequest {
    return QueryRewardsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRewardsRequest>): QueryRewardsRequest {
    const message = createBaseQueryRewardsRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryRewardsResponse(): QueryRewardsResponse {
  return { accumulatedRewards: "" };
}

export const QueryRewardsResponse: MessageFns<QueryRewardsResponse> = {
  encode(message: QueryRewardsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accumulatedRewards !== "") {
      writer.uint32(10).string(message.accumulatedRewards);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRewardsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRewardsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accumulatedRewards = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRewardsResponse {
    return {
      accumulatedRewards: isSet(object.accumulatedRewards)
        ? globalThis.String(object.accumulatedRewards)
        : isSet(object.accumulated_rewards)
        ? globalThis.String(object.accumulated_rewards)
        : "",
    };
  },

  toJSON(message: QueryRewardsResponse): unknown {
    const obj: any = {};
    if (message.accumulatedRewards !== "") {
      obj.accumulatedRewards = message.accumulatedRewards;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRewardsResponse>): QueryRewardsResponse {
    return QueryRewardsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRewardsResponse>): QueryRewardsResponse {
    const message = createBaseQueryRewardsResponse();
    message.accumulatedRewards = object.accumulatedRewards ?? "";
    return message;
  },
};

function createBaseQueryStatsRequest(): QueryStatsRequest {
  return {};
}

export const QueryStatsRequest: MessageFns<QueryStatsRequest> = {
  encode(_: QueryStatsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryStatsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryStatsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryStatsRequest {
    return {};
  },

  toJSON(_: QueryStatsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryStatsRequest>): QueryStatsRequest {
    return QueryStatsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryStatsRequest>): QueryStatsRequest {
    const message = createBaseQueryStatsRequest();
    return message;
  },
};

function createBaseQueryStatsResponse(): QueryStatsResponse {
  return { totalRegistered: "0", totalActive: "0", totalRewards: "", lastRewardHeight: "0" };
}

export const QueryStatsResponse: MessageFns<QueryStatsResponse> = {
  encode(message: QueryStatsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totalRegistered !== "0") {
      writer.uint32(8).uint64(message.totalRegistered);
    }
    if (message.totalActive !== "0") {
      writer.uint32(16).uint64(message.totalActive);
    }
    if (message.totalRewards !== "") {
      writer.uint32(26).string(message.totalRewards);
    }
    if (message.lastRewardHeight !== "0") {
      writer.uint32(32).int64(message.lastRewardHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryStatsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryStatsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.totalRegistered = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalActive = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.totalRewards = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lastRewardHeight = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryStatsResponse {
    return {
      totalRegistered: isSet(object.totalRegistered)
        ? globalThis.String(object.totalRegistered)
        : isSet(object.total_registered)
        ? globalThis.String(object.total_registered)
        : "0",
      totalActive: isSet(object.totalActive)
        ? globalThis.String(object.totalActive)
        : isSet(object.total_active)
        ? globalThis.String(object.total_active)
        : "0",
      totalRewards: isSet(object.totalRewards)
        ? globalThis.String(object.totalRewards)
        : isSet(object.total_rewards)
        ? globalThis.String(object.total_rewards)
        : "",
      lastRewardHeight: isSet(object.lastRewardHeight)
        ? globalThis.String(object.lastRewardHeight)
        : isSet(object.last_reward_height)
        ? globalThis.String(object.last_reward_height)
        : "0",
    };
  },

  toJSON(message: QueryStatsResponse): unknown {
    const obj: any = {};
    if (message.totalRegistered !== "0") {
      obj.totalRegistered = message.totalRegistered;
    }
    if (message.totalActive !== "0") {
      obj.totalActive = message.totalActive;
    }
    if (message.totalRewards !== "") {
      obj.totalRewards = message.totalRewards;
    }
    if (message.lastRewardHeight !== "0") {
      obj.lastRewardHeight = message.lastRewardHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryStatsResponse>): QueryStatsResponse {
    return QueryStatsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryStatsResponse>): QueryStatsResponse {
    const message = createBaseQueryStatsResponse();
    message.totalRegistered = object.totalRegistered ?? "0";
    message.totalActive = object.totalActive ?? "0";
    message.totalRewards = object.totalRewards ?? "";
    message.lastRewardHeight = object.lastRewardHeight ?? "0";
    return message;
  },
};

/** Query defines the lightnode module's gRPC query service. */
export type QueryDefinition = typeof QueryDefinition;
export const QueryDefinition = {
  name: "Query",
  fullName: "qorechain.lightnode.v1.Query",
  methods: {
    /** LightNode returns a single light node by operator address. */
    lightNode: {
      name: "LightNode",
      requestType: QueryLightNodeRequest as typeof QueryLightNodeRequest,
      requestStream: false,
      responseType: QueryLightNodeResponse as typeof QueryLightNodeResponse,
      responseStream: false,
      options: {},
    },
    /** LightNodes lists all registered light nodes. */
    lightNodes: {
      name: "LightNodes",
      requestType: QueryLightNodesRequest as typeof QueryLightNodesRequest,
      requestStream: false,
      responseType: QueryLightNodesResponse as typeof QueryLightNodesResponse,
      responseStream: false,
      options: {},
    },
    /** Params returns the lightnode module parameters. */
    params: {
      name: "Params",
      requestType: QueryParamsRequest as typeof QueryParamsRequest,
      requestStream: false,
      responseType: QueryParamsResponse as typeof QueryParamsResponse,
      responseStream: false,
      options: {},
    },
    /** Rewards returns an operator's accumulated (claimable) rewards. */
    rewards: {
      name: "Rewards",
      requestType: QueryRewardsRequest as typeof QueryRewardsRequest,
      requestStream: false,
      responseType: QueryRewardsResponse as typeof QueryRewardsResponse,
      responseStream: false,
      options: {},
    },
    /** Stats returns module-level light node statistics. */
    stats: {
      name: "Stats",
      requestType: QueryStatsRequest as typeof QueryStatsRequest,
      requestStream: false,
      responseType: QueryStatsResponse as typeof QueryStatsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
