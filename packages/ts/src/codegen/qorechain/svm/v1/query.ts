// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/svm/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.svm.v1";

export interface QuerySlotRequest {
}

export interface QuerySlotResponse {
  slot: string;
}

export interface QueryAccountRequest {
  /** address is the base58-encoded 32-byte SVM account address. */
  address: string;
}

export interface QueryAccountResponse {
  account?: SVMAccountInfo | undefined;
}

export interface QueryProgramRequest {
  /** address is the base58-encoded 32-byte program address. */
  address: string;
}

export interface QueryProgramResponse {
  program?: ProgramInfo | undefined;
}

/** SVMAccountInfo is the query-facing view of an SVM account. */
export interface SVMAccountInfo {
  address: string;
  lamports: string;
  dataLen: string;
  data: Uint8Array;
  owner: string;
  executable: boolean;
  rentEpoch: string;
}

/** ProgramInfo is the query-facing view of program metadata. */
export interface ProgramInfo {
  programAddress: string;
  upgradeAuthority: string;
  deploySlot: string;
  lastDeploySlot: string;
  dataAccount: string;
}

function createBaseQuerySlotRequest(): QuerySlotRequest {
  return {};
}

export const QuerySlotRequest: MessageFns<QuerySlotRequest> = {
  encode(_: QuerySlotRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySlotRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySlotRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QuerySlotRequest {
    return {};
  },

  toJSON(_: QuerySlotRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QuerySlotRequest>): QuerySlotRequest {
    return QuerySlotRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QuerySlotRequest>): QuerySlotRequest {
    const message = createBaseQuerySlotRequest();
    return message;
  },
};

function createBaseQuerySlotResponse(): QuerySlotResponse {
  return { slot: "0" };
}

export const QuerySlotResponse: MessageFns<QuerySlotResponse> = {
  encode(message: QuerySlotResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slot !== "0") {
      writer.uint32(8).uint64(message.slot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuerySlotResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySlotResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.slot = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySlotResponse {
    return { slot: isSet(object.slot) ? globalThis.String(object.slot) : "0" };
  },

  toJSON(message: QuerySlotResponse): unknown {
    const obj: any = {};
    if (message.slot !== "0") {
      obj.slot = message.slot;
    }
    return obj;
  },

  create(base?: DeepPartial<QuerySlotResponse>): QuerySlotResponse {
    return QuerySlotResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QuerySlotResponse>): QuerySlotResponse {
    const message = createBaseQuerySlotResponse();
    message.slot = object.slot ?? "0";
    return message;
  },
};

function createBaseQueryAccountRequest(): QueryAccountRequest {
  return { address: "" };
}

export const QueryAccountRequest: MessageFns<QueryAccountRequest> = {
  encode(message: QueryAccountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAccountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAccountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAccountRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryAccountRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAccountRequest>): QueryAccountRequest {
    return QueryAccountRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAccountRequest>): QueryAccountRequest {
    const message = createBaseQueryAccountRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryAccountResponse(): QueryAccountResponse {
  return { account: undefined };
}

export const QueryAccountResponse: MessageFns<QueryAccountResponse> = {
  encode(message: QueryAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.account !== undefined) {
      SVMAccountInfo.encode(message.account, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAccountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.account = SVMAccountInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAccountResponse {
    return { account: isSet(object.account) ? SVMAccountInfo.fromJSON(object.account) : undefined };
  },

  toJSON(message: QueryAccountResponse): unknown {
    const obj: any = {};
    if (message.account !== undefined) {
      obj.account = SVMAccountInfo.toJSON(message.account);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAccountResponse>): QueryAccountResponse {
    return QueryAccountResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAccountResponse>): QueryAccountResponse {
    const message = createBaseQueryAccountResponse();
    message.account = (object.account !== undefined && object.account !== null)
      ? SVMAccountInfo.fromPartial(object.account)
      : undefined;
    return message;
  },
};

function createBaseQueryProgramRequest(): QueryProgramRequest {
  return { address: "" };
}

export const QueryProgramRequest: MessageFns<QueryProgramRequest> = {
  encode(message: QueryProgramRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryProgramRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryProgramRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryProgramRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryProgramRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryProgramRequest>): QueryProgramRequest {
    return QueryProgramRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryProgramRequest>): QueryProgramRequest {
    const message = createBaseQueryProgramRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryProgramResponse(): QueryProgramResponse {
  return { program: undefined };
}

export const QueryProgramResponse: MessageFns<QueryProgramResponse> = {
  encode(message: QueryProgramResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.program !== undefined) {
      ProgramInfo.encode(message.program, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryProgramResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryProgramResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.program = ProgramInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryProgramResponse {
    return { program: isSet(object.program) ? ProgramInfo.fromJSON(object.program) : undefined };
  },

  toJSON(message: QueryProgramResponse): unknown {
    const obj: any = {};
    if (message.program !== undefined) {
      obj.program = ProgramInfo.toJSON(message.program);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryProgramResponse>): QueryProgramResponse {
    return QueryProgramResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryProgramResponse>): QueryProgramResponse {
    const message = createBaseQueryProgramResponse();
    message.program = (object.program !== undefined && object.program !== null)
      ? ProgramInfo.fromPartial(object.program)
      : undefined;
    return message;
  },
};

function createBaseSVMAccountInfo(): SVMAccountInfo {
  return {
    address: "",
    lamports: "0",
    dataLen: "0",
    data: new Uint8Array(0),
    owner: "",
    executable: false,
    rentEpoch: "0",
  };
}

export const SVMAccountInfo: MessageFns<SVMAccountInfo> = {
  encode(message: SVMAccountInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.lamports !== "0") {
      writer.uint32(16).uint64(message.lamports);
    }
    if (message.dataLen !== "0") {
      writer.uint32(24).uint64(message.dataLen);
    }
    if (message.data.length !== 0) {
      writer.uint32(34).bytes(message.data);
    }
    if (message.owner !== "") {
      writer.uint32(42).string(message.owner);
    }
    if (message.executable !== false) {
      writer.uint32(48).bool(message.executable);
    }
    if (message.rentEpoch !== "0") {
      writer.uint32(56).uint64(message.rentEpoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SVMAccountInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSVMAccountInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.lamports = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dataLen = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.executable = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.rentEpoch = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SVMAccountInfo {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      lamports: isSet(object.lamports) ? globalThis.String(object.lamports) : "0",
      dataLen: isSet(object.dataLen)
        ? globalThis.String(object.dataLen)
        : isSet(object.data_len)
        ? globalThis.String(object.data_len)
        : "0",
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      executable: isSet(object.executable) ? globalThis.Boolean(object.executable) : false,
      rentEpoch: isSet(object.rentEpoch)
        ? globalThis.String(object.rentEpoch)
        : isSet(object.rent_epoch)
        ? globalThis.String(object.rent_epoch)
        : "0",
    };
  },

  toJSON(message: SVMAccountInfo): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.lamports !== "0") {
      obj.lamports = message.lamports;
    }
    if (message.dataLen !== "0") {
      obj.dataLen = message.dataLen;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.executable !== false) {
      obj.executable = message.executable;
    }
    if (message.rentEpoch !== "0") {
      obj.rentEpoch = message.rentEpoch;
    }
    return obj;
  },

  create(base?: DeepPartial<SVMAccountInfo>): SVMAccountInfo {
    return SVMAccountInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SVMAccountInfo>): SVMAccountInfo {
    const message = createBaseSVMAccountInfo();
    message.address = object.address ?? "";
    message.lamports = object.lamports ?? "0";
    message.dataLen = object.dataLen ?? "0";
    message.data = object.data ?? new Uint8Array(0);
    message.owner = object.owner ?? "";
    message.executable = object.executable ?? false;
    message.rentEpoch = object.rentEpoch ?? "0";
    return message;
  },
};

function createBaseProgramInfo(): ProgramInfo {
  return { programAddress: "", upgradeAuthority: "", deploySlot: "0", lastDeploySlot: "0", dataAccount: "" };
}

export const ProgramInfo: MessageFns<ProgramInfo> = {
  encode(message: ProgramInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.programAddress !== "") {
      writer.uint32(10).string(message.programAddress);
    }
    if (message.upgradeAuthority !== "") {
      writer.uint32(18).string(message.upgradeAuthority);
    }
    if (message.deploySlot !== "0") {
      writer.uint32(24).uint64(message.deploySlot);
    }
    if (message.lastDeploySlot !== "0") {
      writer.uint32(32).uint64(message.lastDeploySlot);
    }
    if (message.dataAccount !== "") {
      writer.uint32(42).string(message.dataAccount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProgramInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProgramInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.programAddress = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.upgradeAuthority = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.deploySlot = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lastDeploySlot = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.dataAccount = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProgramInfo {
    return {
      programAddress: isSet(object.programAddress)
        ? globalThis.String(object.programAddress)
        : isSet(object.program_address)
        ? globalThis.String(object.program_address)
        : "",
      upgradeAuthority: isSet(object.upgradeAuthority)
        ? globalThis.String(object.upgradeAuthority)
        : isSet(object.upgrade_authority)
        ? globalThis.String(object.upgrade_authority)
        : "",
      deploySlot: isSet(object.deploySlot)
        ? globalThis.String(object.deploySlot)
        : isSet(object.deploy_slot)
        ? globalThis.String(object.deploy_slot)
        : "0",
      lastDeploySlot: isSet(object.lastDeploySlot)
        ? globalThis.String(object.lastDeploySlot)
        : isSet(object.last_deploy_slot)
        ? globalThis.String(object.last_deploy_slot)
        : "0",
      dataAccount: isSet(object.dataAccount)
        ? globalThis.String(object.dataAccount)
        : isSet(object.data_account)
        ? globalThis.String(object.data_account)
        : "",
    };
  },

  toJSON(message: ProgramInfo): unknown {
    const obj: any = {};
    if (message.programAddress !== "") {
      obj.programAddress = message.programAddress;
    }
    if (message.upgradeAuthority !== "") {
      obj.upgradeAuthority = message.upgradeAuthority;
    }
    if (message.deploySlot !== "0") {
      obj.deploySlot = message.deploySlot;
    }
    if (message.lastDeploySlot !== "0") {
      obj.lastDeploySlot = message.lastDeploySlot;
    }
    if (message.dataAccount !== "") {
      obj.dataAccount = message.dataAccount;
    }
    return obj;
  },

  create(base?: DeepPartial<ProgramInfo>): ProgramInfo {
    return ProgramInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProgramInfo>): ProgramInfo {
    const message = createBaseProgramInfo();
    message.programAddress = object.programAddress ?? "";
    message.upgradeAuthority = object.upgradeAuthority ?? "";
    message.deploySlot = object.deploySlot ?? "0";
    message.lastDeploySlot = object.lastDeploySlot ?? "0";
    message.dataAccount = object.dataAccount ?? "";
    return message;
  },
};

/** Query defines the gRPC query service for the SVM module. */
export type QueryDefinition = typeof QueryDefinition;
export const QueryDefinition = {
  name: "Query",
  fullName: "qorechain.svm.v1.Query",
  methods: {
    /** Slot returns the current SVM virtual slot. */
    slot: {
      name: "Slot",
      requestType: QuerySlotRequest as typeof QuerySlotRequest,
      requestStream: false,
      responseType: QuerySlotResponse as typeof QuerySlotResponse,
      responseStream: false,
      options: {},
    },
    /** Account returns the SVM account at a base58 address. */
    account: {
      name: "Account",
      requestType: QueryAccountRequest as typeof QueryAccountRequest,
      requestStream: false,
      responseType: QueryAccountResponse as typeof QueryAccountResponse,
      responseStream: false,
      options: {},
    },
    /** Program returns program metadata for a base58 program address. */
    program: {
      name: "Program",
      requestType: QueryProgramRequest as typeof QueryProgramRequest,
      requestStream: false,
      responseType: QueryProgramResponse as typeof QueryProgramResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
