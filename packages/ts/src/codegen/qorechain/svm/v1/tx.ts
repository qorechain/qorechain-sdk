// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/svm/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.svm.v1";

export interface MsgDeployProgram {
  sender: string;
  bytecode: Uint8Array;
}

export interface MsgDeployProgramResponse {
  programId: Uint8Array;
}

export interface MsgCreateAccount {
  sender: string;
  owner: Uint8Array;
  space: string;
  lamports: string;
  salt: Uint8Array;
}

export interface MsgCreateAccountResponse {
}

export interface SvmAccountMeta {
  address: Uint8Array;
  isSigner: boolean;
  isWritable: boolean;
}

export interface MsgExecuteProgram {
  sender: string;
  programId: Uint8Array;
  accounts: SvmAccountMeta[];
  data: Uint8Array;
}

export interface MsgExecuteProgramResponse {
  result: Uint8Array;
}

export interface MsgRegisterSVMPQCKey {
  sender: string;
  svmAddr: Uint8Array;
  pqcPubKey: Uint8Array;
}

export interface MsgRegisterSVMPQCKeyResponse {
}

function createBaseMsgDeployProgram(): MsgDeployProgram {
  return { sender: "", bytecode: new Uint8Array(0) };
}

export const MsgDeployProgram: MessageFns<MsgDeployProgram> = {
  encode(message: MsgDeployProgram, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.bytecode.length !== 0) {
      writer.uint32(18).bytes(message.bytecode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgDeployProgram {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgDeployProgram();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bytecode = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgDeployProgram {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      bytecode: isSet(object.bytecode) ? bytesFromBase64(object.bytecode) : new Uint8Array(0),
    };
  },

  toJSON(message: MsgDeployProgram): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.bytecode.length !== 0) {
      obj.bytecode = base64FromBytes(message.bytecode);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgDeployProgram>): MsgDeployProgram {
    return MsgDeployProgram.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgDeployProgram>): MsgDeployProgram {
    const message = createBaseMsgDeployProgram();
    message.sender = object.sender ?? "";
    message.bytecode = object.bytecode ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgDeployProgramResponse(): MsgDeployProgramResponse {
  return { programId: new Uint8Array(0) };
}

export const MsgDeployProgramResponse: MessageFns<MsgDeployProgramResponse> = {
  encode(message: MsgDeployProgramResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.programId.length !== 0) {
      writer.uint32(10).bytes(message.programId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgDeployProgramResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgDeployProgramResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.programId = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgDeployProgramResponse {
    return {
      programId: isSet(object.programId)
        ? bytesFromBase64(object.programId)
        : isSet(object.program_id)
        ? bytesFromBase64(object.program_id)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgDeployProgramResponse): unknown {
    const obj: any = {};
    if (message.programId.length !== 0) {
      obj.programId = base64FromBytes(message.programId);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgDeployProgramResponse>): MsgDeployProgramResponse {
    return MsgDeployProgramResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgDeployProgramResponse>): MsgDeployProgramResponse {
    const message = createBaseMsgDeployProgramResponse();
    message.programId = object.programId ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgCreateAccount(): MsgCreateAccount {
  return { sender: "", owner: new Uint8Array(0), space: "0", lamports: "0", salt: new Uint8Array(0) };
}

export const MsgCreateAccount: MessageFns<MsgCreateAccount> = {
  encode(message: MsgCreateAccount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.owner.length !== 0) {
      writer.uint32(18).bytes(message.owner);
    }
    if (message.space !== "0") {
      writer.uint32(24).uint64(message.space);
    }
    if (message.lamports !== "0") {
      writer.uint32(32).uint64(message.lamports);
    }
    if (message.salt.length !== 0) {
      writer.uint32(42).bytes(message.salt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCreateAccount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCreateAccount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.owner = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.space = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lamports = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.salt = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCreateAccount {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      owner: isSet(object.owner) ? bytesFromBase64(object.owner) : new Uint8Array(0),
      space: isSet(object.space) ? globalThis.String(object.space) : "0",
      lamports: isSet(object.lamports) ? globalThis.String(object.lamports) : "0",
      salt: isSet(object.salt) ? bytesFromBase64(object.salt) : new Uint8Array(0),
    };
  },

  toJSON(message: MsgCreateAccount): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.owner.length !== 0) {
      obj.owner = base64FromBytes(message.owner);
    }
    if (message.space !== "0") {
      obj.space = message.space;
    }
    if (message.lamports !== "0") {
      obj.lamports = message.lamports;
    }
    if (message.salt.length !== 0) {
      obj.salt = base64FromBytes(message.salt);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgCreateAccount>): MsgCreateAccount {
    return MsgCreateAccount.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgCreateAccount>): MsgCreateAccount {
    const message = createBaseMsgCreateAccount();
    message.sender = object.sender ?? "";
    message.owner = object.owner ?? new Uint8Array(0);
    message.space = object.space ?? "0";
    message.lamports = object.lamports ?? "0";
    message.salt = object.salt ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgCreateAccountResponse(): MsgCreateAccountResponse {
  return {};
}

export const MsgCreateAccountResponse: MessageFns<MsgCreateAccountResponse> = {
  encode(_: MsgCreateAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCreateAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCreateAccountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgCreateAccountResponse {
    return {};
  },

  toJSON(_: MsgCreateAccountResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgCreateAccountResponse>): MsgCreateAccountResponse {
    return MsgCreateAccountResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgCreateAccountResponse>): MsgCreateAccountResponse {
    const message = createBaseMsgCreateAccountResponse();
    return message;
  },
};

function createBaseSvmAccountMeta(): SvmAccountMeta {
  return { address: new Uint8Array(0), isSigner: false, isWritable: false };
}

export const SvmAccountMeta: MessageFns<SvmAccountMeta> = {
  encode(message: SvmAccountMeta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address.length !== 0) {
      writer.uint32(10).bytes(message.address);
    }
    if (message.isSigner !== false) {
      writer.uint32(16).bool(message.isSigner);
    }
    if (message.isWritable !== false) {
      writer.uint32(24).bool(message.isWritable);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SvmAccountMeta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSvmAccountMeta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isSigner = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isWritable = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SvmAccountMeta {
    return {
      address: isSet(object.address) ? bytesFromBase64(object.address) : new Uint8Array(0),
      isSigner: isSet(object.isSigner)
        ? globalThis.Boolean(object.isSigner)
        : isSet(object.is_signer)
        ? globalThis.Boolean(object.is_signer)
        : false,
      isWritable: isSet(object.isWritable)
        ? globalThis.Boolean(object.isWritable)
        : isSet(object.is_writable)
        ? globalThis.Boolean(object.is_writable)
        : false,
    };
  },

  toJSON(message: SvmAccountMeta): unknown {
    const obj: any = {};
    if (message.address.length !== 0) {
      obj.address = base64FromBytes(message.address);
    }
    if (message.isSigner !== false) {
      obj.isSigner = message.isSigner;
    }
    if (message.isWritable !== false) {
      obj.isWritable = message.isWritable;
    }
    return obj;
  },

  create(base?: DeepPartial<SvmAccountMeta>): SvmAccountMeta {
    return SvmAccountMeta.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SvmAccountMeta>): SvmAccountMeta {
    const message = createBaseSvmAccountMeta();
    message.address = object.address ?? new Uint8Array(0);
    message.isSigner = object.isSigner ?? false;
    message.isWritable = object.isWritable ?? false;
    return message;
  },
};

function createBaseMsgExecuteProgram(): MsgExecuteProgram {
  return { sender: "", programId: new Uint8Array(0), accounts: [], data: new Uint8Array(0) };
}

export const MsgExecuteProgram: MessageFns<MsgExecuteProgram> = {
  encode(message: MsgExecuteProgram, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.programId.length !== 0) {
      writer.uint32(18).bytes(message.programId);
    }
    for (const v of message.accounts) {
      SvmAccountMeta.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.data.length !== 0) {
      writer.uint32(34).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgExecuteProgram {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgExecuteProgram();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.programId = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.accounts.push(SvmAccountMeta.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgExecuteProgram {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      programId: isSet(object.programId)
        ? bytesFromBase64(object.programId)
        : isSet(object.program_id)
        ? bytesFromBase64(object.program_id)
        : new Uint8Array(0),
      accounts: globalThis.Array.isArray(object?.accounts)
        ? object.accounts.map((e: any) => SvmAccountMeta.fromJSON(e))
        : [],
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
    };
  },

  toJSON(message: MsgExecuteProgram): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.programId.length !== 0) {
      obj.programId = base64FromBytes(message.programId);
    }
    if (message.accounts?.length) {
      obj.accounts = message.accounts.map((e) => SvmAccountMeta.toJSON(e));
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgExecuteProgram>): MsgExecuteProgram {
    return MsgExecuteProgram.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgExecuteProgram>): MsgExecuteProgram {
    const message = createBaseMsgExecuteProgram();
    message.sender = object.sender ?? "";
    message.programId = object.programId ?? new Uint8Array(0);
    message.accounts = object.accounts?.map((e) => SvmAccountMeta.fromPartial(e)) || [];
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgExecuteProgramResponse(): MsgExecuteProgramResponse {
  return { result: new Uint8Array(0) };
}

export const MsgExecuteProgramResponse: MessageFns<MsgExecuteProgramResponse> = {
  encode(message: MsgExecuteProgramResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.result.length !== 0) {
      writer.uint32(10).bytes(message.result);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgExecuteProgramResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgExecuteProgramResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.result = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgExecuteProgramResponse {
    return { result: isSet(object.result) ? bytesFromBase64(object.result) : new Uint8Array(0) };
  },

  toJSON(message: MsgExecuteProgramResponse): unknown {
    const obj: any = {};
    if (message.result.length !== 0) {
      obj.result = base64FromBytes(message.result);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgExecuteProgramResponse>): MsgExecuteProgramResponse {
    return MsgExecuteProgramResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgExecuteProgramResponse>): MsgExecuteProgramResponse {
    const message = createBaseMsgExecuteProgramResponse();
    message.result = object.result ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgRegisterSVMPQCKey(): MsgRegisterSVMPQCKey {
  return { sender: "", svmAddr: new Uint8Array(0), pqcPubKey: new Uint8Array(0) };
}

export const MsgRegisterSVMPQCKey: MessageFns<MsgRegisterSVMPQCKey> = {
  encode(message: MsgRegisterSVMPQCKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.svmAddr.length !== 0) {
      writer.uint32(18).bytes(message.svmAddr);
    }
    if (message.pqcPubKey.length !== 0) {
      writer.uint32(26).bytes(message.pqcPubKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterSVMPQCKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterSVMPQCKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.svmAddr = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pqcPubKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterSVMPQCKey {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      svmAddr: isSet(object.svmAddr)
        ? bytesFromBase64(object.svmAddr)
        : isSet(object.svm_addr)
        ? bytesFromBase64(object.svm_addr)
        : new Uint8Array(0),
      pqcPubKey: isSet(object.pqcPubKey)
        ? bytesFromBase64(object.pqcPubKey)
        : isSet(object.pqc_pub_key)
        ? bytesFromBase64(object.pqc_pub_key)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgRegisterSVMPQCKey): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.svmAddr.length !== 0) {
      obj.svmAddr = base64FromBytes(message.svmAddr);
    }
    if (message.pqcPubKey.length !== 0) {
      obj.pqcPubKey = base64FromBytes(message.pqcPubKey);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterSVMPQCKey>): MsgRegisterSVMPQCKey {
    return MsgRegisterSVMPQCKey.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterSVMPQCKey>): MsgRegisterSVMPQCKey {
    const message = createBaseMsgRegisterSVMPQCKey();
    message.sender = object.sender ?? "";
    message.svmAddr = object.svmAddr ?? new Uint8Array(0);
    message.pqcPubKey = object.pqcPubKey ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgRegisterSVMPQCKeyResponse(): MsgRegisterSVMPQCKeyResponse {
  return {};
}

export const MsgRegisterSVMPQCKeyResponse: MessageFns<MsgRegisterSVMPQCKeyResponse> = {
  encode(_: MsgRegisterSVMPQCKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterSVMPQCKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterSVMPQCKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRegisterSVMPQCKeyResponse {
    return {};
  },

  toJSON(_: MsgRegisterSVMPQCKeyResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterSVMPQCKeyResponse>): MsgRegisterSVMPQCKeyResponse {
    return MsgRegisterSVMPQCKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgRegisterSVMPQCKeyResponse>): MsgRegisterSVMPQCKeyResponse {
    const message = createBaseMsgRegisterSVMPQCKeyResponse();
    return message;
  },
};

export type MsgDefinition = typeof MsgDefinition;
export const MsgDefinition = {
  name: "Msg",
  fullName: "qorechain.svm.v1.Msg",
  methods: {
    deployProgram: {
      name: "DeployProgram",
      requestType: MsgDeployProgram as typeof MsgDeployProgram,
      requestStream: false,
      responseType: MsgDeployProgramResponse as typeof MsgDeployProgramResponse,
      responseStream: false,
      options: {},
    },
    createAccount: {
      name: "CreateAccount",
      requestType: MsgCreateAccount as typeof MsgCreateAccount,
      requestStream: false,
      responseType: MsgCreateAccountResponse as typeof MsgCreateAccountResponse,
      responseStream: false,
      options: {},
    },
    executeProgram: {
      name: "ExecuteProgram",
      requestType: MsgExecuteProgram as typeof MsgExecuteProgram,
      requestStream: false,
      responseType: MsgExecuteProgramResponse as typeof MsgExecuteProgramResponse,
      responseStream: false,
      options: {},
    },
    registerSVMPQCKey: {
      name: "RegisterSVMPQCKey",
      requestType: MsgRegisterSVMPQCKey as typeof MsgRegisterSVMPQCKey,
      requestStream: false,
      responseType: MsgRegisterSVMPQCKeyResponse as typeof MsgRegisterSVMPQCKeyResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
