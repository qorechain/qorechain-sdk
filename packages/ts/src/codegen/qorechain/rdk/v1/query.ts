// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/rdk/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.rdk.v1";

/** ParamsView is the query-facing view of the module parameters. */
export interface ParamsView {
  maxRollups: number;
  minStakeForRollup: string;
  rollupCreationBurnRate: string;
  defaultChallengeWindow: string;
  maxDaBlobSize: string;
  blobRetentionBlocks: string;
  maxBatchesPerBlock: number;
}

/** RollupView is the query-facing view of a rollup config (scalar fields). */
export interface RollupView {
  rollupId: string;
  creator: string;
  profile: string;
  settlementMode: string;
  daBackend: string;
  blockTimeMs: string;
  maxTxPerBlock: string;
  vmType: string;
  status: string;
  stakeAmount: string;
  layerId: string;
  createdHeight: string;
}

/** BatchView is the query-facing view of a settlement batch. */
export interface BatchView {
  rollupId: string;
  batchIndex: string;
  stateRoot: string;
  prevStateRoot: string;
  txCount: string;
  dataHash: string;
  proofType: string;
  status: string;
  submittedAt: string;
  finalizedAt: string;
  withdrawalsRoot: string;
}

export interface QueryParamsRequest {
}

export interface QueryParamsResponse {
  params?: ParamsView | undefined;
}

export interface QueryRollupRequest {
  rollupId: string;
}

export interface QueryRollupResponse {
  rollup?: RollupView | undefined;
}

export interface QueryRollupsRequest {
}

export interface QueryRollupsResponse {
  rollups: RollupView[];
}

export interface QueryBatchRequest {
  rollupId: string;
  batchIndex: string;
}

export interface QueryBatchResponse {
  batch?: BatchView | undefined;
}

export interface QueryLatestBatchRequest {
  rollupId: string;
}

export interface QueryLatestBatchResponse {
  batch?: BatchView | undefined;
}

function createBaseParamsView(): ParamsView {
  return {
    maxRollups: 0,
    minStakeForRollup: "0",
    rollupCreationBurnRate: "",
    defaultChallengeWindow: "0",
    maxDaBlobSize: "0",
    blobRetentionBlocks: "0",
    maxBatchesPerBlock: 0,
  };
}

export const ParamsView: MessageFns<ParamsView> = {
  encode(message: ParamsView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxRollups !== 0) {
      writer.uint32(8).uint32(message.maxRollups);
    }
    if (message.minStakeForRollup !== "0") {
      writer.uint32(16).int64(message.minStakeForRollup);
    }
    if (message.rollupCreationBurnRate !== "") {
      writer.uint32(26).string(message.rollupCreationBurnRate);
    }
    if (message.defaultChallengeWindow !== "0") {
      writer.uint32(32).uint64(message.defaultChallengeWindow);
    }
    if (message.maxDaBlobSize !== "0") {
      writer.uint32(40).uint64(message.maxDaBlobSize);
    }
    if (message.blobRetentionBlocks !== "0") {
      writer.uint32(48).uint64(message.blobRetentionBlocks);
    }
    if (message.maxBatchesPerBlock !== 0) {
      writer.uint32(56).uint32(message.maxBatchesPerBlock);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParamsView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParamsView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxRollups = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.minStakeForRollup = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.rollupCreationBurnRate = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.defaultChallengeWindow = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxDaBlobSize = reader.uint64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.blobRetentionBlocks = reader.uint64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxBatchesPerBlock = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParamsView {
    return {
      maxRollups: isSet(object.maxRollups)
        ? globalThis.Number(object.maxRollups)
        : isSet(object.max_rollups)
        ? globalThis.Number(object.max_rollups)
        : 0,
      minStakeForRollup: isSet(object.minStakeForRollup)
        ? globalThis.String(object.minStakeForRollup)
        : isSet(object.min_stake_for_rollup)
        ? globalThis.String(object.min_stake_for_rollup)
        : "0",
      rollupCreationBurnRate: isSet(object.rollupCreationBurnRate)
        ? globalThis.String(object.rollupCreationBurnRate)
        : isSet(object.rollup_creation_burn_rate)
        ? globalThis.String(object.rollup_creation_burn_rate)
        : "",
      defaultChallengeWindow: isSet(object.defaultChallengeWindow)
        ? globalThis.String(object.defaultChallengeWindow)
        : isSet(object.default_challenge_window)
        ? globalThis.String(object.default_challenge_window)
        : "0",
      maxDaBlobSize: isSet(object.maxDaBlobSize)
        ? globalThis.String(object.maxDaBlobSize)
        : isSet(object.max_da_blob_size)
        ? globalThis.String(object.max_da_blob_size)
        : "0",
      blobRetentionBlocks: isSet(object.blobRetentionBlocks)
        ? globalThis.String(object.blobRetentionBlocks)
        : isSet(object.blob_retention_blocks)
        ? globalThis.String(object.blob_retention_blocks)
        : "0",
      maxBatchesPerBlock: isSet(object.maxBatchesPerBlock)
        ? globalThis.Number(object.maxBatchesPerBlock)
        : isSet(object.max_batches_per_block)
        ? globalThis.Number(object.max_batches_per_block)
        : 0,
    };
  },

  toJSON(message: ParamsView): unknown {
    const obj: any = {};
    if (message.maxRollups !== 0) {
      obj.maxRollups = Math.round(message.maxRollups);
    }
    if (message.minStakeForRollup !== "0") {
      obj.minStakeForRollup = message.minStakeForRollup;
    }
    if (message.rollupCreationBurnRate !== "") {
      obj.rollupCreationBurnRate = message.rollupCreationBurnRate;
    }
    if (message.defaultChallengeWindow !== "0") {
      obj.defaultChallengeWindow = message.defaultChallengeWindow;
    }
    if (message.maxDaBlobSize !== "0") {
      obj.maxDaBlobSize = message.maxDaBlobSize;
    }
    if (message.blobRetentionBlocks !== "0") {
      obj.blobRetentionBlocks = message.blobRetentionBlocks;
    }
    if (message.maxBatchesPerBlock !== 0) {
      obj.maxBatchesPerBlock = Math.round(message.maxBatchesPerBlock);
    }
    return obj;
  },

  create(base?: DeepPartial<ParamsView>): ParamsView {
    return ParamsView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ParamsView>): ParamsView {
    const message = createBaseParamsView();
    message.maxRollups = object.maxRollups ?? 0;
    message.minStakeForRollup = object.minStakeForRollup ?? "0";
    message.rollupCreationBurnRate = object.rollupCreationBurnRate ?? "";
    message.defaultChallengeWindow = object.defaultChallengeWindow ?? "0";
    message.maxDaBlobSize = object.maxDaBlobSize ?? "0";
    message.blobRetentionBlocks = object.blobRetentionBlocks ?? "0";
    message.maxBatchesPerBlock = object.maxBatchesPerBlock ?? 0;
    return message;
  },
};

function createBaseRollupView(): RollupView {
  return {
    rollupId: "",
    creator: "",
    profile: "",
    settlementMode: "",
    daBackend: "",
    blockTimeMs: "0",
    maxTxPerBlock: "0",
    vmType: "",
    status: "",
    stakeAmount: "0",
    layerId: "",
    createdHeight: "0",
  };
}

export const RollupView: MessageFns<RollupView> = {
  encode(message: RollupView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollupId !== "") {
      writer.uint32(10).string(message.rollupId);
    }
    if (message.creator !== "") {
      writer.uint32(18).string(message.creator);
    }
    if (message.profile !== "") {
      writer.uint32(26).string(message.profile);
    }
    if (message.settlementMode !== "") {
      writer.uint32(34).string(message.settlementMode);
    }
    if (message.daBackend !== "") {
      writer.uint32(42).string(message.daBackend);
    }
    if (message.blockTimeMs !== "0") {
      writer.uint32(48).uint64(message.blockTimeMs);
    }
    if (message.maxTxPerBlock !== "0") {
      writer.uint32(56).uint64(message.maxTxPerBlock);
    }
    if (message.vmType !== "") {
      writer.uint32(66).string(message.vmType);
    }
    if (message.status !== "") {
      writer.uint32(74).string(message.status);
    }
    if (message.stakeAmount !== "0") {
      writer.uint32(80).int64(message.stakeAmount);
    }
    if (message.layerId !== "") {
      writer.uint32(90).string(message.layerId);
    }
    if (message.createdHeight !== "0") {
      writer.uint32(96).int64(message.createdHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RollupView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRollupView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rollupId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.profile = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.settlementMode = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.daBackend = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.blockTimeMs = reader.uint64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxTxPerBlock = reader.uint64().toString();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.vmType = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.stakeAmount = reader.int64().toString();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.layerId = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.createdHeight = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RollupView {
    return {
      rollupId: isSet(object.rollupId)
        ? globalThis.String(object.rollupId)
        : isSet(object.rollup_id)
        ? globalThis.String(object.rollup_id)
        : "",
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      profile: isSet(object.profile) ? globalThis.String(object.profile) : "",
      settlementMode: isSet(object.settlementMode)
        ? globalThis.String(object.settlementMode)
        : isSet(object.settlement_mode)
        ? globalThis.String(object.settlement_mode)
        : "",
      daBackend: isSet(object.daBackend)
        ? globalThis.String(object.daBackend)
        : isSet(object.da_backend)
        ? globalThis.String(object.da_backend)
        : "",
      blockTimeMs: isSet(object.blockTimeMs)
        ? globalThis.String(object.blockTimeMs)
        : isSet(object.block_time_ms)
        ? globalThis.String(object.block_time_ms)
        : "0",
      maxTxPerBlock: isSet(object.maxTxPerBlock)
        ? globalThis.String(object.maxTxPerBlock)
        : isSet(object.max_tx_per_block)
        ? globalThis.String(object.max_tx_per_block)
        : "0",
      vmType: isSet(object.vmType)
        ? globalThis.String(object.vmType)
        : isSet(object.vm_type)
        ? globalThis.String(object.vm_type)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      stakeAmount: isSet(object.stakeAmount)
        ? globalThis.String(object.stakeAmount)
        : isSet(object.stake_amount)
        ? globalThis.String(object.stake_amount)
        : "0",
      layerId: isSet(object.layerId)
        ? globalThis.String(object.layerId)
        : isSet(object.layer_id)
        ? globalThis.String(object.layer_id)
        : "",
      createdHeight: isSet(object.createdHeight)
        ? globalThis.String(object.createdHeight)
        : isSet(object.created_height)
        ? globalThis.String(object.created_height)
        : "0",
    };
  },

  toJSON(message: RollupView): unknown {
    const obj: any = {};
    if (message.rollupId !== "") {
      obj.rollupId = message.rollupId;
    }
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.profile !== "") {
      obj.profile = message.profile;
    }
    if (message.settlementMode !== "") {
      obj.settlementMode = message.settlementMode;
    }
    if (message.daBackend !== "") {
      obj.daBackend = message.daBackend;
    }
    if (message.blockTimeMs !== "0") {
      obj.blockTimeMs = message.blockTimeMs;
    }
    if (message.maxTxPerBlock !== "0") {
      obj.maxTxPerBlock = message.maxTxPerBlock;
    }
    if (message.vmType !== "") {
      obj.vmType = message.vmType;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.stakeAmount !== "0") {
      obj.stakeAmount = message.stakeAmount;
    }
    if (message.layerId !== "") {
      obj.layerId = message.layerId;
    }
    if (message.createdHeight !== "0") {
      obj.createdHeight = message.createdHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<RollupView>): RollupView {
    return RollupView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RollupView>): RollupView {
    const message = createBaseRollupView();
    message.rollupId = object.rollupId ?? "";
    message.creator = object.creator ?? "";
    message.profile = object.profile ?? "";
    message.settlementMode = object.settlementMode ?? "";
    message.daBackend = object.daBackend ?? "";
    message.blockTimeMs = object.blockTimeMs ?? "0";
    message.maxTxPerBlock = object.maxTxPerBlock ?? "0";
    message.vmType = object.vmType ?? "";
    message.status = object.status ?? "";
    message.stakeAmount = object.stakeAmount ?? "0";
    message.layerId = object.layerId ?? "";
    message.createdHeight = object.createdHeight ?? "0";
    return message;
  },
};

function createBaseBatchView(): BatchView {
  return {
    rollupId: "",
    batchIndex: "0",
    stateRoot: "",
    prevStateRoot: "",
    txCount: "0",
    dataHash: "",
    proofType: "",
    status: "",
    submittedAt: "0",
    finalizedAt: "0",
    withdrawalsRoot: "",
  };
}

export const BatchView: MessageFns<BatchView> = {
  encode(message: BatchView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollupId !== "") {
      writer.uint32(10).string(message.rollupId);
    }
    if (message.batchIndex !== "0") {
      writer.uint32(16).uint64(message.batchIndex);
    }
    if (message.stateRoot !== "") {
      writer.uint32(26).string(message.stateRoot);
    }
    if (message.prevStateRoot !== "") {
      writer.uint32(34).string(message.prevStateRoot);
    }
    if (message.txCount !== "0") {
      writer.uint32(40).uint64(message.txCount);
    }
    if (message.dataHash !== "") {
      writer.uint32(50).string(message.dataHash);
    }
    if (message.proofType !== "") {
      writer.uint32(58).string(message.proofType);
    }
    if (message.status !== "") {
      writer.uint32(66).string(message.status);
    }
    if (message.submittedAt !== "0") {
      writer.uint32(72).int64(message.submittedAt);
    }
    if (message.finalizedAt !== "0") {
      writer.uint32(80).int64(message.finalizedAt);
    }
    if (message.withdrawalsRoot !== "") {
      writer.uint32(90).string(message.withdrawalsRoot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rollupId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.batchIndex = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.stateRoot = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.prevStateRoot = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.txCount = reader.uint64().toString();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.dataHash = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.proofType = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.submittedAt = reader.int64().toString();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.finalizedAt = reader.int64().toString();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.withdrawalsRoot = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchView {
    return {
      rollupId: isSet(object.rollupId)
        ? globalThis.String(object.rollupId)
        : isSet(object.rollup_id)
        ? globalThis.String(object.rollup_id)
        : "",
      batchIndex: isSet(object.batchIndex)
        ? globalThis.String(object.batchIndex)
        : isSet(object.batch_index)
        ? globalThis.String(object.batch_index)
        : "0",
      stateRoot: isSet(object.stateRoot)
        ? globalThis.String(object.stateRoot)
        : isSet(object.state_root)
        ? globalThis.String(object.state_root)
        : "",
      prevStateRoot: isSet(object.prevStateRoot)
        ? globalThis.String(object.prevStateRoot)
        : isSet(object.prev_state_root)
        ? globalThis.String(object.prev_state_root)
        : "",
      txCount: isSet(object.txCount)
        ? globalThis.String(object.txCount)
        : isSet(object.tx_count)
        ? globalThis.String(object.tx_count)
        : "0",
      dataHash: isSet(object.dataHash)
        ? globalThis.String(object.dataHash)
        : isSet(object.data_hash)
        ? globalThis.String(object.data_hash)
        : "",
      proofType: isSet(object.proofType)
        ? globalThis.String(object.proofType)
        : isSet(object.proof_type)
        ? globalThis.String(object.proof_type)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      submittedAt: isSet(object.submittedAt)
        ? globalThis.String(object.submittedAt)
        : isSet(object.submitted_at)
        ? globalThis.String(object.submitted_at)
        : "0",
      finalizedAt: isSet(object.finalizedAt)
        ? globalThis.String(object.finalizedAt)
        : isSet(object.finalized_at)
        ? globalThis.String(object.finalized_at)
        : "0",
      withdrawalsRoot: isSet(object.withdrawalsRoot)
        ? globalThis.String(object.withdrawalsRoot)
        : isSet(object.withdrawals_root)
        ? globalThis.String(object.withdrawals_root)
        : "",
    };
  },

  toJSON(message: BatchView): unknown {
    const obj: any = {};
    if (message.rollupId !== "") {
      obj.rollupId = message.rollupId;
    }
    if (message.batchIndex !== "0") {
      obj.batchIndex = message.batchIndex;
    }
    if (message.stateRoot !== "") {
      obj.stateRoot = message.stateRoot;
    }
    if (message.prevStateRoot !== "") {
      obj.prevStateRoot = message.prevStateRoot;
    }
    if (message.txCount !== "0") {
      obj.txCount = message.txCount;
    }
    if (message.dataHash !== "") {
      obj.dataHash = message.dataHash;
    }
    if (message.proofType !== "") {
      obj.proofType = message.proofType;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.submittedAt !== "0") {
      obj.submittedAt = message.submittedAt;
    }
    if (message.finalizedAt !== "0") {
      obj.finalizedAt = message.finalizedAt;
    }
    if (message.withdrawalsRoot !== "") {
      obj.withdrawalsRoot = message.withdrawalsRoot;
    }
    return obj;
  },

  create(base?: DeepPartial<BatchView>): BatchView {
    return BatchView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BatchView>): BatchView {
    const message = createBaseBatchView();
    message.rollupId = object.rollupId ?? "";
    message.batchIndex = object.batchIndex ?? "0";
    message.stateRoot = object.stateRoot ?? "";
    message.prevStateRoot = object.prevStateRoot ?? "";
    message.txCount = object.txCount ?? "0";
    message.dataHash = object.dataHash ?? "";
    message.proofType = object.proofType ?? "";
    message.status = object.status ?? "";
    message.submittedAt = object.submittedAt ?? "0";
    message.finalizedAt = object.finalizedAt ?? "0";
    message.withdrawalsRoot = object.withdrawalsRoot ?? "";
    return message;
  },
};

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      ParamsView.encode(message.params, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = ParamsView.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? ParamsView.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = ParamsView.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? ParamsView.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryRollupRequest(): QueryRollupRequest {
  return { rollupId: "" };
}

export const QueryRollupRequest: MessageFns<QueryRollupRequest> = {
  encode(message: QueryRollupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollupId !== "") {
      writer.uint32(10).string(message.rollupId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRollupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRollupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rollupId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRollupRequest {
    return {
      rollupId: isSet(object.rollupId)
        ? globalThis.String(object.rollupId)
        : isSet(object.rollup_id)
        ? globalThis.String(object.rollup_id)
        : "",
    };
  },

  toJSON(message: QueryRollupRequest): unknown {
    const obj: any = {};
    if (message.rollupId !== "") {
      obj.rollupId = message.rollupId;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRollupRequest>): QueryRollupRequest {
    return QueryRollupRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRollupRequest>): QueryRollupRequest {
    const message = createBaseQueryRollupRequest();
    message.rollupId = object.rollupId ?? "";
    return message;
  },
};

function createBaseQueryRollupResponse(): QueryRollupResponse {
  return { rollup: undefined };
}

export const QueryRollupResponse: MessageFns<QueryRollupResponse> = {
  encode(message: QueryRollupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollup !== undefined) {
      RollupView.encode(message.rollup, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRollupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRollupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rollup = RollupView.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRollupResponse {
    return { rollup: isSet(object.rollup) ? RollupView.fromJSON(object.rollup) : undefined };
  },

  toJSON(message: QueryRollupResponse): unknown {
    const obj: any = {};
    if (message.rollup !== undefined) {
      obj.rollup = RollupView.toJSON(message.rollup);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRollupResponse>): QueryRollupResponse {
    return QueryRollupResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRollupResponse>): QueryRollupResponse {
    const message = createBaseQueryRollupResponse();
    message.rollup = (object.rollup !== undefined && object.rollup !== null)
      ? RollupView.fromPartial(object.rollup)
      : undefined;
    return message;
  },
};

function createBaseQueryRollupsRequest(): QueryRollupsRequest {
  return {};
}

export const QueryRollupsRequest: MessageFns<QueryRollupsRequest> = {
  encode(_: QueryRollupsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRollupsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRollupsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryRollupsRequest {
    return {};
  },

  toJSON(_: QueryRollupsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryRollupsRequest>): QueryRollupsRequest {
    return QueryRollupsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryRollupsRequest>): QueryRollupsRequest {
    const message = createBaseQueryRollupsRequest();
    return message;
  },
};

function createBaseQueryRollupsResponse(): QueryRollupsResponse {
  return { rollups: [] };
}

export const QueryRollupsResponse: MessageFns<QueryRollupsResponse> = {
  encode(message: QueryRollupsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.rollups) {
      RollupView.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRollupsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRollupsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rollups.push(RollupView.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRollupsResponse {
    return {
      rollups: globalThis.Array.isArray(object?.rollups) ? object.rollups.map((e: any) => RollupView.fromJSON(e)) : [],
    };
  },

  toJSON(message: QueryRollupsResponse): unknown {
    const obj: any = {};
    if (message.rollups?.length) {
      obj.rollups = message.rollups.map((e) => RollupView.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRollupsResponse>): QueryRollupsResponse {
    return QueryRollupsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRollupsResponse>): QueryRollupsResponse {
    const message = createBaseQueryRollupsResponse();
    message.rollups = object.rollups?.map((e) => RollupView.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryBatchRequest(): QueryBatchRequest {
  return { rollupId: "", batchIndex: "0" };
}

export const QueryBatchRequest: MessageFns<QueryBatchRequest> = {
  encode(message: QueryBatchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollupId !== "") {
      writer.uint32(10).string(message.rollupId);
    }
    if (message.batchIndex !== "0") {
      writer.uint32(16).uint64(message.batchIndex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryBatchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryBatchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rollupId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.batchIndex = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryBatchRequest {
    return {
      rollupId: isSet(object.rollupId)
        ? globalThis.String(object.rollupId)
        : isSet(object.rollup_id)
        ? globalThis.String(object.rollup_id)
        : "",
      batchIndex: isSet(object.batchIndex)
        ? globalThis.String(object.batchIndex)
        : isSet(object.batch_index)
        ? globalThis.String(object.batch_index)
        : "0",
    };
  },

  toJSON(message: QueryBatchRequest): unknown {
    const obj: any = {};
    if (message.rollupId !== "") {
      obj.rollupId = message.rollupId;
    }
    if (message.batchIndex !== "0") {
      obj.batchIndex = message.batchIndex;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryBatchRequest>): QueryBatchRequest {
    return QueryBatchRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryBatchRequest>): QueryBatchRequest {
    const message = createBaseQueryBatchRequest();
    message.rollupId = object.rollupId ?? "";
    message.batchIndex = object.batchIndex ?? "0";
    return message;
  },
};

function createBaseQueryBatchResponse(): QueryBatchResponse {
  return { batch: undefined };
}

export const QueryBatchResponse: MessageFns<QueryBatchResponse> = {
  encode(message: QueryBatchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.batch !== undefined) {
      BatchView.encode(message.batch, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryBatchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryBatchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.batch = BatchView.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryBatchResponse {
    return { batch: isSet(object.batch) ? BatchView.fromJSON(object.batch) : undefined };
  },

  toJSON(message: QueryBatchResponse): unknown {
    const obj: any = {};
    if (message.batch !== undefined) {
      obj.batch = BatchView.toJSON(message.batch);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryBatchResponse>): QueryBatchResponse {
    return QueryBatchResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryBatchResponse>): QueryBatchResponse {
    const message = createBaseQueryBatchResponse();
    message.batch = (object.batch !== undefined && object.batch !== null)
      ? BatchView.fromPartial(object.batch)
      : undefined;
    return message;
  },
};

function createBaseQueryLatestBatchRequest(): QueryLatestBatchRequest {
  return { rollupId: "" };
}

export const QueryLatestBatchRequest: MessageFns<QueryLatestBatchRequest> = {
  encode(message: QueryLatestBatchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollupId !== "") {
      writer.uint32(10).string(message.rollupId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryLatestBatchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryLatestBatchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rollupId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryLatestBatchRequest {
    return {
      rollupId: isSet(object.rollupId)
        ? globalThis.String(object.rollupId)
        : isSet(object.rollup_id)
        ? globalThis.String(object.rollup_id)
        : "",
    };
  },

  toJSON(message: QueryLatestBatchRequest): unknown {
    const obj: any = {};
    if (message.rollupId !== "") {
      obj.rollupId = message.rollupId;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryLatestBatchRequest>): QueryLatestBatchRequest {
    return QueryLatestBatchRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryLatestBatchRequest>): QueryLatestBatchRequest {
    const message = createBaseQueryLatestBatchRequest();
    message.rollupId = object.rollupId ?? "";
    return message;
  },
};

function createBaseQueryLatestBatchResponse(): QueryLatestBatchResponse {
  return { batch: undefined };
}

export const QueryLatestBatchResponse: MessageFns<QueryLatestBatchResponse> = {
  encode(message: QueryLatestBatchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.batch !== undefined) {
      BatchView.encode(message.batch, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryLatestBatchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryLatestBatchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.batch = BatchView.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryLatestBatchResponse {
    return { batch: isSet(object.batch) ? BatchView.fromJSON(object.batch) : undefined };
  },

  toJSON(message: QueryLatestBatchResponse): unknown {
    const obj: any = {};
    if (message.batch !== undefined) {
      obj.batch = BatchView.toJSON(message.batch);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryLatestBatchResponse>): QueryLatestBatchResponse {
    return QueryLatestBatchResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryLatestBatchResponse>): QueryLatestBatchResponse {
    const message = createBaseQueryLatestBatchResponse();
    message.batch = (object.batch !== undefined && object.batch !== null)
      ? BatchView.fromPartial(object.batch)
      : undefined;
    return message;
  },
};

/** Query defines the gRPC query service for the rdk (rollup) module. */
export type QueryDefinition = typeof QueryDefinition;
export const QueryDefinition = {
  name: "Query",
  fullName: "qorechain.rdk.v1.Query",
  methods: {
    /** Params returns the module parameters. */
    params: {
      name: "Params",
      requestType: QueryParamsRequest as typeof QueryParamsRequest,
      requestStream: false,
      responseType: QueryParamsResponse as typeof QueryParamsResponse,
      responseStream: false,
      options: {},
    },
    /** Rollup returns a single rollup config by ID. */
    rollup: {
      name: "Rollup",
      requestType: QueryRollupRequest as typeof QueryRollupRequest,
      requestStream: false,
      responseType: QueryRollupResponse as typeof QueryRollupResponse,
      responseStream: false,
      options: {},
    },
    /** Rollups lists all rollups. */
    rollups: {
      name: "Rollups",
      requestType: QueryRollupsRequest as typeof QueryRollupsRequest,
      requestStream: false,
      responseType: QueryRollupsResponse as typeof QueryRollupsResponse,
      responseStream: false,
      options: {},
    },
    /** Batch returns a settlement batch by (rollup_id, batch_index). */
    batch: {
      name: "Batch",
      requestType: QueryBatchRequest as typeof QueryBatchRequest,
      requestStream: false,
      responseType: QueryBatchResponse as typeof QueryBatchResponse,
      responseStream: false,
      options: {},
    },
    /** LatestBatch returns the latest settlement batch for a rollup. */
    latestBatch: {
      name: "LatestBatch",
      requestType: QueryLatestBatchRequest as typeof QueryLatestBatchRequest,
      requestStream: false,
      responseType: QueryLatestBatchResponse as typeof QueryLatestBatchResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
