// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/crossvm/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.crossvm.v1";

export interface CrossVMMessageView {
  id: string;
  sourceVm: string;
  targetVm: string;
  sender: string;
  sourceContract: string;
  targetContract: string;
  payloadHex: string;
  status: string;
  createdHeight: string;
  executedHeight: string;
  error: string;
}

export interface QueryParamsRequest {
}

export interface QueryParamsResponse {
  maxMessageSize: string;
  maxQueueSize: number;
  queueTimeoutBlocks: string;
  enabled: boolean;
}

export interface QueryPendingMessagesRequest {
}

export interface QueryPendingMessagesResponse {
  messages: CrossVMMessageView[];
  count: number;
}

export interface QueryMessageRequest {
  id: string;
}

export interface QueryMessageResponse {
  message?: CrossVMMessageView | undefined;
  found: boolean;
}

function createBaseCrossVMMessageView(): CrossVMMessageView {
  return {
    id: "",
    sourceVm: "",
    targetVm: "",
    sender: "",
    sourceContract: "",
    targetContract: "",
    payloadHex: "",
    status: "",
    createdHeight: "0",
    executedHeight: "0",
    error: "",
  };
}

export const CrossVMMessageView: MessageFns<CrossVMMessageView> = {
  encode(message: CrossVMMessageView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.sourceVm !== "") {
      writer.uint32(18).string(message.sourceVm);
    }
    if (message.targetVm !== "") {
      writer.uint32(26).string(message.targetVm);
    }
    if (message.sender !== "") {
      writer.uint32(34).string(message.sender);
    }
    if (message.sourceContract !== "") {
      writer.uint32(42).string(message.sourceContract);
    }
    if (message.targetContract !== "") {
      writer.uint32(50).string(message.targetContract);
    }
    if (message.payloadHex !== "") {
      writer.uint32(58).string(message.payloadHex);
    }
    if (message.status !== "") {
      writer.uint32(66).string(message.status);
    }
    if (message.createdHeight !== "0") {
      writer.uint32(72).int64(message.createdHeight);
    }
    if (message.executedHeight !== "0") {
      writer.uint32(80).int64(message.executedHeight);
    }
    if (message.error !== "") {
      writer.uint32(90).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CrossVMMessageView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCrossVMMessageView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourceVm = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.targetVm = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.sourceContract = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.targetContract = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.payloadHex = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.createdHeight = reader.int64().toString();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.executedHeight = reader.int64().toString();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CrossVMMessageView {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      sourceVm: isSet(object.sourceVm)
        ? globalThis.String(object.sourceVm)
        : isSet(object.source_vm)
        ? globalThis.String(object.source_vm)
        : "",
      targetVm: isSet(object.targetVm)
        ? globalThis.String(object.targetVm)
        : isSet(object.target_vm)
        ? globalThis.String(object.target_vm)
        : "",
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      sourceContract: isSet(object.sourceContract)
        ? globalThis.String(object.sourceContract)
        : isSet(object.source_contract)
        ? globalThis.String(object.source_contract)
        : "",
      targetContract: isSet(object.targetContract)
        ? globalThis.String(object.targetContract)
        : isSet(object.target_contract)
        ? globalThis.String(object.target_contract)
        : "",
      payloadHex: isSet(object.payloadHex)
        ? globalThis.String(object.payloadHex)
        : isSet(object.payload_hex)
        ? globalThis.String(object.payload_hex)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      createdHeight: isSet(object.createdHeight)
        ? globalThis.String(object.createdHeight)
        : isSet(object.created_height)
        ? globalThis.String(object.created_height)
        : "0",
      executedHeight: isSet(object.executedHeight)
        ? globalThis.String(object.executedHeight)
        : isSet(object.executed_height)
        ? globalThis.String(object.executed_height)
        : "0",
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: CrossVMMessageView): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.sourceVm !== "") {
      obj.sourceVm = message.sourceVm;
    }
    if (message.targetVm !== "") {
      obj.targetVm = message.targetVm;
    }
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.sourceContract !== "") {
      obj.sourceContract = message.sourceContract;
    }
    if (message.targetContract !== "") {
      obj.targetContract = message.targetContract;
    }
    if (message.payloadHex !== "") {
      obj.payloadHex = message.payloadHex;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.createdHeight !== "0") {
      obj.createdHeight = message.createdHeight;
    }
    if (message.executedHeight !== "0") {
      obj.executedHeight = message.executedHeight;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<CrossVMMessageView>): CrossVMMessageView {
    return CrossVMMessageView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CrossVMMessageView>): CrossVMMessageView {
    const message = createBaseCrossVMMessageView();
    message.id = object.id ?? "";
    message.sourceVm = object.sourceVm ?? "";
    message.targetVm = object.targetVm ?? "";
    message.sender = object.sender ?? "";
    message.sourceContract = object.sourceContract ?? "";
    message.targetContract = object.targetContract ?? "";
    message.payloadHex = object.payloadHex ?? "";
    message.status = object.status ?? "";
    message.createdHeight = object.createdHeight ?? "0";
    message.executedHeight = object.executedHeight ?? "0";
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { maxMessageSize: "0", maxQueueSize: 0, queueTimeoutBlocks: "0", enabled: false };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxMessageSize !== "0") {
      writer.uint32(8).uint64(message.maxMessageSize);
    }
    if (message.maxQueueSize !== 0) {
      writer.uint32(16).uint32(message.maxQueueSize);
    }
    if (message.queueTimeoutBlocks !== "0") {
      writer.uint32(24).int64(message.queueTimeoutBlocks);
    }
    if (message.enabled !== false) {
      writer.uint32(32).bool(message.enabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxMessageSize = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxQueueSize = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.queueTimeoutBlocks = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return {
      maxMessageSize: isSet(object.maxMessageSize)
        ? globalThis.String(object.maxMessageSize)
        : isSet(object.max_message_size)
        ? globalThis.String(object.max_message_size)
        : "0",
      maxQueueSize: isSet(object.maxQueueSize)
        ? globalThis.Number(object.maxQueueSize)
        : isSet(object.max_queue_size)
        ? globalThis.Number(object.max_queue_size)
        : 0,
      queueTimeoutBlocks: isSet(object.queueTimeoutBlocks)
        ? globalThis.String(object.queueTimeoutBlocks)
        : isSet(object.queue_timeout_blocks)
        ? globalThis.String(object.queue_timeout_blocks)
        : "0",
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
    };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.maxMessageSize !== "0") {
      obj.maxMessageSize = message.maxMessageSize;
    }
    if (message.maxQueueSize !== 0) {
      obj.maxQueueSize = Math.round(message.maxQueueSize);
    }
    if (message.queueTimeoutBlocks !== "0") {
      obj.queueTimeoutBlocks = message.queueTimeoutBlocks;
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.maxMessageSize = object.maxMessageSize ?? "0";
    message.maxQueueSize = object.maxQueueSize ?? 0;
    message.queueTimeoutBlocks = object.queueTimeoutBlocks ?? "0";
    message.enabled = object.enabled ?? false;
    return message;
  },
};

function createBaseQueryPendingMessagesRequest(): QueryPendingMessagesRequest {
  return {};
}

export const QueryPendingMessagesRequest: MessageFns<QueryPendingMessagesRequest> = {
  encode(_: QueryPendingMessagesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryPendingMessagesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryPendingMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryPendingMessagesRequest {
    return {};
  },

  toJSON(_: QueryPendingMessagesRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryPendingMessagesRequest>): QueryPendingMessagesRequest {
    return QueryPendingMessagesRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryPendingMessagesRequest>): QueryPendingMessagesRequest {
    const message = createBaseQueryPendingMessagesRequest();
    return message;
  },
};

function createBaseQueryPendingMessagesResponse(): QueryPendingMessagesResponse {
  return { messages: [], count: 0 };
}

export const QueryPendingMessagesResponse: MessageFns<QueryPendingMessagesResponse> = {
  encode(message: QueryPendingMessagesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messages) {
      CrossVMMessageView.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.count !== 0) {
      writer.uint32(16).uint32(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryPendingMessagesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryPendingMessagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messages.push(CrossVMMessageView.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryPendingMessagesResponse {
    return {
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => CrossVMMessageView.fromJSON(e))
        : [],
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: QueryPendingMessagesResponse): unknown {
    const obj: any = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => CrossVMMessageView.toJSON(e));
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryPendingMessagesResponse>): QueryPendingMessagesResponse {
    return QueryPendingMessagesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryPendingMessagesResponse>): QueryPendingMessagesResponse {
    const message = createBaseQueryPendingMessagesResponse();
    message.messages = object.messages?.map((e) => CrossVMMessageView.fromPartial(e)) || [];
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseQueryMessageRequest(): QueryMessageRequest {
  return { id: "" };
}

export const QueryMessageRequest: MessageFns<QueryMessageRequest> = {
  encode(message: QueryMessageRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMessageRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryMessageRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: QueryMessageRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryMessageRequest>): QueryMessageRequest {
    return QueryMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryMessageRequest>): QueryMessageRequest {
    const message = createBaseQueryMessageRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseQueryMessageResponse(): QueryMessageResponse {
  return { message: undefined, found: false };
}

export const QueryMessageResponse: MessageFns<QueryMessageResponse> = {
  encode(message: QueryMessageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== undefined) {
      CrossVMMessageView.encode(message.message, writer.uint32(10).fork()).join();
    }
    if (message.found !== false) {
      writer.uint32(16).bool(message.found);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMessageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMessageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = CrossVMMessageView.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryMessageResponse {
    return {
      message: isSet(object.message) ? CrossVMMessageView.fromJSON(object.message) : undefined,
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
    };
  },

  toJSON(message: QueryMessageResponse): unknown {
    const obj: any = {};
    if (message.message !== undefined) {
      obj.message = CrossVMMessageView.toJSON(message.message);
    }
    if (message.found !== false) {
      obj.found = message.found;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryMessageResponse>): QueryMessageResponse {
    return QueryMessageResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryMessageResponse>): QueryMessageResponse {
    const message = createBaseQueryMessageResponse();
    message.message = (object.message !== undefined && object.message !== null)
      ? CrossVMMessageView.fromPartial(object.message)
      : undefined;
    message.found = object.found ?? false;
    return message;
  },
};

/** Query defines the crossvm module's gRPC query service. */
export type QueryDefinition = typeof QueryDefinition;
export const QueryDefinition = {
  name: "Query",
  fullName: "qorechain.crossvm.v1.Query",
  methods: {
    /** Params returns the crossvm module parameters. */
    params: {
      name: "Params",
      requestType: QueryParamsRequest as typeof QueryParamsRequest,
      requestStream: false,
      responseType: QueryParamsResponse as typeof QueryParamsResponse,
      responseStream: false,
      options: {},
    },
    /** PendingMessages returns the current cross-VM message queue. */
    pendingMessages: {
      name: "PendingMessages",
      requestType: QueryPendingMessagesRequest as typeof QueryPendingMessagesRequest,
      requestStream: false,
      responseType: QueryPendingMessagesResponse as typeof QueryPendingMessagesResponse,
      responseStream: false,
      options: {},
    },
    /** Message returns a single cross-VM message by id. */
    message: {
      name: "Message",
      requestType: QueryMessageRequest as typeof QueryMessageRequest,
      requestStream: false,
      responseType: QueryMessageResponse as typeof QueryMessageResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
