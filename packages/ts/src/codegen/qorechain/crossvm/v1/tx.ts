// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/crossvm/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Coin } from "../../../cosmos/base/v1beta1/coin";

export const protobufPackage = "qorechain.crossvm.v1";

export interface MsgCrossVMCall {
  sender: string;
  /**
   * source_vm is IGNORED on input. The chain derives the origin lane from the
   * execution context, because a caller describing itself cannot be evidence of
   * what it is. Retained so the field number stays taken and older clients that
   * still set it are accepted rather than rejected.
   */
  sourceVm: string;
  targetVm: string;
  targetContract: string;
  payload: Uint8Array;
  funds: Coin[];
  /**
   * async queues the call instead of executing it, to be dispatched later by
   * ProcessQueue. The default is to execute now and return the answer, which is
   * what a caller that needs the result requires.
   */
  async: boolean;
}

export interface MsgCrossVMCallResponse {
  messageId: string;
  /** executed is false for a queued call, whose result is not known yet. */
  executed: boolean;
  /**
   * data is the callee's return value, carried back to the caller. A CosmWasm
   * contract reads it from the submessage reply.
   */
  data: Uint8Array;
  gasUsed: string;
}

export interface MsgProcessQueue {
  authority: string;
}

export interface MsgProcessQueueResponse {
}

function createBaseMsgCrossVMCall(): MsgCrossVMCall {
  return {
    sender: "",
    sourceVm: "",
    targetVm: "",
    targetContract: "",
    payload: new Uint8Array(0),
    funds: [],
    async: false,
  };
}

export const MsgCrossVMCall: MessageFns<MsgCrossVMCall> = {
  encode(message: MsgCrossVMCall, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.sourceVm !== "") {
      writer.uint32(18).string(message.sourceVm);
    }
    if (message.targetVm !== "") {
      writer.uint32(26).string(message.targetVm);
    }
    if (message.targetContract !== "") {
      writer.uint32(34).string(message.targetContract);
    }
    if (message.payload.length !== 0) {
      writer.uint32(42).bytes(message.payload);
    }
    for (const v of message.funds) {
      Coin.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.async !== false) {
      writer.uint32(56).bool(message.async);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCrossVMCall {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCrossVMCall();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourceVm = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.targetVm = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.targetContract = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.funds.push(Coin.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.async = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCrossVMCall {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      sourceVm: isSet(object.sourceVm)
        ? globalThis.String(object.sourceVm)
        : isSet(object.source_vm)
        ? globalThis.String(object.source_vm)
        : "",
      targetVm: isSet(object.targetVm)
        ? globalThis.String(object.targetVm)
        : isSet(object.target_vm)
        ? globalThis.String(object.target_vm)
        : "",
      targetContract: isSet(object.targetContract)
        ? globalThis.String(object.targetContract)
        : isSet(object.target_contract)
        ? globalThis.String(object.target_contract)
        : "",
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      funds: globalThis.Array.isArray(object?.funds)
        ? object.funds.map((e: any) => Coin.fromJSON(e))
        : [],
      async: isSet(object.async) ? globalThis.Boolean(object.async) : false,
    };
  },

  toJSON(message: MsgCrossVMCall): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.sourceVm !== "") {
      obj.sourceVm = message.sourceVm;
    }
    if (message.targetVm !== "") {
      obj.targetVm = message.targetVm;
    }
    if (message.targetContract !== "") {
      obj.targetContract = message.targetContract;
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.funds?.length) {
      obj.funds = message.funds.map((e) => Coin.toJSON(e));
    }
    if (message.async !== false) {
      obj.async = message.async;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgCrossVMCall>): MsgCrossVMCall {
    return MsgCrossVMCall.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgCrossVMCall>): MsgCrossVMCall {
    const message = createBaseMsgCrossVMCall();
    message.sender = object.sender ?? "";
    message.sourceVm = object.sourceVm ?? "";
    message.targetVm = object.targetVm ?? "";
    message.targetContract = object.targetContract ?? "";
    message.payload = object.payload ?? new Uint8Array(0);
    message.funds = object.funds?.map((e) => Coin.fromPartial(e)) || [];
    message.async = object.async ?? false;
    return message;
  },
};

function createBaseMsgCrossVMCallResponse(): MsgCrossVMCallResponse {
  return { messageId: "", executed: false, data: new Uint8Array(0), gasUsed: "0" };
}

export const MsgCrossVMCallResponse: MessageFns<MsgCrossVMCallResponse> = {
  encode(message: MsgCrossVMCallResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.messageId !== "") {
      writer.uint32(10).string(message.messageId);
    }
    if (message.executed !== false) {
      writer.uint32(16).bool(message.executed);
    }
    if (message.data.length !== 0) {
      writer.uint32(26).bytes(message.data);
    }
    if (message.gasUsed !== "0") {
      writer.uint32(32).uint64(message.gasUsed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCrossVMCallResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCrossVMCallResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.executed = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.gasUsed = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCrossVMCallResponse {
    return {
      messageId: isSet(object.messageId)
        ? globalThis.String(object.messageId)
        : isSet(object.message_id)
        ? globalThis.String(object.message_id)
        : "",
      executed: isSet(object.executed) ? globalThis.Boolean(object.executed) : false,
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
      gasUsed: isSet(object.gasUsed)
        ? globalThis.String(object.gasUsed)
        : isSet(object.gas_used)
        ? globalThis.String(object.gas_used)
        : "0",
    };
  },

  toJSON(message: MsgCrossVMCallResponse): unknown {
    const obj: any = {};
    if (message.messageId !== "") {
      obj.messageId = message.messageId;
    }
    if (message.executed !== false) {
      obj.executed = message.executed;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.gasUsed !== "0") {
      obj.gasUsed = message.gasUsed;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgCrossVMCallResponse>): MsgCrossVMCallResponse {
    return MsgCrossVMCallResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgCrossVMCallResponse>): MsgCrossVMCallResponse {
    const message = createBaseMsgCrossVMCallResponse();
    message.messageId = object.messageId ?? "";
    message.executed = object.executed ?? false;
    message.data = object.data ?? new Uint8Array(0);
    message.gasUsed = object.gasUsed ?? "0";
    return message;
  },
};

function createBaseMsgProcessQueue(): MsgProcessQueue {
  return { authority: "" };
}

export const MsgProcessQueue: MessageFns<MsgProcessQueue> = {
  encode(message: MsgProcessQueue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgProcessQueue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgProcessQueue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgProcessQueue {
    return { authority: isSet(object.authority) ? globalThis.String(object.authority) : "" };
  },

  toJSON(message: MsgProcessQueue): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgProcessQueue>): MsgProcessQueue {
    return MsgProcessQueue.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgProcessQueue>): MsgProcessQueue {
    const message = createBaseMsgProcessQueue();
    message.authority = object.authority ?? "";
    return message;
  },
};

function createBaseMsgProcessQueueResponse(): MsgProcessQueueResponse {
  return {};
}

export const MsgProcessQueueResponse: MessageFns<MsgProcessQueueResponse> = {
  encode(_: MsgProcessQueueResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgProcessQueueResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgProcessQueueResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgProcessQueueResponse {
    return {};
  },

  toJSON(_: MsgProcessQueueResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgProcessQueueResponse>): MsgProcessQueueResponse {
    return MsgProcessQueueResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgProcessQueueResponse>): MsgProcessQueueResponse {
    const message = createBaseMsgProcessQueueResponse();
    return message;
  },
};

/** Msg defines the crossvm module's transaction service. */
export type MsgDefinition = typeof MsgDefinition;
export const MsgDefinition = {
  name: "Msg",
  fullName: "qorechain.crossvm.v1.Msg",
  methods: {
    /**
     * CrossVMCall invokes a contract on another VM. It executes within the
     * transaction and returns the callee's answer, unless async is set, in which
     * case it is queued for ProcessQueue to dispatch later.
     */
    crossVMCall: {
      name: "CrossVMCall",
      requestType: MsgCrossVMCall as typeof MsgCrossVMCall,
      requestStream: false,
      responseType: MsgCrossVMCallResponse as typeof MsgCrossVMCallResponse,
      responseStream: false,
      options: {},
    },
    /** ProcessQueue processes the pending cross-VM message queue. */
    processQueue: {
      name: "ProcessQueue",
      requestType: MsgProcessQueue as typeof MsgProcessQueue,
      requestStream: false,
      responseType: MsgProcessQueueResponse as typeof MsgProcessQueueResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
