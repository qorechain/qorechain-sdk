// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/license/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.license.v1";

/** LicenseView is the query-facing view of a license grant. */
export interface LicenseView {
  grantee: string;
  featureId: string;
  expiresAt: string;
  grantedAt: string;
  grantedBy: string;
  suspended: boolean;
  metadata: string;
}

export interface QueryCheckRequest {
  grantee: string;
  featureId: string;
}

export interface QueryCheckResponse {
  license?:
    | LicenseView
    | undefined;
  /** active is true when the grant exists and is not suspended. */
  active: boolean;
}

export interface QueryHoldersRequest {
  featureId: string;
}

export interface QueryHoldersResponse {
  licenses: LicenseView[];
}

export interface QueryListRequest {
  grantee: string;
}

export interface QueryListResponse {
  licenses: LicenseView[];
}

function createBaseLicenseView(): LicenseView {
  return { grantee: "", featureId: "", expiresAt: "0", grantedAt: "0", grantedBy: "", suspended: false, metadata: "" };
}

export const LicenseView: MessageFns<LicenseView> = {
  encode(message: LicenseView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.grantee !== "") {
      writer.uint32(10).string(message.grantee);
    }
    if (message.featureId !== "") {
      writer.uint32(18).string(message.featureId);
    }
    if (message.expiresAt !== "0") {
      writer.uint32(24).int64(message.expiresAt);
    }
    if (message.grantedAt !== "0") {
      writer.uint32(32).int64(message.grantedAt);
    }
    if (message.grantedBy !== "") {
      writer.uint32(42).string(message.grantedBy);
    }
    if (message.suspended !== false) {
      writer.uint32(48).bool(message.suspended);
    }
    if (message.metadata !== "") {
      writer.uint32(58).string(message.metadata);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LicenseView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLicenseView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.grantee = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.featureId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expiresAt = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.grantedAt = reader.int64().toString();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.grantedBy = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.suspended = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.metadata = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LicenseView {
    return {
      grantee: isSet(object.grantee) ? globalThis.String(object.grantee) : "",
      featureId: isSet(object.featureId)
        ? globalThis.String(object.featureId)
        : isSet(object.feature_id)
        ? globalThis.String(object.feature_id)
        : "",
      expiresAt: isSet(object.expiresAt)
        ? globalThis.String(object.expiresAt)
        : isSet(object.expires_at)
        ? globalThis.String(object.expires_at)
        : "0",
      grantedAt: isSet(object.grantedAt)
        ? globalThis.String(object.grantedAt)
        : isSet(object.granted_at)
        ? globalThis.String(object.granted_at)
        : "0",
      grantedBy: isSet(object.grantedBy)
        ? globalThis.String(object.grantedBy)
        : isSet(object.granted_by)
        ? globalThis.String(object.granted_by)
        : "",
      suspended: isSet(object.suspended) ? globalThis.Boolean(object.suspended) : false,
      metadata: isSet(object.metadata) ? globalThis.String(object.metadata) : "",
    };
  },

  toJSON(message: LicenseView): unknown {
    const obj: any = {};
    if (message.grantee !== "") {
      obj.grantee = message.grantee;
    }
    if (message.featureId !== "") {
      obj.featureId = message.featureId;
    }
    if (message.expiresAt !== "0") {
      obj.expiresAt = message.expiresAt;
    }
    if (message.grantedAt !== "0") {
      obj.grantedAt = message.grantedAt;
    }
    if (message.grantedBy !== "") {
      obj.grantedBy = message.grantedBy;
    }
    if (message.suspended !== false) {
      obj.suspended = message.suspended;
    }
    if (message.metadata !== "") {
      obj.metadata = message.metadata;
    }
    return obj;
  },

  create(base?: DeepPartial<LicenseView>): LicenseView {
    return LicenseView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LicenseView>): LicenseView {
    const message = createBaseLicenseView();
    message.grantee = object.grantee ?? "";
    message.featureId = object.featureId ?? "";
    message.expiresAt = object.expiresAt ?? "0";
    message.grantedAt = object.grantedAt ?? "0";
    message.grantedBy = object.grantedBy ?? "";
    message.suspended = object.suspended ?? false;
    message.metadata = object.metadata ?? "";
    return message;
  },
};

function createBaseQueryCheckRequest(): QueryCheckRequest {
  return { grantee: "", featureId: "" };
}

export const QueryCheckRequest: MessageFns<QueryCheckRequest> = {
  encode(message: QueryCheckRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.grantee !== "") {
      writer.uint32(10).string(message.grantee);
    }
    if (message.featureId !== "") {
      writer.uint32(18).string(message.featureId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryCheckRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryCheckRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.grantee = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.featureId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryCheckRequest {
    return {
      grantee: isSet(object.grantee) ? globalThis.String(object.grantee) : "",
      featureId: isSet(object.featureId)
        ? globalThis.String(object.featureId)
        : isSet(object.feature_id)
        ? globalThis.String(object.feature_id)
        : "",
    };
  },

  toJSON(message: QueryCheckRequest): unknown {
    const obj: any = {};
    if (message.grantee !== "") {
      obj.grantee = message.grantee;
    }
    if (message.featureId !== "") {
      obj.featureId = message.featureId;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryCheckRequest>): QueryCheckRequest {
    return QueryCheckRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryCheckRequest>): QueryCheckRequest {
    const message = createBaseQueryCheckRequest();
    message.grantee = object.grantee ?? "";
    message.featureId = object.featureId ?? "";
    return message;
  },
};

function createBaseQueryCheckResponse(): QueryCheckResponse {
  return { license: undefined, active: false };
}

export const QueryCheckResponse: MessageFns<QueryCheckResponse> = {
  encode(message: QueryCheckResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.license !== undefined) {
      LicenseView.encode(message.license, writer.uint32(10).fork()).join();
    }
    if (message.active !== false) {
      writer.uint32(16).bool(message.active);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryCheckResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryCheckResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.license = LicenseView.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.active = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryCheckResponse {
    return {
      license: isSet(object.license) ? LicenseView.fromJSON(object.license) : undefined,
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
    };
  },

  toJSON(message: QueryCheckResponse): unknown {
    const obj: any = {};
    if (message.license !== undefined) {
      obj.license = LicenseView.toJSON(message.license);
    }
    if (message.active !== false) {
      obj.active = message.active;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryCheckResponse>): QueryCheckResponse {
    return QueryCheckResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryCheckResponse>): QueryCheckResponse {
    const message = createBaseQueryCheckResponse();
    message.license = (object.license !== undefined && object.license !== null)
      ? LicenseView.fromPartial(object.license)
      : undefined;
    message.active = object.active ?? false;
    return message;
  },
};

function createBaseQueryHoldersRequest(): QueryHoldersRequest {
  return { featureId: "" };
}

export const QueryHoldersRequest: MessageFns<QueryHoldersRequest> = {
  encode(message: QueryHoldersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.featureId !== "") {
      writer.uint32(10).string(message.featureId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryHoldersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryHoldersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.featureId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryHoldersRequest {
    return {
      featureId: isSet(object.featureId)
        ? globalThis.String(object.featureId)
        : isSet(object.feature_id)
        ? globalThis.String(object.feature_id)
        : "",
    };
  },

  toJSON(message: QueryHoldersRequest): unknown {
    const obj: any = {};
    if (message.featureId !== "") {
      obj.featureId = message.featureId;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryHoldersRequest>): QueryHoldersRequest {
    return QueryHoldersRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryHoldersRequest>): QueryHoldersRequest {
    const message = createBaseQueryHoldersRequest();
    message.featureId = object.featureId ?? "";
    return message;
  },
};

function createBaseQueryHoldersResponse(): QueryHoldersResponse {
  return { licenses: [] };
}

export const QueryHoldersResponse: MessageFns<QueryHoldersResponse> = {
  encode(message: QueryHoldersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.licenses) {
      LicenseView.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryHoldersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryHoldersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.licenses.push(LicenseView.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryHoldersResponse {
    return {
      licenses: globalThis.Array.isArray(object?.licenses)
        ? object.licenses.map((e: any) => LicenseView.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryHoldersResponse): unknown {
    const obj: any = {};
    if (message.licenses?.length) {
      obj.licenses = message.licenses.map((e) => LicenseView.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<QueryHoldersResponse>): QueryHoldersResponse {
    return QueryHoldersResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryHoldersResponse>): QueryHoldersResponse {
    const message = createBaseQueryHoldersResponse();
    message.licenses = object.licenses?.map((e) => LicenseView.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryListRequest(): QueryListRequest {
  return { grantee: "" };
}

export const QueryListRequest: MessageFns<QueryListRequest> = {
  encode(message: QueryListRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.grantee !== "") {
      writer.uint32(10).string(message.grantee);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryListRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.grantee = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListRequest {
    return { grantee: isSet(object.grantee) ? globalThis.String(object.grantee) : "" };
  },

  toJSON(message: QueryListRequest): unknown {
    const obj: any = {};
    if (message.grantee !== "") {
      obj.grantee = message.grantee;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryListRequest>): QueryListRequest {
    return QueryListRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryListRequest>): QueryListRequest {
    const message = createBaseQueryListRequest();
    message.grantee = object.grantee ?? "";
    return message;
  },
};

function createBaseQueryListResponse(): QueryListResponse {
  return { licenses: [] };
}

export const QueryListResponse: MessageFns<QueryListResponse> = {
  encode(message: QueryListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.licenses) {
      LicenseView.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.licenses.push(LicenseView.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListResponse {
    return {
      licenses: globalThis.Array.isArray(object?.licenses)
        ? object.licenses.map((e: any) => LicenseView.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryListResponse): unknown {
    const obj: any = {};
    if (message.licenses?.length) {
      obj.licenses = message.licenses.map((e) => LicenseView.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<QueryListResponse>): QueryListResponse {
    return QueryListResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryListResponse>): QueryListResponse {
    const message = createBaseQueryListResponse();
    message.licenses = object.licenses?.map((e) => LicenseView.fromPartial(e)) || [];
    return message;
  },
};

/** Query defines the gRPC query service for the license module. */
export type QueryDefinition = typeof QueryDefinition;
export const QueryDefinition = {
  name: "Query",
  fullName: "qorechain.license.v1.Query",
  methods: {
    /** Check returns a single license grant for (grantee, feature_id). */
    check: {
      name: "Check",
      requestType: QueryCheckRequest as typeof QueryCheckRequest,
      requestStream: false,
      responseType: QueryCheckResponse as typeof QueryCheckResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              52,
              18,
              50,
              47,
              113,
              111,
              114,
              101,
              99,
              104,
              97,
              105,
              110,
              47,
              108,
              105,
              99,
              101,
              110,
              115,
              101,
              47,
              118,
              49,
              47,
              99,
              104,
              101,
              99,
              107,
              47,
              123,
              103,
              114,
              97,
              110,
              116,
              101,
              101,
              125,
              47,
              123,
              102,
              101,
              97,
              116,
              117,
              114,
              101,
              95,
              105,
              100,
              125,
            ]) as Uint8Array,
          ],
        },
      },
    },
    /** Holders lists every grantee holding a given feature license. */
    holders: {
      name: "Holders",
      requestType: QueryHoldersRequest as typeof QueryHoldersRequest,
      requestStream: false,
      responseType: QueryHoldersResponse as typeof QueryHoldersResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              44,
              18,
              42,
              47,
              113,
              111,
              114,
              101,
              99,
              104,
              97,
              105,
              110,
              47,
              108,
              105,
              99,
              101,
              110,
              115,
              101,
              47,
              118,
              49,
              47,
              104,
              111,
              108,
              100,
              101,
              114,
              115,
              47,
              123,
              102,
              101,
              97,
              116,
              117,
              114,
              101,
              95,
              105,
              100,
              125,
            ]) as Uint8Array,
          ],
        },
      },
    },
    /** List returns every license held by a grantee. */
    list: {
      name: "List",
      requestType: QueryListRequest as typeof QueryListRequest,
      requestStream: false,
      responseType: QueryListResponse as typeof QueryListResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              38,
              18,
              36,
              47,
              113,
              111,
              114,
              101,
              99,
              104,
              97,
              105,
              110,
              47,
              108,
              105,
              99,
              101,
              110,
              115,
              101,
              47,
              118,
              49,
              47,
              108,
              105,
              115,
              116,
              47,
              123,
              103,
              114,
              97,
              110,
              116,
              101,
              101,
              125,
            ]) as Uint8Array,
          ],
        },
      },
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
