// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/abstractaccount/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Coin } from "../../../cosmos/base/v1beta1/coin";

export const protobufPackage = "qorechain.abstractaccount.v1";

/** SpendingRule defines spending limits for an abstract account. */
export interface SpendingRule {
  id: string;
  dailyLimit: string;
  perTxLimit: string;
  allowedDenoms: string[];
  enabled: boolean;
}

export interface MsgCreateAbstractAccount {
  owner: string;
  accountType: string;
}

export interface MsgCreateAbstractAccountResponse {
  address: string;
}

export interface MsgUpdateSpendingRules {
  owner: string;
  accountAddress: string;
  rules: SpendingRule[];
}

export interface MsgUpdateSpendingRulesResponse {
}

/**
 * MsgRegisterAuthenticator links a foreign-scheme wallet key (e.g. a Phantom
 * ed25519 key) to an account so it may act for that account under least-privilege,
 * time-bounded, revocable terms. Only the account owner (root key) may call it.
 */
export interface MsgRegisterAuthenticator {
  owner: string;
  accountAddress: string;
  /** "ed25519" | "secp256k1" */
  scheme: string;
  /** raw public key bytes */
  pubkey: Uint8Array;
  /** e.g. "svm", "send" */
  permissions: string[];
  /** unix seconds; must be within MaxSessionTTL */
  expiryUnix: string;
  label: string;
}

export interface MsgRegisterAuthenticatorResponse {
}

/**
 * MsgRevokeAuthenticator instantly disables a previously linked wallet key.
 * Only the account owner (root key) may call it.
 */
export interface MsgRevokeAuthenticator {
  owner: string;
  accountAddress: string;
  scheme: string;
  pubkey: Uint8Array;
}

export interface MsgRevokeAuthenticatorResponse {
}

/**
 * MsgExecuteEVM executes an EVM call/transfer authorized by a linked
 * authenticator (v3.1.85). The relayer submits + pays fees; the authenticator
 * (scheme,pubkey) signs the domain-separated sign-bytes binding chain-id, the
 * canonical account, the pubkey, to/value/data and the account's expected EVM
 * nonce (replay protection — the nonce must equal the account's current EVM
 * nonce, and executing the call increments it, so a signature cannot be replayed).
 * The chain resolves the authenticator, checks the "evm" permission + SpendingRule
 * against `value`, then executes the call FROM the canonical account's EVM address.
 */
export interface MsgExecuteEVM {
  relayer: string;
  /** bech32 canonical account (the authenticator's owner) */
  account: string;
  /** "ed25519" | "secp256k1" */
  scheme: string;
  /** authenticator public key */
  pubkey: Uint8Array;
  /** authenticator signature over the EVM auth sign-bytes */
  signature: Uint8Array;
  /** 0x-hex recipient/contract; empty = contract create */
  to: string;
  /** native QOR amount in wei (aqor), decimal string */
  value: string;
  /** EVM calldata */
  data: Uint8Array;
  gasLimit: string;
  /** MUST equal the account's current EVM nonce */
  nonce: string;
}

export interface MsgExecuteEVMResponse {
  success: boolean;
  ret: Uint8Array;
  gasUsed: string;
  vmError: string;
}

/**
 * MsgExecuteCosmos executes a Native-lane (Cosmos) bank transfer authorized by a
 * linked authenticator (v3.1.85). It is the Native counterpart of MsgExecuteEVM:
 * the relayer submits + pays fees and signs the outer tx (so the account's own
 * PQC-required signature is not needed — the relayer's hybrid-PQC signature
 * satisfies the ante on the envelope), while the authenticator (scheme,pubkey)
 * signs the domain-separated sign-bytes binding chain-id, the canonical account,
 * the pubkey, the recipient, the amount and a per-authenticator sequence (replay:
 * nonce must equal the account+key's current sequence, incremented on success).
 * The chain resolves the authenticator, checks the "send" permission + SpendingRule
 * against `amount`, then moves the coins FROM the canonical account via x/bank.
 * This lets an external key (Phantom ed25519 / EVM secp256k1) spend native QOR from
 * a PQC-required account under least-privilege, spend-limited, revocable terms.
 */
export interface MsgExecuteCosmos {
  relayer: string;
  /** bech32 canonical account (the authenticator's owner) */
  account: string;
  /** "ed25519" | "secp256k1" */
  scheme: string;
  /** authenticator public key */
  pubkey: Uint8Array;
  /** authenticator signature over the Cosmos auth sign-bytes */
  signature: Uint8Array;
  /** bech32 recipient */
  to: string;
  amount: Coin[];
  /** MUST equal the account+key's current authenticator sequence */
  nonce: string;
}

export interface MsgExecuteCosmosResponse {
  success: boolean;
}

function createBaseSpendingRule(): SpendingRule {
  return { id: "", dailyLimit: "0", perTxLimit: "0", allowedDenoms: [], enabled: false };
}

export const SpendingRule: MessageFns<SpendingRule> = {
  encode(message: SpendingRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.dailyLimit !== "0") {
      writer.uint32(16).int64(message.dailyLimit);
    }
    if (message.perTxLimit !== "0") {
      writer.uint32(24).int64(message.perTxLimit);
    }
    for (const v of message.allowedDenoms) {
      writer.uint32(34).string(v!);
    }
    if (message.enabled !== false) {
      writer.uint32(40).bool(message.enabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpendingRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpendingRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.dailyLimit = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.perTxLimit = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.allowedDenoms.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpendingRule {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      dailyLimit: isSet(object.dailyLimit)
        ? globalThis.String(object.dailyLimit)
        : isSet(object.daily_limit)
        ? globalThis.String(object.daily_limit)
        : "0",
      perTxLimit: isSet(object.perTxLimit)
        ? globalThis.String(object.perTxLimit)
        : isSet(object.per_tx_limit)
        ? globalThis.String(object.per_tx_limit)
        : "0",
      allowedDenoms: globalThis.Array.isArray(object?.allowedDenoms)
        ? object.allowedDenoms.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.allowed_denoms)
        ? object.allowed_denoms.map((e: any) => globalThis.String(e))
        : [],
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
    };
  },

  toJSON(message: SpendingRule): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.dailyLimit !== "0") {
      obj.dailyLimit = message.dailyLimit;
    }
    if (message.perTxLimit !== "0") {
      obj.perTxLimit = message.perTxLimit;
    }
    if (message.allowedDenoms?.length) {
      obj.allowedDenoms = message.allowedDenoms;
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    return obj;
  },

  create(base?: DeepPartial<SpendingRule>): SpendingRule {
    return SpendingRule.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SpendingRule>): SpendingRule {
    const message = createBaseSpendingRule();
    message.id = object.id ?? "";
    message.dailyLimit = object.dailyLimit ?? "0";
    message.perTxLimit = object.perTxLimit ?? "0";
    message.allowedDenoms = object.allowedDenoms?.map((e) => e) || [];
    message.enabled = object.enabled ?? false;
    return message;
  },
};

function createBaseMsgCreateAbstractAccount(): MsgCreateAbstractAccount {
  return { owner: "", accountType: "" };
}

export const MsgCreateAbstractAccount: MessageFns<MsgCreateAbstractAccount> = {
  encode(message: MsgCreateAbstractAccount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    if (message.accountType !== "") {
      writer.uint32(18).string(message.accountType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCreateAbstractAccount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCreateAbstractAccount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.accountType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCreateAbstractAccount {
    return {
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      accountType: isSet(object.accountType)
        ? globalThis.String(object.accountType)
        : isSet(object.account_type)
        ? globalThis.String(object.account_type)
        : "",
    };
  },

  toJSON(message: MsgCreateAbstractAccount): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.accountType !== "") {
      obj.accountType = message.accountType;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgCreateAbstractAccount>): MsgCreateAbstractAccount {
    return MsgCreateAbstractAccount.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgCreateAbstractAccount>): MsgCreateAbstractAccount {
    const message = createBaseMsgCreateAbstractAccount();
    message.owner = object.owner ?? "";
    message.accountType = object.accountType ?? "";
    return message;
  },
};

function createBaseMsgCreateAbstractAccountResponse(): MsgCreateAbstractAccountResponse {
  return { address: "" };
}

export const MsgCreateAbstractAccountResponse: MessageFns<MsgCreateAbstractAccountResponse> = {
  encode(message: MsgCreateAbstractAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCreateAbstractAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCreateAbstractAccountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCreateAbstractAccountResponse {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: MsgCreateAbstractAccountResponse): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgCreateAbstractAccountResponse>): MsgCreateAbstractAccountResponse {
    return MsgCreateAbstractAccountResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgCreateAbstractAccountResponse>): MsgCreateAbstractAccountResponse {
    const message = createBaseMsgCreateAbstractAccountResponse();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseMsgUpdateSpendingRules(): MsgUpdateSpendingRules {
  return { owner: "", accountAddress: "", rules: [] };
}

export const MsgUpdateSpendingRules: MessageFns<MsgUpdateSpendingRules> = {
  encode(message: MsgUpdateSpendingRules, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    if (message.accountAddress !== "") {
      writer.uint32(18).string(message.accountAddress);
    }
    for (const v of message.rules) {
      SpendingRule.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateSpendingRules {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateSpendingRules();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.accountAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.rules.push(SpendingRule.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateSpendingRules {
    return {
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      accountAddress: isSet(object.accountAddress)
        ? globalThis.String(object.accountAddress)
        : isSet(object.account_address)
        ? globalThis.String(object.account_address)
        : "",
      rules: globalThis.Array.isArray(object?.rules) ? object.rules.map((e: any) => SpendingRule.fromJSON(e)) : [],
    };
  },

  toJSON(message: MsgUpdateSpendingRules): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.accountAddress !== "") {
      obj.accountAddress = message.accountAddress;
    }
    if (message.rules?.length) {
      obj.rules = message.rules.map((e) => SpendingRule.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateSpendingRules>): MsgUpdateSpendingRules {
    return MsgUpdateSpendingRules.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgUpdateSpendingRules>): MsgUpdateSpendingRules {
    const message = createBaseMsgUpdateSpendingRules();
    message.owner = object.owner ?? "";
    message.accountAddress = object.accountAddress ?? "";
    message.rules = object.rules?.map((e) => SpendingRule.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMsgUpdateSpendingRulesResponse(): MsgUpdateSpendingRulesResponse {
  return {};
}

export const MsgUpdateSpendingRulesResponse: MessageFns<MsgUpdateSpendingRulesResponse> = {
  encode(_: MsgUpdateSpendingRulesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateSpendingRulesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateSpendingRulesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateSpendingRulesResponse {
    return {};
  },

  toJSON(_: MsgUpdateSpendingRulesResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateSpendingRulesResponse>): MsgUpdateSpendingRulesResponse {
    return MsgUpdateSpendingRulesResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgUpdateSpendingRulesResponse>): MsgUpdateSpendingRulesResponse {
    const message = createBaseMsgUpdateSpendingRulesResponse();
    return message;
  },
};

function createBaseMsgRegisterAuthenticator(): MsgRegisterAuthenticator {
  return {
    owner: "",
    accountAddress: "",
    scheme: "",
    pubkey: new Uint8Array(0),
    permissions: [],
    expiryUnix: "0",
    label: "",
  };
}

export const MsgRegisterAuthenticator: MessageFns<MsgRegisterAuthenticator> = {
  encode(message: MsgRegisterAuthenticator, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    if (message.accountAddress !== "") {
      writer.uint32(18).string(message.accountAddress);
    }
    if (message.scheme !== "") {
      writer.uint32(26).string(message.scheme);
    }
    if (message.pubkey.length !== 0) {
      writer.uint32(34).bytes(message.pubkey);
    }
    for (const v of message.permissions) {
      writer.uint32(42).string(v!);
    }
    if (message.expiryUnix !== "0") {
      writer.uint32(48).int64(message.expiryUnix);
    }
    if (message.label !== "") {
      writer.uint32(58).string(message.label);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterAuthenticator {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterAuthenticator();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.accountAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scheme = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pubkey = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.permissions.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.expiryUnix = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.label = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterAuthenticator {
    return {
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      accountAddress: isSet(object.accountAddress)
        ? globalThis.String(object.accountAddress)
        : isSet(object.account_address)
        ? globalThis.String(object.account_address)
        : "",
      scheme: isSet(object.scheme) ? globalThis.String(object.scheme) : "",
      pubkey: isSet(object.pubkey) ? bytesFromBase64(object.pubkey) : new Uint8Array(0),
      permissions: globalThis.Array.isArray(object?.permissions)
        ? object.permissions.map((e: any) => globalThis.String(e))
        : [],
      expiryUnix: isSet(object.expiryUnix)
        ? globalThis.String(object.expiryUnix)
        : isSet(object.expiry_unix)
        ? globalThis.String(object.expiry_unix)
        : "0",
      label: isSet(object.label) ? globalThis.String(object.label) : "",
    };
  },

  toJSON(message: MsgRegisterAuthenticator): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.accountAddress !== "") {
      obj.accountAddress = message.accountAddress;
    }
    if (message.scheme !== "") {
      obj.scheme = message.scheme;
    }
    if (message.pubkey.length !== 0) {
      obj.pubkey = base64FromBytes(message.pubkey);
    }
    if (message.permissions?.length) {
      obj.permissions = message.permissions;
    }
    if (message.expiryUnix !== "0") {
      obj.expiryUnix = message.expiryUnix;
    }
    if (message.label !== "") {
      obj.label = message.label;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterAuthenticator>): MsgRegisterAuthenticator {
    return MsgRegisterAuthenticator.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterAuthenticator>): MsgRegisterAuthenticator {
    const message = createBaseMsgRegisterAuthenticator();
    message.owner = object.owner ?? "";
    message.accountAddress = object.accountAddress ?? "";
    message.scheme = object.scheme ?? "";
    message.pubkey = object.pubkey ?? new Uint8Array(0);
    message.permissions = object.permissions?.map((e) => e) || [];
    message.expiryUnix = object.expiryUnix ?? "0";
    message.label = object.label ?? "";
    return message;
  },
};

function createBaseMsgRegisterAuthenticatorResponse(): MsgRegisterAuthenticatorResponse {
  return {};
}

export const MsgRegisterAuthenticatorResponse: MessageFns<MsgRegisterAuthenticatorResponse> = {
  encode(_: MsgRegisterAuthenticatorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterAuthenticatorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterAuthenticatorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRegisterAuthenticatorResponse {
    return {};
  },

  toJSON(_: MsgRegisterAuthenticatorResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterAuthenticatorResponse>): MsgRegisterAuthenticatorResponse {
    return MsgRegisterAuthenticatorResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgRegisterAuthenticatorResponse>): MsgRegisterAuthenticatorResponse {
    const message = createBaseMsgRegisterAuthenticatorResponse();
    return message;
  },
};

function createBaseMsgRevokeAuthenticator(): MsgRevokeAuthenticator {
  return { owner: "", accountAddress: "", scheme: "", pubkey: new Uint8Array(0) };
}

export const MsgRevokeAuthenticator: MessageFns<MsgRevokeAuthenticator> = {
  encode(message: MsgRevokeAuthenticator, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    if (message.accountAddress !== "") {
      writer.uint32(18).string(message.accountAddress);
    }
    if (message.scheme !== "") {
      writer.uint32(26).string(message.scheme);
    }
    if (message.pubkey.length !== 0) {
      writer.uint32(34).bytes(message.pubkey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRevokeAuthenticator {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRevokeAuthenticator();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.accountAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scheme = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pubkey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRevokeAuthenticator {
    return {
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      accountAddress: isSet(object.accountAddress)
        ? globalThis.String(object.accountAddress)
        : isSet(object.account_address)
        ? globalThis.String(object.account_address)
        : "",
      scheme: isSet(object.scheme) ? globalThis.String(object.scheme) : "",
      pubkey: isSet(object.pubkey) ? bytesFromBase64(object.pubkey) : new Uint8Array(0),
    };
  },

  toJSON(message: MsgRevokeAuthenticator): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.accountAddress !== "") {
      obj.accountAddress = message.accountAddress;
    }
    if (message.scheme !== "") {
      obj.scheme = message.scheme;
    }
    if (message.pubkey.length !== 0) {
      obj.pubkey = base64FromBytes(message.pubkey);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRevokeAuthenticator>): MsgRevokeAuthenticator {
    return MsgRevokeAuthenticator.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRevokeAuthenticator>): MsgRevokeAuthenticator {
    const message = createBaseMsgRevokeAuthenticator();
    message.owner = object.owner ?? "";
    message.accountAddress = object.accountAddress ?? "";
    message.scheme = object.scheme ?? "";
    message.pubkey = object.pubkey ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgRevokeAuthenticatorResponse(): MsgRevokeAuthenticatorResponse {
  return {};
}

export const MsgRevokeAuthenticatorResponse: MessageFns<MsgRevokeAuthenticatorResponse> = {
  encode(_: MsgRevokeAuthenticatorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRevokeAuthenticatorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRevokeAuthenticatorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRevokeAuthenticatorResponse {
    return {};
  },

  toJSON(_: MsgRevokeAuthenticatorResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgRevokeAuthenticatorResponse>): MsgRevokeAuthenticatorResponse {
    return MsgRevokeAuthenticatorResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgRevokeAuthenticatorResponse>): MsgRevokeAuthenticatorResponse {
    const message = createBaseMsgRevokeAuthenticatorResponse();
    return message;
  },
};

function createBaseMsgExecuteEVM(): MsgExecuteEVM {
  return {
    relayer: "",
    account: "",
    scheme: "",
    pubkey: new Uint8Array(0),
    signature: new Uint8Array(0),
    to: "",
    value: "",
    data: new Uint8Array(0),
    gasLimit: "0",
    nonce: "0",
  };
}

export const MsgExecuteEVM: MessageFns<MsgExecuteEVM> = {
  encode(message: MsgExecuteEVM, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.relayer !== "") {
      writer.uint32(10).string(message.relayer);
    }
    if (message.account !== "") {
      writer.uint32(18).string(message.account);
    }
    if (message.scheme !== "") {
      writer.uint32(26).string(message.scheme);
    }
    if (message.pubkey.length !== 0) {
      writer.uint32(34).bytes(message.pubkey);
    }
    if (message.signature.length !== 0) {
      writer.uint32(42).bytes(message.signature);
    }
    if (message.to !== "") {
      writer.uint32(50).string(message.to);
    }
    if (message.value !== "") {
      writer.uint32(58).string(message.value);
    }
    if (message.data.length !== 0) {
      writer.uint32(66).bytes(message.data);
    }
    if (message.gasLimit !== "0") {
      writer.uint32(72).uint64(message.gasLimit);
    }
    if (message.nonce !== "0") {
      writer.uint32(80).uint64(message.nonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgExecuteEVM {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgExecuteEVM();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.relayer = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.account = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scheme = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pubkey = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.to = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.value = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.gasLimit = reader.uint64().toString();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.nonce = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgExecuteEVM {
    return {
      relayer: isSet(object.relayer) ? globalThis.String(object.relayer) : "",
      account: isSet(object.account) ? globalThis.String(object.account) : "",
      scheme: isSet(object.scheme) ? globalThis.String(object.scheme) : "",
      pubkey: isSet(object.pubkey) ? bytesFromBase64(object.pubkey) : new Uint8Array(0),
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
      to: isSet(object.to) ? globalThis.String(object.to) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
      gasLimit: isSet(object.gasLimit)
        ? globalThis.String(object.gasLimit)
        : isSet(object.gas_limit)
        ? globalThis.String(object.gas_limit)
        : "0",
      nonce: isSet(object.nonce) ? globalThis.String(object.nonce) : "0",
    };
  },

  toJSON(message: MsgExecuteEVM): unknown {
    const obj: any = {};
    if (message.relayer !== "") {
      obj.relayer = message.relayer;
    }
    if (message.account !== "") {
      obj.account = message.account;
    }
    if (message.scheme !== "") {
      obj.scheme = message.scheme;
    }
    if (message.pubkey.length !== 0) {
      obj.pubkey = base64FromBytes(message.pubkey);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.to !== "") {
      obj.to = message.to;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.gasLimit !== "0") {
      obj.gasLimit = message.gasLimit;
    }
    if (message.nonce !== "0") {
      obj.nonce = message.nonce;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgExecuteEVM>): MsgExecuteEVM {
    return MsgExecuteEVM.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgExecuteEVM>): MsgExecuteEVM {
    const message = createBaseMsgExecuteEVM();
    message.relayer = object.relayer ?? "";
    message.account = object.account ?? "";
    message.scheme = object.scheme ?? "";
    message.pubkey = object.pubkey ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    message.to = object.to ?? "";
    message.value = object.value ?? "";
    message.data = object.data ?? new Uint8Array(0);
    message.gasLimit = object.gasLimit ?? "0";
    message.nonce = object.nonce ?? "0";
    return message;
  },
};

function createBaseMsgExecuteEVMResponse(): MsgExecuteEVMResponse {
  return { success: false, ret: new Uint8Array(0), gasUsed: "0", vmError: "" };
}

export const MsgExecuteEVMResponse: MessageFns<MsgExecuteEVMResponse> = {
  encode(message: MsgExecuteEVMResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.ret.length !== 0) {
      writer.uint32(18).bytes(message.ret);
    }
    if (message.gasUsed !== "0") {
      writer.uint32(24).uint64(message.gasUsed);
    }
    if (message.vmError !== "") {
      writer.uint32(34).string(message.vmError);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgExecuteEVMResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgExecuteEVMResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ret = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.gasUsed = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.vmError = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgExecuteEVMResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      ret: isSet(object.ret) ? bytesFromBase64(object.ret) : new Uint8Array(0),
      gasUsed: isSet(object.gasUsed)
        ? globalThis.String(object.gasUsed)
        : isSet(object.gas_used)
        ? globalThis.String(object.gas_used)
        : "0",
      vmError: isSet(object.vmError)
        ? globalThis.String(object.vmError)
        : isSet(object.vm_error)
        ? globalThis.String(object.vm_error)
        : "",
    };
  },

  toJSON(message: MsgExecuteEVMResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.ret.length !== 0) {
      obj.ret = base64FromBytes(message.ret);
    }
    if (message.gasUsed !== "0") {
      obj.gasUsed = message.gasUsed;
    }
    if (message.vmError !== "") {
      obj.vmError = message.vmError;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgExecuteEVMResponse>): MsgExecuteEVMResponse {
    return MsgExecuteEVMResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgExecuteEVMResponse>): MsgExecuteEVMResponse {
    const message = createBaseMsgExecuteEVMResponse();
    message.success = object.success ?? false;
    message.ret = object.ret ?? new Uint8Array(0);
    message.gasUsed = object.gasUsed ?? "0";
    message.vmError = object.vmError ?? "";
    return message;
  },
};

function createBaseMsgExecuteCosmos(): MsgExecuteCosmos {
  return {
    relayer: "",
    account: "",
    scheme: "",
    pubkey: new Uint8Array(0),
    signature: new Uint8Array(0),
    to: "",
    amount: [],
    nonce: "0",
  };
}

export const MsgExecuteCosmos: MessageFns<MsgExecuteCosmos> = {
  encode(message: MsgExecuteCosmos, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.relayer !== "") {
      writer.uint32(10).string(message.relayer);
    }
    if (message.account !== "") {
      writer.uint32(18).string(message.account);
    }
    if (message.scheme !== "") {
      writer.uint32(26).string(message.scheme);
    }
    if (message.pubkey.length !== 0) {
      writer.uint32(34).bytes(message.pubkey);
    }
    if (message.signature.length !== 0) {
      writer.uint32(42).bytes(message.signature);
    }
    if (message.to !== "") {
      writer.uint32(50).string(message.to);
    }
    for (const v of message.amount) {
      Coin.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.nonce !== "0") {
      writer.uint32(64).uint64(message.nonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgExecuteCosmos {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgExecuteCosmos();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.relayer = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.account = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scheme = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pubkey = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.to = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.amount.push(Coin.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.nonce = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgExecuteCosmos {
    return {
      relayer: isSet(object.relayer) ? globalThis.String(object.relayer) : "",
      account: isSet(object.account) ? globalThis.String(object.account) : "",
      scheme: isSet(object.scheme) ? globalThis.String(object.scheme) : "",
      pubkey: isSet(object.pubkey) ? bytesFromBase64(object.pubkey) : new Uint8Array(0),
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
      to: isSet(object.to) ? globalThis.String(object.to) : "",
      amount: globalThis.Array.isArray(object?.amount) ? object.amount.map((e: any) => Coin.fromJSON(e)) : [],
      nonce: isSet(object.nonce) ? globalThis.String(object.nonce) : "0",
    };
  },

  toJSON(message: MsgExecuteCosmos): unknown {
    const obj: any = {};
    if (message.relayer !== "") {
      obj.relayer = message.relayer;
    }
    if (message.account !== "") {
      obj.account = message.account;
    }
    if (message.scheme !== "") {
      obj.scheme = message.scheme;
    }
    if (message.pubkey.length !== 0) {
      obj.pubkey = base64FromBytes(message.pubkey);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.to !== "") {
      obj.to = message.to;
    }
    if (message.amount?.length) {
      obj.amount = message.amount.map((e) => Coin.toJSON(e));
    }
    if (message.nonce !== "0") {
      obj.nonce = message.nonce;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgExecuteCosmos>): MsgExecuteCosmos {
    return MsgExecuteCosmos.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgExecuteCosmos>): MsgExecuteCosmos {
    const message = createBaseMsgExecuteCosmos();
    message.relayer = object.relayer ?? "";
    message.account = object.account ?? "";
    message.scheme = object.scheme ?? "";
    message.pubkey = object.pubkey ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    message.to = object.to ?? "";
    message.amount = object.amount?.map((e) => Coin.fromPartial(e)) || [];
    message.nonce = object.nonce ?? "0";
    return message;
  },
};

function createBaseMsgExecuteCosmosResponse(): MsgExecuteCosmosResponse {
  return { success: false };
}

export const MsgExecuteCosmosResponse: MessageFns<MsgExecuteCosmosResponse> = {
  encode(message: MsgExecuteCosmosResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgExecuteCosmosResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgExecuteCosmosResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgExecuteCosmosResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: MsgExecuteCosmosResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgExecuteCosmosResponse>): MsgExecuteCosmosResponse {
    return MsgExecuteCosmosResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgExecuteCosmosResponse>): MsgExecuteCosmosResponse {
    const message = createBaseMsgExecuteCosmosResponse();
    message.success = object.success ?? false;
    return message;
  },
};

/** Msg defines the abstractaccount module's transaction service. */
export type MsgDefinition = typeof MsgDefinition;
export const MsgDefinition = {
  name: "Msg",
  fullName: "qorechain.abstractaccount.v1.Msg",
  methods: {
    createAbstractAccount: {
      name: "CreateAbstractAccount",
      requestType: MsgCreateAbstractAccount as typeof MsgCreateAbstractAccount,
      requestStream: false,
      responseType: MsgCreateAbstractAccountResponse as typeof MsgCreateAbstractAccountResponse,
      responseStream: false,
      options: {},
    },
    updateSpendingRules: {
      name: "UpdateSpendingRules",
      requestType: MsgUpdateSpendingRules as typeof MsgUpdateSpendingRules,
      requestStream: false,
      responseType: MsgUpdateSpendingRulesResponse as typeof MsgUpdateSpendingRulesResponse,
      responseStream: false,
      options: {},
    },
    registerAuthenticator: {
      name: "RegisterAuthenticator",
      requestType: MsgRegisterAuthenticator as typeof MsgRegisterAuthenticator,
      requestStream: false,
      responseType: MsgRegisterAuthenticatorResponse as typeof MsgRegisterAuthenticatorResponse,
      responseStream: false,
      options: {},
    },
    revokeAuthenticator: {
      name: "RevokeAuthenticator",
      requestType: MsgRevokeAuthenticator as typeof MsgRevokeAuthenticator,
      requestStream: false,
      responseType: MsgRevokeAuthenticatorResponse as typeof MsgRevokeAuthenticatorResponse,
      responseStream: false,
      options: {},
    },
    executeEVM: {
      name: "ExecuteEVM",
      requestType: MsgExecuteEVM as typeof MsgExecuteEVM,
      requestStream: false,
      responseType: MsgExecuteEVMResponse as typeof MsgExecuteEVMResponse,
      responseStream: false,
      options: {},
    },
    executeCosmos: {
      name: "ExecuteCosmos",
      requestType: MsgExecuteCosmos as typeof MsgExecuteCosmos,
      requestStream: false,
      responseType: MsgExecuteCosmosResponse as typeof MsgExecuteCosmosResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
