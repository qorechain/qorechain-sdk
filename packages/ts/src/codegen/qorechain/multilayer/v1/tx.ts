// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/multilayer/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.multilayer.v1";

export interface MsgRegisterSidechain {
  creator: string;
  layerId: string;
  description: string;
  targetBlockTimeMs: string;
  maxTransactionsPerBlock: string;
  minValidators: number;
  settlementIntervalBlocks: string;
  supportedVmTypes: string[];
  supportedDomains: string[];
}

export interface MsgRegisterSidechainResponse {
  layerId: string;
  chainId: string;
  status: string;
}

export interface MsgRegisterPaychain {
  creator: string;
  layerId: string;
  description: string;
  maxTransactionsPerBlock: string;
  settlementIntervalBlocks: string;
  baseFeeMultiplier: string;
}

export interface MsgRegisterPaychainResponse {
  layerId: string;
  status: string;
}

export interface MsgAnchorState {
  relayer: string;
  layerId: string;
  layerHeight: string;
  stateRoot: Uint8Array;
  validatorSetHash: Uint8Array;
  pqcAggregateSignature: Uint8Array;
  transactionCount: string;
  compressedStateProof: Uint8Array;
}

export interface MsgAnchorStateResponse {
  mainChainHeight: string;
  accepted: boolean;
}

export interface MsgRouteTransaction {
  sender: string;
  transactionPayload: Uint8Array;
  preferredLayer: string;
  maxLatencyMs: string;
  maxFee: string;
}

export interface MsgRouteTransactionResponse {
  selectedLayer: string;
  reason: string;
  crossLayerMessageId: string;
}

export interface MsgUpdateLayerStatus {
  authority: string;
  layerId: string;
  newStatus: string;
  reason: string;
}

export interface MsgUpdateLayerStatusResponse {
}

export interface MsgChallengeAnchor {
  challenger: string;
  layerId: string;
  anchorHeight: string;
  fraudProof: Uint8Array;
  challengeReason: string;
}

export interface MsgChallengeAnchorResponse {
  challengeAccepted: boolean;
  resolution: string;
}

function createBaseMsgRegisterSidechain(): MsgRegisterSidechain {
  return {
    creator: "",
    layerId: "",
    description: "",
    targetBlockTimeMs: "0",
    maxTransactionsPerBlock: "0",
    minValidators: 0,
    settlementIntervalBlocks: "0",
    supportedVmTypes: [],
    supportedDomains: [],
  };
}

export const MsgRegisterSidechain: MessageFns<MsgRegisterSidechain> = {
  encode(message: MsgRegisterSidechain, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.layerId !== "") {
      writer.uint32(18).string(message.layerId);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.targetBlockTimeMs !== "0") {
      writer.uint32(32).uint64(message.targetBlockTimeMs);
    }
    if (message.maxTransactionsPerBlock !== "0") {
      writer.uint32(40).uint64(message.maxTransactionsPerBlock);
    }
    if (message.minValidators !== 0) {
      writer.uint32(48).uint32(message.minValidators);
    }
    if (message.settlementIntervalBlocks !== "0") {
      writer.uint32(56).uint64(message.settlementIntervalBlocks);
    }
    for (const v of message.supportedVmTypes) {
      writer.uint32(66).string(v!);
    }
    for (const v of message.supportedDomains) {
      writer.uint32(74).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterSidechain {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterSidechain();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.layerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.targetBlockTimeMs = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxTransactionsPerBlock = reader.uint64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.minValidators = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.settlementIntervalBlocks = reader.uint64().toString();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.supportedVmTypes.push(reader.string());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.supportedDomains.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterSidechain {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      layerId: isSet(object.layerId)
        ? globalThis.String(object.layerId)
        : isSet(object.layer_id)
        ? globalThis.String(object.layer_id)
        : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      targetBlockTimeMs: isSet(object.targetBlockTimeMs)
        ? globalThis.String(object.targetBlockTimeMs)
        : isSet(object.target_block_time_ms)
        ? globalThis.String(object.target_block_time_ms)
        : "0",
      maxTransactionsPerBlock: isSet(object.maxTransactionsPerBlock)
        ? globalThis.String(object.maxTransactionsPerBlock)
        : isSet(object.max_transactions_per_block)
        ? globalThis.String(object.max_transactions_per_block)
        : "0",
      minValidators: isSet(object.minValidators)
        ? globalThis.Number(object.minValidators)
        : isSet(object.min_validators)
        ? globalThis.Number(object.min_validators)
        : 0,
      settlementIntervalBlocks: isSet(object.settlementIntervalBlocks)
        ? globalThis.String(object.settlementIntervalBlocks)
        : isSet(object.settlement_interval_blocks)
        ? globalThis.String(object.settlement_interval_blocks)
        : "0",
      supportedVmTypes: globalThis.Array.isArray(object?.supportedVmTypes)
        ? object.supportedVmTypes.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.supported_vm_types)
        ? object.supported_vm_types.map((e: any) => globalThis.String(e))
        : [],
      supportedDomains: globalThis.Array.isArray(object?.supportedDomains)
        ? object.supportedDomains.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.supported_domains)
        ? object.supported_domains.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: MsgRegisterSidechain): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.layerId !== "") {
      obj.layerId = message.layerId;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.targetBlockTimeMs !== "0") {
      obj.targetBlockTimeMs = message.targetBlockTimeMs;
    }
    if (message.maxTransactionsPerBlock !== "0") {
      obj.maxTransactionsPerBlock = message.maxTransactionsPerBlock;
    }
    if (message.minValidators !== 0) {
      obj.minValidators = Math.round(message.minValidators);
    }
    if (message.settlementIntervalBlocks !== "0") {
      obj.settlementIntervalBlocks = message.settlementIntervalBlocks;
    }
    if (message.supportedVmTypes?.length) {
      obj.supportedVmTypes = message.supportedVmTypes;
    }
    if (message.supportedDomains?.length) {
      obj.supportedDomains = message.supportedDomains;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterSidechain>): MsgRegisterSidechain {
    return MsgRegisterSidechain.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterSidechain>): MsgRegisterSidechain {
    const message = createBaseMsgRegisterSidechain();
    message.creator = object.creator ?? "";
    message.layerId = object.layerId ?? "";
    message.description = object.description ?? "";
    message.targetBlockTimeMs = object.targetBlockTimeMs ?? "0";
    message.maxTransactionsPerBlock = object.maxTransactionsPerBlock ?? "0";
    message.minValidators = object.minValidators ?? 0;
    message.settlementIntervalBlocks = object.settlementIntervalBlocks ?? "0";
    message.supportedVmTypes = object.supportedVmTypes?.map((e) => e) || [];
    message.supportedDomains = object.supportedDomains?.map((e) => e) || [];
    return message;
  },
};

function createBaseMsgRegisterSidechainResponse(): MsgRegisterSidechainResponse {
  return { layerId: "", chainId: "", status: "" };
}

export const MsgRegisterSidechainResponse: MessageFns<MsgRegisterSidechainResponse> = {
  encode(message: MsgRegisterSidechainResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.layerId !== "") {
      writer.uint32(10).string(message.layerId);
    }
    if (message.chainId !== "") {
      writer.uint32(18).string(message.chainId);
    }
    if (message.status !== "") {
      writer.uint32(26).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterSidechainResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterSidechainResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.layerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chainId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterSidechainResponse {
    return {
      layerId: isSet(object.layerId)
        ? globalThis.String(object.layerId)
        : isSet(object.layer_id)
        ? globalThis.String(object.layer_id)
        : "",
      chainId: isSet(object.chainId)
        ? globalThis.String(object.chainId)
        : isSet(object.chain_id)
        ? globalThis.String(object.chain_id)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: MsgRegisterSidechainResponse): unknown {
    const obj: any = {};
    if (message.layerId !== "") {
      obj.layerId = message.layerId;
    }
    if (message.chainId !== "") {
      obj.chainId = message.chainId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterSidechainResponse>): MsgRegisterSidechainResponse {
    return MsgRegisterSidechainResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterSidechainResponse>): MsgRegisterSidechainResponse {
    const message = createBaseMsgRegisterSidechainResponse();
    message.layerId = object.layerId ?? "";
    message.chainId = object.chainId ?? "";
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseMsgRegisterPaychain(): MsgRegisterPaychain {
  return {
    creator: "",
    layerId: "",
    description: "",
    maxTransactionsPerBlock: "0",
    settlementIntervalBlocks: "0",
    baseFeeMultiplier: "",
  };
}

export const MsgRegisterPaychain: MessageFns<MsgRegisterPaychain> = {
  encode(message: MsgRegisterPaychain, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.layerId !== "") {
      writer.uint32(18).string(message.layerId);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.maxTransactionsPerBlock !== "0") {
      writer.uint32(32).uint64(message.maxTransactionsPerBlock);
    }
    if (message.settlementIntervalBlocks !== "0") {
      writer.uint32(40).uint64(message.settlementIntervalBlocks);
    }
    if (message.baseFeeMultiplier !== "") {
      writer.uint32(50).string(message.baseFeeMultiplier);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterPaychain {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterPaychain();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.layerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxTransactionsPerBlock = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.settlementIntervalBlocks = reader.uint64().toString();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.baseFeeMultiplier = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterPaychain {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      layerId: isSet(object.layerId)
        ? globalThis.String(object.layerId)
        : isSet(object.layer_id)
        ? globalThis.String(object.layer_id)
        : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      maxTransactionsPerBlock: isSet(object.maxTransactionsPerBlock)
        ? globalThis.String(object.maxTransactionsPerBlock)
        : isSet(object.max_transactions_per_block)
        ? globalThis.String(object.max_transactions_per_block)
        : "0",
      settlementIntervalBlocks: isSet(object.settlementIntervalBlocks)
        ? globalThis.String(object.settlementIntervalBlocks)
        : isSet(object.settlement_interval_blocks)
        ? globalThis.String(object.settlement_interval_blocks)
        : "0",
      baseFeeMultiplier: isSet(object.baseFeeMultiplier)
        ? globalThis.String(object.baseFeeMultiplier)
        : isSet(object.base_fee_multiplier)
        ? globalThis.String(object.base_fee_multiplier)
        : "",
    };
  },

  toJSON(message: MsgRegisterPaychain): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.layerId !== "") {
      obj.layerId = message.layerId;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.maxTransactionsPerBlock !== "0") {
      obj.maxTransactionsPerBlock = message.maxTransactionsPerBlock;
    }
    if (message.settlementIntervalBlocks !== "0") {
      obj.settlementIntervalBlocks = message.settlementIntervalBlocks;
    }
    if (message.baseFeeMultiplier !== "") {
      obj.baseFeeMultiplier = message.baseFeeMultiplier;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterPaychain>): MsgRegisterPaychain {
    return MsgRegisterPaychain.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterPaychain>): MsgRegisterPaychain {
    const message = createBaseMsgRegisterPaychain();
    message.creator = object.creator ?? "";
    message.layerId = object.layerId ?? "";
    message.description = object.description ?? "";
    message.maxTransactionsPerBlock = object.maxTransactionsPerBlock ?? "0";
    message.settlementIntervalBlocks = object.settlementIntervalBlocks ?? "0";
    message.baseFeeMultiplier = object.baseFeeMultiplier ?? "";
    return message;
  },
};

function createBaseMsgRegisterPaychainResponse(): MsgRegisterPaychainResponse {
  return { layerId: "", status: "" };
}

export const MsgRegisterPaychainResponse: MessageFns<MsgRegisterPaychainResponse> = {
  encode(message: MsgRegisterPaychainResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.layerId !== "") {
      writer.uint32(10).string(message.layerId);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterPaychainResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterPaychainResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.layerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterPaychainResponse {
    return {
      layerId: isSet(object.layerId)
        ? globalThis.String(object.layerId)
        : isSet(object.layer_id)
        ? globalThis.String(object.layer_id)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: MsgRegisterPaychainResponse): unknown {
    const obj: any = {};
    if (message.layerId !== "") {
      obj.layerId = message.layerId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterPaychainResponse>): MsgRegisterPaychainResponse {
    return MsgRegisterPaychainResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterPaychainResponse>): MsgRegisterPaychainResponse {
    const message = createBaseMsgRegisterPaychainResponse();
    message.layerId = object.layerId ?? "";
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseMsgAnchorState(): MsgAnchorState {
  return {
    relayer: "",
    layerId: "",
    layerHeight: "0",
    stateRoot: new Uint8Array(0),
    validatorSetHash: new Uint8Array(0),
    pqcAggregateSignature: new Uint8Array(0),
    transactionCount: "0",
    compressedStateProof: new Uint8Array(0),
  };
}

export const MsgAnchorState: MessageFns<MsgAnchorState> = {
  encode(message: MsgAnchorState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.relayer !== "") {
      writer.uint32(10).string(message.relayer);
    }
    if (message.layerId !== "") {
      writer.uint32(18).string(message.layerId);
    }
    if (message.layerHeight !== "0") {
      writer.uint32(24).uint64(message.layerHeight);
    }
    if (message.stateRoot.length !== 0) {
      writer.uint32(34).bytes(message.stateRoot);
    }
    if (message.validatorSetHash.length !== 0) {
      writer.uint32(42).bytes(message.validatorSetHash);
    }
    if (message.pqcAggregateSignature.length !== 0) {
      writer.uint32(50).bytes(message.pqcAggregateSignature);
    }
    if (message.transactionCount !== "0") {
      writer.uint32(56).uint64(message.transactionCount);
    }
    if (message.compressedStateProof.length !== 0) {
      writer.uint32(66).bytes(message.compressedStateProof);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgAnchorState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAnchorState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.relayer = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.layerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.layerHeight = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.stateRoot = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.validatorSetHash = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.pqcAggregateSignature = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.transactionCount = reader.uint64().toString();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.compressedStateProof = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgAnchorState {
    return {
      relayer: isSet(object.relayer) ? globalThis.String(object.relayer) : "",
      layerId: isSet(object.layerId)
        ? globalThis.String(object.layerId)
        : isSet(object.layer_id)
        ? globalThis.String(object.layer_id)
        : "",
      layerHeight: isSet(object.layerHeight)
        ? globalThis.String(object.layerHeight)
        : isSet(object.layer_height)
        ? globalThis.String(object.layer_height)
        : "0",
      stateRoot: isSet(object.stateRoot)
        ? bytesFromBase64(object.stateRoot)
        : isSet(object.state_root)
        ? bytesFromBase64(object.state_root)
        : new Uint8Array(0),
      validatorSetHash: isSet(object.validatorSetHash)
        ? bytesFromBase64(object.validatorSetHash)
        : isSet(object.validator_set_hash)
        ? bytesFromBase64(object.validator_set_hash)
        : new Uint8Array(0),
      pqcAggregateSignature: isSet(object.pqcAggregateSignature)
        ? bytesFromBase64(object.pqcAggregateSignature)
        : isSet(object.pqc_aggregate_signature)
        ? bytesFromBase64(object.pqc_aggregate_signature)
        : new Uint8Array(0),
      transactionCount: isSet(object.transactionCount)
        ? globalThis.String(object.transactionCount)
        : isSet(object.transaction_count)
        ? globalThis.String(object.transaction_count)
        : "0",
      compressedStateProof: isSet(object.compressedStateProof)
        ? bytesFromBase64(object.compressedStateProof)
        : isSet(object.compressed_state_proof)
        ? bytesFromBase64(object.compressed_state_proof)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgAnchorState): unknown {
    const obj: any = {};
    if (message.relayer !== "") {
      obj.relayer = message.relayer;
    }
    if (message.layerId !== "") {
      obj.layerId = message.layerId;
    }
    if (message.layerHeight !== "0") {
      obj.layerHeight = message.layerHeight;
    }
    if (message.stateRoot.length !== 0) {
      obj.stateRoot = base64FromBytes(message.stateRoot);
    }
    if (message.validatorSetHash.length !== 0) {
      obj.validatorSetHash = base64FromBytes(message.validatorSetHash);
    }
    if (message.pqcAggregateSignature.length !== 0) {
      obj.pqcAggregateSignature = base64FromBytes(message.pqcAggregateSignature);
    }
    if (message.transactionCount !== "0") {
      obj.transactionCount = message.transactionCount;
    }
    if (message.compressedStateProof.length !== 0) {
      obj.compressedStateProof = base64FromBytes(message.compressedStateProof);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgAnchorState>): MsgAnchorState {
    return MsgAnchorState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgAnchorState>): MsgAnchorState {
    const message = createBaseMsgAnchorState();
    message.relayer = object.relayer ?? "";
    message.layerId = object.layerId ?? "";
    message.layerHeight = object.layerHeight ?? "0";
    message.stateRoot = object.stateRoot ?? new Uint8Array(0);
    message.validatorSetHash = object.validatorSetHash ?? new Uint8Array(0);
    message.pqcAggregateSignature = object.pqcAggregateSignature ?? new Uint8Array(0);
    message.transactionCount = object.transactionCount ?? "0";
    message.compressedStateProof = object.compressedStateProof ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgAnchorStateResponse(): MsgAnchorStateResponse {
  return { mainChainHeight: "0", accepted: false };
}

export const MsgAnchorStateResponse: MessageFns<MsgAnchorStateResponse> = {
  encode(message: MsgAnchorStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mainChainHeight !== "0") {
      writer.uint32(8).uint64(message.mainChainHeight);
    }
    if (message.accepted !== false) {
      writer.uint32(16).bool(message.accepted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgAnchorStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAnchorStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.mainChainHeight = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgAnchorStateResponse {
    return {
      mainChainHeight: isSet(object.mainChainHeight)
        ? globalThis.String(object.mainChainHeight)
        : isSet(object.main_chain_height)
        ? globalThis.String(object.main_chain_height)
        : "0",
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
    };
  },

  toJSON(message: MsgAnchorStateResponse): unknown {
    const obj: any = {};
    if (message.mainChainHeight !== "0") {
      obj.mainChainHeight = message.mainChainHeight;
    }
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgAnchorStateResponse>): MsgAnchorStateResponse {
    return MsgAnchorStateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgAnchorStateResponse>): MsgAnchorStateResponse {
    const message = createBaseMsgAnchorStateResponse();
    message.mainChainHeight = object.mainChainHeight ?? "0";
    message.accepted = object.accepted ?? false;
    return message;
  },
};

function createBaseMsgRouteTransaction(): MsgRouteTransaction {
  return { sender: "", transactionPayload: new Uint8Array(0), preferredLayer: "", maxLatencyMs: "0", maxFee: "" };
}

export const MsgRouteTransaction: MessageFns<MsgRouteTransaction> = {
  encode(message: MsgRouteTransaction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.transactionPayload.length !== 0) {
      writer.uint32(18).bytes(message.transactionPayload);
    }
    if (message.preferredLayer !== "") {
      writer.uint32(26).string(message.preferredLayer);
    }
    if (message.maxLatencyMs !== "0") {
      writer.uint32(32).uint64(message.maxLatencyMs);
    }
    if (message.maxFee !== "") {
      writer.uint32(42).string(message.maxFee);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRouteTransaction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRouteTransaction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.transactionPayload = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.preferredLayer = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxLatencyMs = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.maxFee = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRouteTransaction {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      transactionPayload: isSet(object.transactionPayload)
        ? bytesFromBase64(object.transactionPayload)
        : isSet(object.transaction_payload)
        ? bytesFromBase64(object.transaction_payload)
        : new Uint8Array(0),
      preferredLayer: isSet(object.preferredLayer)
        ? globalThis.String(object.preferredLayer)
        : isSet(object.preferred_layer)
        ? globalThis.String(object.preferred_layer)
        : "",
      maxLatencyMs: isSet(object.maxLatencyMs)
        ? globalThis.String(object.maxLatencyMs)
        : isSet(object.max_latency_ms)
        ? globalThis.String(object.max_latency_ms)
        : "0",
      maxFee: isSet(object.maxFee)
        ? globalThis.String(object.maxFee)
        : isSet(object.max_fee)
        ? globalThis.String(object.max_fee)
        : "",
    };
  },

  toJSON(message: MsgRouteTransaction): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.transactionPayload.length !== 0) {
      obj.transactionPayload = base64FromBytes(message.transactionPayload);
    }
    if (message.preferredLayer !== "") {
      obj.preferredLayer = message.preferredLayer;
    }
    if (message.maxLatencyMs !== "0") {
      obj.maxLatencyMs = message.maxLatencyMs;
    }
    if (message.maxFee !== "") {
      obj.maxFee = message.maxFee;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRouteTransaction>): MsgRouteTransaction {
    return MsgRouteTransaction.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRouteTransaction>): MsgRouteTransaction {
    const message = createBaseMsgRouteTransaction();
    message.sender = object.sender ?? "";
    message.transactionPayload = object.transactionPayload ?? new Uint8Array(0);
    message.preferredLayer = object.preferredLayer ?? "";
    message.maxLatencyMs = object.maxLatencyMs ?? "0";
    message.maxFee = object.maxFee ?? "";
    return message;
  },
};

function createBaseMsgRouteTransactionResponse(): MsgRouteTransactionResponse {
  return { selectedLayer: "", reason: "", crossLayerMessageId: "" };
}

export const MsgRouteTransactionResponse: MessageFns<MsgRouteTransactionResponse> = {
  encode(message: MsgRouteTransactionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.selectedLayer !== "") {
      writer.uint32(10).string(message.selectedLayer);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    if (message.crossLayerMessageId !== "") {
      writer.uint32(26).string(message.crossLayerMessageId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRouteTransactionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRouteTransactionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selectedLayer = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.crossLayerMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRouteTransactionResponse {
    return {
      selectedLayer: isSet(object.selectedLayer)
        ? globalThis.String(object.selectedLayer)
        : isSet(object.selected_layer)
        ? globalThis.String(object.selected_layer)
        : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      crossLayerMessageId: isSet(object.crossLayerMessageId)
        ? globalThis.String(object.crossLayerMessageId)
        : isSet(object.cross_layer_message_id)
        ? globalThis.String(object.cross_layer_message_id)
        : "",
    };
  },

  toJSON(message: MsgRouteTransactionResponse): unknown {
    const obj: any = {};
    if (message.selectedLayer !== "") {
      obj.selectedLayer = message.selectedLayer;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.crossLayerMessageId !== "") {
      obj.crossLayerMessageId = message.crossLayerMessageId;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRouteTransactionResponse>): MsgRouteTransactionResponse {
    return MsgRouteTransactionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRouteTransactionResponse>): MsgRouteTransactionResponse {
    const message = createBaseMsgRouteTransactionResponse();
    message.selectedLayer = object.selectedLayer ?? "";
    message.reason = object.reason ?? "";
    message.crossLayerMessageId = object.crossLayerMessageId ?? "";
    return message;
  },
};

function createBaseMsgUpdateLayerStatus(): MsgUpdateLayerStatus {
  return { authority: "", layerId: "", newStatus: "", reason: "" };
}

export const MsgUpdateLayerStatus: MessageFns<MsgUpdateLayerStatus> = {
  encode(message: MsgUpdateLayerStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.layerId !== "") {
      writer.uint32(18).string(message.layerId);
    }
    if (message.newStatus !== "") {
      writer.uint32(26).string(message.newStatus);
    }
    if (message.reason !== "") {
      writer.uint32(34).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateLayerStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateLayerStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.layerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.newStatus = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateLayerStatus {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      layerId: isSet(object.layerId)
        ? globalThis.String(object.layerId)
        : isSet(object.layer_id)
        ? globalThis.String(object.layer_id)
        : "",
      newStatus: isSet(object.newStatus)
        ? globalThis.String(object.newStatus)
        : isSet(object.new_status)
        ? globalThis.String(object.new_status)
        : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: MsgUpdateLayerStatus): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.layerId !== "") {
      obj.layerId = message.layerId;
    }
    if (message.newStatus !== "") {
      obj.newStatus = message.newStatus;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateLayerStatus>): MsgUpdateLayerStatus {
    return MsgUpdateLayerStatus.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgUpdateLayerStatus>): MsgUpdateLayerStatus {
    const message = createBaseMsgUpdateLayerStatus();
    message.authority = object.authority ?? "";
    message.layerId = object.layerId ?? "";
    message.newStatus = object.newStatus ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseMsgUpdateLayerStatusResponse(): MsgUpdateLayerStatusResponse {
  return {};
}

export const MsgUpdateLayerStatusResponse: MessageFns<MsgUpdateLayerStatusResponse> = {
  encode(_: MsgUpdateLayerStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateLayerStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateLayerStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateLayerStatusResponse {
    return {};
  },

  toJSON(_: MsgUpdateLayerStatusResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateLayerStatusResponse>): MsgUpdateLayerStatusResponse {
    return MsgUpdateLayerStatusResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgUpdateLayerStatusResponse>): MsgUpdateLayerStatusResponse {
    const message = createBaseMsgUpdateLayerStatusResponse();
    return message;
  },
};

function createBaseMsgChallengeAnchor(): MsgChallengeAnchor {
  return { challenger: "", layerId: "", anchorHeight: "0", fraudProof: new Uint8Array(0), challengeReason: "" };
}

export const MsgChallengeAnchor: MessageFns<MsgChallengeAnchor> = {
  encode(message: MsgChallengeAnchor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.challenger !== "") {
      writer.uint32(10).string(message.challenger);
    }
    if (message.layerId !== "") {
      writer.uint32(18).string(message.layerId);
    }
    if (message.anchorHeight !== "0") {
      writer.uint32(24).uint64(message.anchorHeight);
    }
    if (message.fraudProof.length !== 0) {
      writer.uint32(34).bytes(message.fraudProof);
    }
    if (message.challengeReason !== "") {
      writer.uint32(42).string(message.challengeReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgChallengeAnchor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgChallengeAnchor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.challenger = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.layerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.anchorHeight = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.fraudProof = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.challengeReason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgChallengeAnchor {
    return {
      challenger: isSet(object.challenger) ? globalThis.String(object.challenger) : "",
      layerId: isSet(object.layerId)
        ? globalThis.String(object.layerId)
        : isSet(object.layer_id)
        ? globalThis.String(object.layer_id)
        : "",
      anchorHeight: isSet(object.anchorHeight)
        ? globalThis.String(object.anchorHeight)
        : isSet(object.anchor_height)
        ? globalThis.String(object.anchor_height)
        : "0",
      fraudProof: isSet(object.fraudProof)
        ? bytesFromBase64(object.fraudProof)
        : isSet(object.fraud_proof)
        ? bytesFromBase64(object.fraud_proof)
        : new Uint8Array(0),
      challengeReason: isSet(object.challengeReason)
        ? globalThis.String(object.challengeReason)
        : isSet(object.challenge_reason)
        ? globalThis.String(object.challenge_reason)
        : "",
    };
  },

  toJSON(message: MsgChallengeAnchor): unknown {
    const obj: any = {};
    if (message.challenger !== "") {
      obj.challenger = message.challenger;
    }
    if (message.layerId !== "") {
      obj.layerId = message.layerId;
    }
    if (message.anchorHeight !== "0") {
      obj.anchorHeight = message.anchorHeight;
    }
    if (message.fraudProof.length !== 0) {
      obj.fraudProof = base64FromBytes(message.fraudProof);
    }
    if (message.challengeReason !== "") {
      obj.challengeReason = message.challengeReason;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgChallengeAnchor>): MsgChallengeAnchor {
    return MsgChallengeAnchor.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgChallengeAnchor>): MsgChallengeAnchor {
    const message = createBaseMsgChallengeAnchor();
    message.challenger = object.challenger ?? "";
    message.layerId = object.layerId ?? "";
    message.anchorHeight = object.anchorHeight ?? "0";
    message.fraudProof = object.fraudProof ?? new Uint8Array(0);
    message.challengeReason = object.challengeReason ?? "";
    return message;
  },
};

function createBaseMsgChallengeAnchorResponse(): MsgChallengeAnchorResponse {
  return { challengeAccepted: false, resolution: "" };
}

export const MsgChallengeAnchorResponse: MessageFns<MsgChallengeAnchorResponse> = {
  encode(message: MsgChallengeAnchorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.challengeAccepted !== false) {
      writer.uint32(8).bool(message.challengeAccepted);
    }
    if (message.resolution !== "") {
      writer.uint32(18).string(message.resolution);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgChallengeAnchorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgChallengeAnchorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.challengeAccepted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.resolution = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgChallengeAnchorResponse {
    return {
      challengeAccepted: isSet(object.challengeAccepted)
        ? globalThis.Boolean(object.challengeAccepted)
        : isSet(object.challenge_accepted)
        ? globalThis.Boolean(object.challenge_accepted)
        : false,
      resolution: isSet(object.resolution) ? globalThis.String(object.resolution) : "",
    };
  },

  toJSON(message: MsgChallengeAnchorResponse): unknown {
    const obj: any = {};
    if (message.challengeAccepted !== false) {
      obj.challengeAccepted = message.challengeAccepted;
    }
    if (message.resolution !== "") {
      obj.resolution = message.resolution;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgChallengeAnchorResponse>): MsgChallengeAnchorResponse {
    return MsgChallengeAnchorResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgChallengeAnchorResponse>): MsgChallengeAnchorResponse {
    const message = createBaseMsgChallengeAnchorResponse();
    message.challengeAccepted = object.challengeAccepted ?? false;
    message.resolution = object.resolution ?? "";
    return message;
  },
};

export type MsgDefinition = typeof MsgDefinition;
export const MsgDefinition = {
  name: "Msg",
  fullName: "qorechain.multilayer.v1.Msg",
  methods: {
    registerSidechain: {
      name: "RegisterSidechain",
      requestType: MsgRegisterSidechain as typeof MsgRegisterSidechain,
      requestStream: false,
      responseType: MsgRegisterSidechainResponse as typeof MsgRegisterSidechainResponse,
      responseStream: false,
      options: {},
    },
    registerPaychain: {
      name: "RegisterPaychain",
      requestType: MsgRegisterPaychain as typeof MsgRegisterPaychain,
      requestStream: false,
      responseType: MsgRegisterPaychainResponse as typeof MsgRegisterPaychainResponse,
      responseStream: false,
      options: {},
    },
    anchorState: {
      name: "AnchorState",
      requestType: MsgAnchorState as typeof MsgAnchorState,
      requestStream: false,
      responseType: MsgAnchorStateResponse as typeof MsgAnchorStateResponse,
      responseStream: false,
      options: {},
    },
    routeTransaction: {
      name: "RouteTransaction",
      requestType: MsgRouteTransaction as typeof MsgRouteTransaction,
      requestStream: false,
      responseType: MsgRouteTransactionResponse as typeof MsgRouteTransactionResponse,
      responseStream: false,
      options: {},
    },
    updateLayerStatus: {
      name: "UpdateLayerStatus",
      requestType: MsgUpdateLayerStatus as typeof MsgUpdateLayerStatus,
      requestStream: false,
      responseType: MsgUpdateLayerStatusResponse as typeof MsgUpdateLayerStatusResponse,
      responseStream: false,
      options: {},
    },
    challengeAnchor: {
      name: "ChallengeAnchor",
      requestType: MsgChallengeAnchor as typeof MsgChallengeAnchor,
      requestStream: false,
      responseType: MsgChallengeAnchorResponse as typeof MsgChallengeAnchorResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
