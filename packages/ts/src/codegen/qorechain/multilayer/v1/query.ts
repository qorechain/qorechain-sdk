// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: qorechain/multilayer/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "qorechain.multilayer.v1";

export interface ParamsView {
  maxSidechains: string;
  maxPaychains: string;
  minAnchorInterval: string;
  maxAnchorInterval: string;
  defaultChallengePeriod: string;
  minSidechainStake: string;
  minPaychainStake: string;
  routingEnabled: boolean;
  routingConfidenceThreshold: string;
  crossLayerFeeBundling: boolean;
}

export interface LayerView {
  layerId: string;
  layerType: string;
  status: string;
  chainId: string;
  description: string;
  targetBlockTimeMs: string;
  maxTransactionsPerBlock: string;
  minValidators: number;
  settlementIntervalBlocks: string;
  challengePeriodSeconds: string;
}

export interface RoutingStatsView {
  totalRouted: string;
  routedToMain: string;
  routedToSidechains: string;
  routedToPaychains: string;
  averageGasSavingsPercent: string;
  averageLatencyImprovementPercent: string;
}

export interface QueryParamsRequest {
}

export interface QueryParamsResponse {
  params?: ParamsView | undefined;
}

export interface QueryLayerRequest {
  layerId: string;
}

export interface QueryLayerResponse {
  layer?: LayerView | undefined;
}

export interface QueryLayersRequest {
}

export interface QueryLayersResponse {
  layers: LayerView[];
}

export interface QueryRoutingStatsRequest {
}

export interface QueryRoutingStatsView {
  stats?: RoutingStatsView | undefined;
}

function createBaseParamsView(): ParamsView {
  return {
    maxSidechains: "0",
    maxPaychains: "0",
    minAnchorInterval: "0",
    maxAnchorInterval: "0",
    defaultChallengePeriod: "0",
    minSidechainStake: "",
    minPaychainStake: "",
    routingEnabled: false,
    routingConfidenceThreshold: "",
    crossLayerFeeBundling: false,
  };
}

export const ParamsView: MessageFns<ParamsView> = {
  encode(message: ParamsView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxSidechains !== "0") {
      writer.uint32(8).uint64(message.maxSidechains);
    }
    if (message.maxPaychains !== "0") {
      writer.uint32(16).uint64(message.maxPaychains);
    }
    if (message.minAnchorInterval !== "0") {
      writer.uint32(24).uint64(message.minAnchorInterval);
    }
    if (message.maxAnchorInterval !== "0") {
      writer.uint32(32).uint64(message.maxAnchorInterval);
    }
    if (message.defaultChallengePeriod !== "0") {
      writer.uint32(40).uint64(message.defaultChallengePeriod);
    }
    if (message.minSidechainStake !== "") {
      writer.uint32(50).string(message.minSidechainStake);
    }
    if (message.minPaychainStake !== "") {
      writer.uint32(58).string(message.minPaychainStake);
    }
    if (message.routingEnabled !== false) {
      writer.uint32(64).bool(message.routingEnabled);
    }
    if (message.routingConfidenceThreshold !== "") {
      writer.uint32(74).string(message.routingConfidenceThreshold);
    }
    if (message.crossLayerFeeBundling !== false) {
      writer.uint32(80).bool(message.crossLayerFeeBundling);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParamsView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParamsView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxSidechains = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxPaychains = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.minAnchorInterval = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxAnchorInterval = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.defaultChallengePeriod = reader.uint64().toString();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.minSidechainStake = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.minPaychainStake = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.routingEnabled = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.routingConfidenceThreshold = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.crossLayerFeeBundling = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParamsView {
    return {
      maxSidechains: isSet(object.maxSidechains)
        ? globalThis.String(object.maxSidechains)
        : isSet(object.max_sidechains)
        ? globalThis.String(object.max_sidechains)
        : "0",
      maxPaychains: isSet(object.maxPaychains)
        ? globalThis.String(object.maxPaychains)
        : isSet(object.max_paychains)
        ? globalThis.String(object.max_paychains)
        : "0",
      minAnchorInterval: isSet(object.minAnchorInterval)
        ? globalThis.String(object.minAnchorInterval)
        : isSet(object.min_anchor_interval)
        ? globalThis.String(object.min_anchor_interval)
        : "0",
      maxAnchorInterval: isSet(object.maxAnchorInterval)
        ? globalThis.String(object.maxAnchorInterval)
        : isSet(object.max_anchor_interval)
        ? globalThis.String(object.max_anchor_interval)
        : "0",
      defaultChallengePeriod: isSet(object.defaultChallengePeriod)
        ? globalThis.String(object.defaultChallengePeriod)
        : isSet(object.default_challenge_period)
        ? globalThis.String(object.default_challenge_period)
        : "0",
      minSidechainStake: isSet(object.minSidechainStake)
        ? globalThis.String(object.minSidechainStake)
        : isSet(object.min_sidechain_stake)
        ? globalThis.String(object.min_sidechain_stake)
        : "",
      minPaychainStake: isSet(object.minPaychainStake)
        ? globalThis.String(object.minPaychainStake)
        : isSet(object.min_paychain_stake)
        ? globalThis.String(object.min_paychain_stake)
        : "",
      routingEnabled: isSet(object.routingEnabled)
        ? globalThis.Boolean(object.routingEnabled)
        : isSet(object.routing_enabled)
        ? globalThis.Boolean(object.routing_enabled)
        : false,
      routingConfidenceThreshold: isSet(object.routingConfidenceThreshold)
        ? globalThis.String(object.routingConfidenceThreshold)
        : isSet(object.routing_confidence_threshold)
        ? globalThis.String(object.routing_confidence_threshold)
        : "",
      crossLayerFeeBundling: isSet(object.crossLayerFeeBundling)
        ? globalThis.Boolean(object.crossLayerFeeBundling)
        : isSet(object.cross_layer_fee_bundling)
        ? globalThis.Boolean(object.cross_layer_fee_bundling)
        : false,
    };
  },

  toJSON(message: ParamsView): unknown {
    const obj: any = {};
    if (message.maxSidechains !== "0") {
      obj.maxSidechains = message.maxSidechains;
    }
    if (message.maxPaychains !== "0") {
      obj.maxPaychains = message.maxPaychains;
    }
    if (message.minAnchorInterval !== "0") {
      obj.minAnchorInterval = message.minAnchorInterval;
    }
    if (message.maxAnchorInterval !== "0") {
      obj.maxAnchorInterval = message.maxAnchorInterval;
    }
    if (message.defaultChallengePeriod !== "0") {
      obj.defaultChallengePeriod = message.defaultChallengePeriod;
    }
    if (message.minSidechainStake !== "") {
      obj.minSidechainStake = message.minSidechainStake;
    }
    if (message.minPaychainStake !== "") {
      obj.minPaychainStake = message.minPaychainStake;
    }
    if (message.routingEnabled !== false) {
      obj.routingEnabled = message.routingEnabled;
    }
    if (message.routingConfidenceThreshold !== "") {
      obj.routingConfidenceThreshold = message.routingConfidenceThreshold;
    }
    if (message.crossLayerFeeBundling !== false) {
      obj.crossLayerFeeBundling = message.crossLayerFeeBundling;
    }
    return obj;
  },

  create(base?: DeepPartial<ParamsView>): ParamsView {
    return ParamsView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ParamsView>): ParamsView {
    const message = createBaseParamsView();
    message.maxSidechains = object.maxSidechains ?? "0";
    message.maxPaychains = object.maxPaychains ?? "0";
    message.minAnchorInterval = object.minAnchorInterval ?? "0";
    message.maxAnchorInterval = object.maxAnchorInterval ?? "0";
    message.defaultChallengePeriod = object.defaultChallengePeriod ?? "0";
    message.minSidechainStake = object.minSidechainStake ?? "";
    message.minPaychainStake = object.minPaychainStake ?? "";
    message.routingEnabled = object.routingEnabled ?? false;
    message.routingConfidenceThreshold = object.routingConfidenceThreshold ?? "";
    message.crossLayerFeeBundling = object.crossLayerFeeBundling ?? false;
    return message;
  },
};

function createBaseLayerView(): LayerView {
  return {
    layerId: "",
    layerType: "",
    status: "",
    chainId: "",
    description: "",
    targetBlockTimeMs: "0",
    maxTransactionsPerBlock: "0",
    minValidators: 0,
    settlementIntervalBlocks: "0",
    challengePeriodSeconds: "0",
  };
}

export const LayerView: MessageFns<LayerView> = {
  encode(message: LayerView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.layerId !== "") {
      writer.uint32(10).string(message.layerId);
    }
    if (message.layerType !== "") {
      writer.uint32(18).string(message.layerType);
    }
    if (message.status !== "") {
      writer.uint32(26).string(message.status);
    }
    if (message.chainId !== "") {
      writer.uint32(34).string(message.chainId);
    }
    if (message.description !== "") {
      writer.uint32(42).string(message.description);
    }
    if (message.targetBlockTimeMs !== "0") {
      writer.uint32(48).uint64(message.targetBlockTimeMs);
    }
    if (message.maxTransactionsPerBlock !== "0") {
      writer.uint32(56).uint64(message.maxTransactionsPerBlock);
    }
    if (message.minValidators !== 0) {
      writer.uint32(64).uint32(message.minValidators);
    }
    if (message.settlementIntervalBlocks !== "0") {
      writer.uint32(72).uint64(message.settlementIntervalBlocks);
    }
    if (message.challengePeriodSeconds !== "0") {
      writer.uint32(80).uint64(message.challengePeriodSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LayerView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLayerView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.layerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.layerType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.chainId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.targetBlockTimeMs = reader.uint64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxTransactionsPerBlock = reader.uint64().toString();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.minValidators = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.settlementIntervalBlocks = reader.uint64().toString();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.challengePeriodSeconds = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LayerView {
    return {
      layerId: isSet(object.layerId)
        ? globalThis.String(object.layerId)
        : isSet(object.layer_id)
        ? globalThis.String(object.layer_id)
        : "",
      layerType: isSet(object.layerType)
        ? globalThis.String(object.layerType)
        : isSet(object.layer_type)
        ? globalThis.String(object.layer_type)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      chainId: isSet(object.chainId)
        ? globalThis.String(object.chainId)
        : isSet(object.chain_id)
        ? globalThis.String(object.chain_id)
        : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      targetBlockTimeMs: isSet(object.targetBlockTimeMs)
        ? globalThis.String(object.targetBlockTimeMs)
        : isSet(object.target_block_time_ms)
        ? globalThis.String(object.target_block_time_ms)
        : "0",
      maxTransactionsPerBlock: isSet(object.maxTransactionsPerBlock)
        ? globalThis.String(object.maxTransactionsPerBlock)
        : isSet(object.max_transactions_per_block)
        ? globalThis.String(object.max_transactions_per_block)
        : "0",
      minValidators: isSet(object.minValidators)
        ? globalThis.Number(object.minValidators)
        : isSet(object.min_validators)
        ? globalThis.Number(object.min_validators)
        : 0,
      settlementIntervalBlocks: isSet(object.settlementIntervalBlocks)
        ? globalThis.String(object.settlementIntervalBlocks)
        : isSet(object.settlement_interval_blocks)
        ? globalThis.String(object.settlement_interval_blocks)
        : "0",
      challengePeriodSeconds: isSet(object.challengePeriodSeconds)
        ? globalThis.String(object.challengePeriodSeconds)
        : isSet(object.challenge_period_seconds)
        ? globalThis.String(object.challenge_period_seconds)
        : "0",
    };
  },

  toJSON(message: LayerView): unknown {
    const obj: any = {};
    if (message.layerId !== "") {
      obj.layerId = message.layerId;
    }
    if (message.layerType !== "") {
      obj.layerType = message.layerType;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.chainId !== "") {
      obj.chainId = message.chainId;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.targetBlockTimeMs !== "0") {
      obj.targetBlockTimeMs = message.targetBlockTimeMs;
    }
    if (message.maxTransactionsPerBlock !== "0") {
      obj.maxTransactionsPerBlock = message.maxTransactionsPerBlock;
    }
    if (message.minValidators !== 0) {
      obj.minValidators = Math.round(message.minValidators);
    }
    if (message.settlementIntervalBlocks !== "0") {
      obj.settlementIntervalBlocks = message.settlementIntervalBlocks;
    }
    if (message.challengePeriodSeconds !== "0") {
      obj.challengePeriodSeconds = message.challengePeriodSeconds;
    }
    return obj;
  },

  create(base?: DeepPartial<LayerView>): LayerView {
    return LayerView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LayerView>): LayerView {
    const message = createBaseLayerView();
    message.layerId = object.layerId ?? "";
    message.layerType = object.layerType ?? "";
    message.status = object.status ?? "";
    message.chainId = object.chainId ?? "";
    message.description = object.description ?? "";
    message.targetBlockTimeMs = object.targetBlockTimeMs ?? "0";
    message.maxTransactionsPerBlock = object.maxTransactionsPerBlock ?? "0";
    message.minValidators = object.minValidators ?? 0;
    message.settlementIntervalBlocks = object.settlementIntervalBlocks ?? "0";
    message.challengePeriodSeconds = object.challengePeriodSeconds ?? "0";
    return message;
  },
};

function createBaseRoutingStatsView(): RoutingStatsView {
  return {
    totalRouted: "0",
    routedToMain: "0",
    routedToSidechains: "0",
    routedToPaychains: "0",
    averageGasSavingsPercent: "",
    averageLatencyImprovementPercent: "",
  };
}

export const RoutingStatsView: MessageFns<RoutingStatsView> = {
  encode(message: RoutingStatsView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totalRouted !== "0") {
      writer.uint32(8).uint64(message.totalRouted);
    }
    if (message.routedToMain !== "0") {
      writer.uint32(16).uint64(message.routedToMain);
    }
    if (message.routedToSidechains !== "0") {
      writer.uint32(24).uint64(message.routedToSidechains);
    }
    if (message.routedToPaychains !== "0") {
      writer.uint32(32).uint64(message.routedToPaychains);
    }
    if (message.averageGasSavingsPercent !== "") {
      writer.uint32(42).string(message.averageGasSavingsPercent);
    }
    if (message.averageLatencyImprovementPercent !== "") {
      writer.uint32(50).string(message.averageLatencyImprovementPercent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoutingStatsView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoutingStatsView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.totalRouted = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.routedToMain = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.routedToSidechains = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.routedToPaychains = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.averageGasSavingsPercent = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.averageLatencyImprovementPercent = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoutingStatsView {
    return {
      totalRouted: isSet(object.totalRouted)
        ? globalThis.String(object.totalRouted)
        : isSet(object.total_routed)
        ? globalThis.String(object.total_routed)
        : "0",
      routedToMain: isSet(object.routedToMain)
        ? globalThis.String(object.routedToMain)
        : isSet(object.routed_to_main)
        ? globalThis.String(object.routed_to_main)
        : "0",
      routedToSidechains: isSet(object.routedToSidechains)
        ? globalThis.String(object.routedToSidechains)
        : isSet(object.routed_to_sidechains)
        ? globalThis.String(object.routed_to_sidechains)
        : "0",
      routedToPaychains: isSet(object.routedToPaychains)
        ? globalThis.String(object.routedToPaychains)
        : isSet(object.routed_to_paychains)
        ? globalThis.String(object.routed_to_paychains)
        : "0",
      averageGasSavingsPercent: isSet(object.averageGasSavingsPercent)
        ? globalThis.String(object.averageGasSavingsPercent)
        : isSet(object.average_gas_savings_percent)
        ? globalThis.String(object.average_gas_savings_percent)
        : "",
      averageLatencyImprovementPercent: isSet(object.averageLatencyImprovementPercent)
        ? globalThis.String(object.averageLatencyImprovementPercent)
        : isSet(object.average_latency_improvement_percent)
        ? globalThis.String(object.average_latency_improvement_percent)
        : "",
    };
  },

  toJSON(message: RoutingStatsView): unknown {
    const obj: any = {};
    if (message.totalRouted !== "0") {
      obj.totalRouted = message.totalRouted;
    }
    if (message.routedToMain !== "0") {
      obj.routedToMain = message.routedToMain;
    }
    if (message.routedToSidechains !== "0") {
      obj.routedToSidechains = message.routedToSidechains;
    }
    if (message.routedToPaychains !== "0") {
      obj.routedToPaychains = message.routedToPaychains;
    }
    if (message.averageGasSavingsPercent !== "") {
      obj.averageGasSavingsPercent = message.averageGasSavingsPercent;
    }
    if (message.averageLatencyImprovementPercent !== "") {
      obj.averageLatencyImprovementPercent = message.averageLatencyImprovementPercent;
    }
    return obj;
  },

  create(base?: DeepPartial<RoutingStatsView>): RoutingStatsView {
    return RoutingStatsView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RoutingStatsView>): RoutingStatsView {
    const message = createBaseRoutingStatsView();
    message.totalRouted = object.totalRouted ?? "0";
    message.routedToMain = object.routedToMain ?? "0";
    message.routedToSidechains = object.routedToSidechains ?? "0";
    message.routedToPaychains = object.routedToPaychains ?? "0";
    message.averageGasSavingsPercent = object.averageGasSavingsPercent ?? "";
    message.averageLatencyImprovementPercent = object.averageLatencyImprovementPercent ?? "";
    return message;
  },
};

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      ParamsView.encode(message.params, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = ParamsView.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? ParamsView.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = ParamsView.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? ParamsView.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryLayerRequest(): QueryLayerRequest {
  return { layerId: "" };
}

export const QueryLayerRequest: MessageFns<QueryLayerRequest> = {
  encode(message: QueryLayerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.layerId !== "") {
      writer.uint32(10).string(message.layerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryLayerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryLayerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.layerId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryLayerRequest {
    return {
      layerId: isSet(object.layerId)
        ? globalThis.String(object.layerId)
        : isSet(object.layer_id)
        ? globalThis.String(object.layer_id)
        : "",
    };
  },

  toJSON(message: QueryLayerRequest): unknown {
    const obj: any = {};
    if (message.layerId !== "") {
      obj.layerId = message.layerId;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryLayerRequest>): QueryLayerRequest {
    return QueryLayerRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryLayerRequest>): QueryLayerRequest {
    const message = createBaseQueryLayerRequest();
    message.layerId = object.layerId ?? "";
    return message;
  },
};

function createBaseQueryLayerResponse(): QueryLayerResponse {
  return { layer: undefined };
}

export const QueryLayerResponse: MessageFns<QueryLayerResponse> = {
  encode(message: QueryLayerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.layer !== undefined) {
      LayerView.encode(message.layer, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryLayerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryLayerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.layer = LayerView.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryLayerResponse {
    return { layer: isSet(object.layer) ? LayerView.fromJSON(object.layer) : undefined };
  },

  toJSON(message: QueryLayerResponse): unknown {
    const obj: any = {};
    if (message.layer !== undefined) {
      obj.layer = LayerView.toJSON(message.layer);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryLayerResponse>): QueryLayerResponse {
    return QueryLayerResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryLayerResponse>): QueryLayerResponse {
    const message = createBaseQueryLayerResponse();
    message.layer = (object.layer !== undefined && object.layer !== null)
      ? LayerView.fromPartial(object.layer)
      : undefined;
    return message;
  },
};

function createBaseQueryLayersRequest(): QueryLayersRequest {
  return {};
}

export const QueryLayersRequest: MessageFns<QueryLayersRequest> = {
  encode(_: QueryLayersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryLayersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryLayersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryLayersRequest {
    return {};
  },

  toJSON(_: QueryLayersRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryLayersRequest>): QueryLayersRequest {
    return QueryLayersRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryLayersRequest>): QueryLayersRequest {
    const message = createBaseQueryLayersRequest();
    return message;
  },
};

function createBaseQueryLayersResponse(): QueryLayersResponse {
  return { layers: [] };
}

export const QueryLayersResponse: MessageFns<QueryLayersResponse> = {
  encode(message: QueryLayersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.layers) {
      LayerView.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryLayersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryLayersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.layers.push(LayerView.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryLayersResponse {
    return {
      layers: globalThis.Array.isArray(object?.layers) ? object.layers.map((e: any) => LayerView.fromJSON(e)) : [],
    };
  },

  toJSON(message: QueryLayersResponse): unknown {
    const obj: any = {};
    if (message.layers?.length) {
      obj.layers = message.layers.map((e) => LayerView.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<QueryLayersResponse>): QueryLayersResponse {
    return QueryLayersResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryLayersResponse>): QueryLayersResponse {
    const message = createBaseQueryLayersResponse();
    message.layers = object.layers?.map((e) => LayerView.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryRoutingStatsRequest(): QueryRoutingStatsRequest {
  return {};
}

export const QueryRoutingStatsRequest: MessageFns<QueryRoutingStatsRequest> = {
  encode(_: QueryRoutingStatsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRoutingStatsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRoutingStatsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryRoutingStatsRequest {
    return {};
  },

  toJSON(_: QueryRoutingStatsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryRoutingStatsRequest>): QueryRoutingStatsRequest {
    return QueryRoutingStatsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryRoutingStatsRequest>): QueryRoutingStatsRequest {
    const message = createBaseQueryRoutingStatsRequest();
    return message;
  },
};

function createBaseQueryRoutingStatsView(): QueryRoutingStatsView {
  return { stats: undefined };
}

export const QueryRoutingStatsView: MessageFns<QueryRoutingStatsView> = {
  encode(message: QueryRoutingStatsView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stats !== undefined) {
      RoutingStatsView.encode(message.stats, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRoutingStatsView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRoutingStatsView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stats = RoutingStatsView.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRoutingStatsView {
    return { stats: isSet(object.stats) ? RoutingStatsView.fromJSON(object.stats) : undefined };
  },

  toJSON(message: QueryRoutingStatsView): unknown {
    const obj: any = {};
    if (message.stats !== undefined) {
      obj.stats = RoutingStatsView.toJSON(message.stats);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRoutingStatsView>): QueryRoutingStatsView {
    return QueryRoutingStatsView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRoutingStatsView>): QueryRoutingStatsView {
    const message = createBaseQueryRoutingStatsView();
    message.stats = (object.stats !== undefined && object.stats !== null)
      ? RoutingStatsView.fromPartial(object.stats)
      : undefined;
    return message;
  },
};

/** Query defines the gRPC query service for the multilayer module. */
export type QueryDefinition = typeof QueryDefinition;
export const QueryDefinition = {
  name: "Query",
  fullName: "qorechain.multilayer.v1.Query",
  methods: {
    /** Params returns the module parameters. */
    params: {
      name: "Params",
      requestType: QueryParamsRequest as typeof QueryParamsRequest,
      requestStream: false,
      responseType: QueryParamsResponse as typeof QueryParamsResponse,
      responseStream: false,
      options: {},
    },
    /** Layer returns a single layer config by ID. */
    layer: {
      name: "Layer",
      requestType: QueryLayerRequest as typeof QueryLayerRequest,
      requestStream: false,
      responseType: QueryLayerResponse as typeof QueryLayerResponse,
      responseStream: false,
      options: {},
    },
    /** Layers lists all layers. */
    layers: {
      name: "Layers",
      requestType: QueryLayersRequest as typeof QueryLayersRequest,
      requestStream: false,
      responseType: QueryLayersResponse as typeof QueryLayersResponse,
      responseStream: false,
      options: {},
    },
    /** RoutingStats returns the cross-layer routing statistics. */
    routingStats: {
      name: "RoutingStats",
      requestType: QueryRoutingStatsRequest as typeof QueryRoutingStatsRequest,
      requestStream: false,
      responseType: QueryRoutingStatsView as typeof QueryRoutingStatsView,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
