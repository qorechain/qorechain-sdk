[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "qorechain"
version = "0.1.0"
description = "Python SDK for QoreChain — networks, accounts (native/EVM/SVM), post-quantum signing, and read clients."
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
authors = [{ name = "Liviu Epure", email = "liviu.etty@gmail.com" }]
keywords = ["qorechain", "blockchain", "cosmos", "evm", "svm", "post-quantum", "ml-dsa", "dilithium"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Typing :: Typed",
]
dependencies = [
  "httpx>=0.27,<1",
  "bip-utils>=2.9,<3",
  "bech32>=1.2,<2",
  "base58>=2.1,<3",
  "dilithium-py>=1.1",
  "cosmpy>=0.11,<1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "pytest-asyncio>=0.23",
  "respx>=0.21",
  "mypy>=1.10",
  "ruff>=0.5",
]

[project.urls]
Homepage = "https://github.com/qorechain/qorechain-sdk"
Repository = "https://github.com/qorechain/qorechain-sdk"
Issues = "https://github.com/qorechain/qorechain-sdk/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/qorechain"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.mypy]
python_version = "3.10"
strict = true
files = ["src/qorechain"]

# Third-party crypto libs ship no type stubs / py.typed marker.
[[tool.mypy.overrides]]
module = [
  "bip_utils.*",
  "dilithium_py.*",
  "bech32.*",
  "base58.*",
  "cosmpy.*",
  "google.protobuf.*",
]
ignore_missing_imports = true

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
